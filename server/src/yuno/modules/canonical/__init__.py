"""The `canonical` module: canonical curriculum graph versions, topics,
relations, content revisions and editorial approvals (spec §4.3).

No other module may import this module's ORM types or repositories, and
this module imports no other module's, per the `Module independence
(spec §3.3)` import-linter contract in `pyproject.toml`.
"""
