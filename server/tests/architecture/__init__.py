"""Architecture tests: import boundaries and schema conventions (IDK-101).

These tests enforce spec §3.2 (module dependency direction, framework-free
domain/application) and spec §4.1 (database conventions) mechanically, so
the rules are self-verifying rather than dependent on code review.
"""
