# IDK-503 (re-run) gate 7 — Runner posture and relational absence

- Gate: Runner posture and relational absence (RUN-03)
- Reviewer role required: security/engineering owner
- Inspection date: 2026-08-15
- Disposition: blocking-finding
- Attestation: pending -- security/engineering owner has not signed this gate. This document records an independent re-inspection; it grants no approval and is not itself an attestation.

This is a full re-run of the prior 2026-08-14 gate-7 review (`docs/approvals/IDK-503/gate-7-runner-posture.md`, read only for scope), against the tree as it stands on 2026-08-15, after two commits (`6bebd2f`, `2621d29`) claimed to close two of that round's three blocking findings. Every claim below was re-derived directly against the current tree; nothing is inherited from the prior document without independent verification.

## Inspected artifacts

| Artifact | What it is | How inspected |
| --- | --- | --- |
| `docs/decisions/IDK-005-local-runner-support-matrix.md` (v1.0) | Toolchain/platform matrix decision | Read in full |
| `docs/decisions/IDK-007-runner-enablement-and-resource-posture.md` (v1.0) | Enablement/limits/isolation decision | Read in full |
| `docs/decisions/IDK-008-database-exercise-posture.md` (v1.0) | Relational-absence decision | Read in full |
| `docs/runner/IDK-406-execution-deferral.md` | Recorded accepted-risk deferral of the IDK-007 isolation layer | Read in full |
| `docs/approvals/IDK-503/gate-7-runner-posture.md` | Prior round's gate record | Read for scope only, per instruction |
| `server/src/yuno/modules/runner/platform_probe.py` (new file, 170 lines) | `PlatformSnapshot`, `evaluate_platform`, `default_platform_snapshot` -- IDK-005 §3 OS/arch/WSL/container detection | Read in full |
| `server/src/yuno/modules/runner/adapters.py` | `detect_command` (now gates on `evaluate_platform` before any command check), `LocalTempWorkspace`, `LocalRunnerProcessPort.run` | Read in full |
| `server/src/yuno/modules/runner/service.py` | `policy_ready`, `capabilities`, `minimal_environment`, `RUNNER_LIMITATION` | Read in full |
| `server/src/yuno/modules/runner/domain.py` | `RunnerLanguage` (Java-only), `RunnerOperation`, `RUNNER_LIMITATION` | Read in full |
| `server/src/yuno/api/dependencies.py` | `get_owner_id` (now a plain `app.state.owner_id` read, no `Depends(get_unit_of_work)`) | Read in full |
| `server/src/yuno/api/app.py:225-260,897-908` | Lifespan startup: `ensure_local_owner` runs before traffic is accepted; `app.state.owner_id = owner.id` cached once | Read source |
| `server/src/yuno/modules/identity/repository.py` | `SqlAlchemyOwnerRepository`: only `get_local_owner`/`create_local_owner` exist, no update/delete method | Read in full |
| `server/src/yuno/config.py:40-104` | `Settings`: `runner_enabled=False`; 11 nullable `runner_*` policy fields (not nine, see Finding — corrected from the prior round); 9 further `runner_*` fields with fixed non-null `Literal` defaults | Read source |
| `server/src/yuno/api/routes/runner.py` | `confirm_runner` (`POST /runner/confirmations`) declares only `Depends(get_owner_id)`, no `Depends(get_unit_of_work)` of its own | Read source |
| `server/tests/integration/test_runner.py` (now 45 tests, up from the prior round's 24) | Full suite, plus targeted named tests | Ran `uv run pytest tests/integration/test_runner.py -v` (45 passed) and targeted runs below |
| `server/tests/integration/test_alembic_representative_upgrades.py:1420` | `test_relational_placeholder_disposal_is_bounded_and_controls_survive` | Ran directly (1 passed) |
| Two ad hoc scripts against fresh scratch SQLite DBs migrated to head via the real `alembic_guard`/`command.upgrade` path, `create_app`/`TestClient` | Independent empirical re-derivation of the B14 UoW-count claim and the disabled-capabilities response, written to `/private/tmp/.../scratchpad/verify_b14.py` and `verify_capabilities.py`, never touching `server/yuno.db`/`server/.e2e.db` | Ran via `uv run python <script>` |
| `detect_command("java","compile-and-test","javac","21")` and `("java","compile-and-test","javac","")`, invoked live on this host | Empirical B16 re-check | Ran via `uv run python -c ...` |
| `server/yuno.db`, `server/.e2e.db` | Local dev/e2e SQLite databases | `sqlite3 <path> "SELECT ..."` -- read-only `SELECT` statements only, no writes |
| `src/selected/core/HandsOnLab.tsx`, `src/selected/operations/OperationalPages.tsx` | Shipped learner-facing runner copy | `grep`, then read exact lines |
| `src/shared/api/schema.d.ts` | Generated OpenAPI client types | `grep` for connector/relational terms |
| `grep` across `server/src`, `src/` | connector/endpoint/jdbc/dsn/socket/psycopg/pymysql/create_engine, `runner_enabled`/`desired_enabled` in settings/contracts, `OwnerRow` usage, raw `owners` table deletion | Zero matches for every prohibited term outside the one docstring noted in Findings |

## Findings

| # | Decision requirement | Shipped reality (with citation) | Verdict |
| --- | --- | --- | --- |
| B16 | IDK-005 §1/§3: platform/architecture/WSL/container gate must run before any command/version check; macOS must not report `supported` | `platform_probe.py` (new) implements bounded `/etc/os-release` read (`_bounded_read`, 8192-byte cap), `ID`/`VERSION_ID` parsing, arch normalization, WSL detection via `WSL_INTEROP`/`WSL_DISTRO_NAME` env vars or case-insensitive `microsoft` in `/proc/sys/kernel/osrelease`, and container detection via `/.dockerenv`, `/run/.containerenv`, or `docker`/`containerd`/`kubepods`/`lxc` markers in `/proc/1/cgroup` -- matching IDK-005 §1's exact detection list. `detect_command` (`adapters.py:66-74`) calls `evaluate_platform(platform_probe())` first and returns `incompatible`/`unsupported-platform` or `platform-unverifiable` before touching `shutil.which`. **Empirically re-run on this macOS/Darwin host**: `detect_command("java","compile-and-test","javac","21")` → `{'state': 'incompatible', 'diagnostic_code': 'unsupported-platform', 'detail': 'Host platform, architecture, or virtualization layer is outside the IDK-005 approved runner matrix (Ubuntu 24.04 LTS, x86_64 or arm64, host or VM only).'}`; same result with an empty prefix. This is the exact opposite of the prior round's `supported` result. | **CLOSED** |
| B14 | IDK-008 §3/§6: the retired `"language":"relational"` signature "returns the standard `422` validation envelope before the route/UoW," zero side effects | `get_owner_id` (`dependencies.py:26-36`) is now `return request.app.state.owner_id` -- no `Depends`, no SQL. `app.state.owner_id = owner.id` is set once at `app.py:908`, after `ensure_local_owner`/`ensure_profile`/`ensure_owner_settings` commit (`app.py:238-242`) and before `yield` releases traffic (`app.py:910`). **Independently re-measured** (script below, not the shipped test): `POST /api/v1/runner/confirmations` with `"language":"relational"` on a fresh scratch DB migrated to head → `uow_calls=0`, `sql_statements=[]`, `pool_checkouts=0`, response `422` `{'code': 'request_validation_error', ..., 'field_errors': [{'field': 'body.language', 'message': "Input should be 'java'"}]}`, `runner_confirmations`=0 rows, `jobs`=0 rows. `confirm_runner` (`routes/runner.py:87-90`) declares only `Depends(get_owner_id)`, not its own `Depends(get_unit_of_work)`, so IDK-008's literal "before ... any unit-of-work is opened" wording now holds **for this exact named route**. | **CLOSED** (for the route IDK-008 names; see caveat below) |
| B14 safety | Is caching `owner_id` on `app.state` safe -- can anything delete/replace the owner row at runtime? | `SqlAlchemyOwnerRepository` (`identity/repository.py`) exposes exactly two methods, `get_local_owner` and `create_local_owner`; neither updates nor deletes an `OwnerRow`. `grep -rn "OwnerRow" server/src/yuno` finds only the model definition and these two repository methods -- no update/delete path exists anywhere in the codebase. `grep` for raw `owners` table references in `server/src/yuno/modules/data_lifecycle/` (IDK-010's delete/retention module) returns nothing outside `owner_id`/`owner_scoped` column references. The `owners` table carries no self-referential cascade. **Safe**: the cached id cannot go stale within a process lifetime. | Confirmed safe |
| B14 systemic claim | Fix must be systemic, not special-cased to one route | `grep -rc "Depends(get_owner_id)" server/src/yuno/api/routes/*.py` → 115 occurrences across 18 route files (commit message claims 19 routers; 18 measured -- immaterial, does not change the systemic conclusion). `test_job_retry_extra_field_422_opens_no_uow` (`test_runner.py:398`) proves the same zero-UoW property on an unrelated route, `POST /jobs/{id}/retry`. Both named regression tests ran directly and passed. | Confirmed systemic |
| B14 caveat | Routes that themselves declare `Depends(get_unit_of_work)` | Unchanged and correctly so -- e.g. any route with its own `Depends(get_unit_of_work)` still opens one for its own work regardless of `get_owner_id`. IDK-008 §3/§65 names specifically the retired relational-confirmation signature on `POST /runner/confirmations`, and that route does not declare its own UoW dependency, so the requirement is satisfied as literally written for the route it names. It says nothing about other routes' own UoW use, so there is no remaining gap against the decision text. | Not a gap |
| B15 | PRD Appendix C rows 3 ("process permissions define remaining host access risk"), 4 ("local machine policy remains outside product control"), 6 ("OS failures may require manual recovery") each require distinct residual copy | `grep -rn "host access\|machine policy\|manual recovery" src/` → one hit, `HandsOnLab.tsx:105`: `Cleanup failed. A process or temporary file may require manual recovery. {run.cleanup_diagnostic}`. This string exists (added in `ba140c8`, predates both rounds) but does **not** clear row 6: it is a conditional error-state message that renders only when `run.cleanup_state === 'cleanup-failed'`, not a standing residual-risk disclosure shown wherever the runner disclosure appears (the way rows 1/2/5's generic string is). It also has no counterpart at all for rows 3 or 4. The only disclosure shown unconditionally, everywhere a runner disclosure appears, remains the single generic string quoted below. No new decision has been recorded since the prior round consolidating the six rows into one disclosure (checked `git log --since 2026-08-14 -- docs/decisions/ docs/runner/`; only IDK-406 itself, already reviewed, and unrelated IDK-001/002/003 approvals appear). `6bebd2f`'s own commit message lists B15 under "Left open, needing an owner decision rather than code." | **STILL OPEN** |

## Appendix C — six threat/limitation rows, re-dispositioned independently

| Row | Threat/limitation | MVP control shipped? | Residual statement labelled in-product? | Disposition |
| --- | --- | --- | --- | --- |
| 1 | Shell injection via learner input | Yes -- `subprocess.Popen(list(spec.argv), shell=False, ...)` (`adapters.py:137-139`); `detect_command`'s own version probe likewise `shell=False` (`adapters.py:85-92`) | Yes -- `HandsOnLab.tsx:102,116`: "Controlled subprocess execution only. This is not a sandbox or hostile-code isolation, and it is not proof of production or AWS behavior." | Clear |
| 2 | Excess CPU/wall-time/output consumption | Yes, at the level actually built -- `apply_limits()` sets `RLIMIT_CPU`/`RLIMIT_AS` (non-Darwin only)/`RLIMIT_NPROC`/`RLIMIT_FSIZE` (`adapters.py:128-134`); poll loop enforces wall/output/temp budgets. **Note**, already recorded and not reopened here: `IDK-406-execution-deferral.md` §2 documents that IDK-007 §5 explicitly rejects `RLIMIT_NPROC`/`RLIMIT_AS`/`RLIMIT_CPU` as insufficient for the aggregate-run guarantee it requires (cgroup v2 is the approved mechanism); that gap is the subject of the accepted-risk record, not a fresh Appendix-C finding | Yes -- same generic string | Clear (control shipped is the accepted-risk-adjusted one, not IDK-007's full cgroup posture; already dispositioned by `IDK-406-execution-deferral.md`) |
| 3 | File-system pollution / host access via process permissions | Yes -- `LocalTempWorkspace.create`/`cleanup` (`adapters.py:111-121`) refuses to `rmtree` anything whose parent isn't `gettempdir()` or whose name lacks the `yuno-runner-` prefix | **No** -- no row-3-specific residual copy anywhere in `src/`; only the generic string above | **Open (B15)** |
| 4 | Environment/secrets leakage into the learner process | Yes -- `minimal_environment()` allowlists only `PATH`/`LANG`/`LC_ALL`/`TZ` then strips any key matching `AWS_`/`SECRET`/`TOKEN`/`PASSWORD`/`CREDENTIAL`/`CONNECTION_STRING`/`DATABASE_URL` (`service.py:264-273`, forbidden-marker list at `service.py:49-55`) | **No** -- no row-4-specific residual copy; only the generic string | **Open (B15)** |
| 5 | Misleading validation (compile/test/static conflated) | Yes -- `RunnerOperation.COMPILE`/`TEST` are distinct (`domain.py:32-34`); static hands-on review is a separate code path in `hands_on/service.py` that never invokes the runner; `RUNNER_LIMITATION` surfaced at `service.py:105,129,696,740` | Yes -- same generic string, plus `HandsOnLab.tsx:102`'s explicit "Runtime execution is separate and cannot create hands-on evidence" clause | Clear |
| 6 | Orphaned process after cancellation/crash | Yes -- `request_termination()` (`adapters.py:165-214`) sends `SIGTERM` via `os.killpg(pgid, ...)`, escalates to `SIGKILL` if still alive | **No** -- the only row-6-adjacent string, `HandsOnLab.tsx:105`'s conditional cleanup-failure message, is scoped to one failure branch and not a standing disclosure; the unconditional disclosure remains the generic string | **Open (B15)** |

## Relational absence (IDK-008) — re-verified empirically

- `RunnerLanguage` admits only `java`: `uv run python -c "from yuno.modules.runner.domain import RunnerLanguage; print(list(RunnerLanguage))"` → `[<RunnerLanguage.JAVA: 'java'>]`.
- The retired `"language":"relational"` confirmation returns the standard `422` with zero persisted rows -- reproduced independently (see B14 row above and the Blocking findings evidence): `runner_confirmations`=0, `jobs`=0 after the call, on a scratch DB never touched by any prior test run.
- No connector credential/endpoint field anywhere: `grep -rn "connector\|jdbc\|dsn" server/src/yuno/config.py server/src/yuno/api/contracts.py server/src/yuno/modules/runner/*.py` → zero matches. `grep` of `src/shared/api/schema.d.ts` for the same terms finds exactly one hit, a docstring at line 4218 explaining that `python`/`relational` were removed -- no field.
- No runner path opens a database socket or process: `grep -rn "socket\|psycopg\|pymysql\|create_engine\|jdbc" server/src/yuno/modules/runner/*.py` → zero matches.
- Migration `c5b1e70a94d2` narrowed the DB-level CHECK: confirmed live on `server/yuno.db` (`sqlite3 yuno.db "SELECT sql FROM sqlite_master WHERE name='runner_confirmations'"` → `CONSTRAINT ck_runner_confirmations_language_valid CHECK (language IN ('java'))`), even though that database is two migrations behind head (see below) -- the narrowing migration is earlier in the chain than the DB's current revision.
- `test_relational_placeholder_disposal_is_bounded_and_controls_survive` (`test_alembic_representative_upgrades.py:1420`, not line 1150 as the prior round cited -- line drift from intervening commits) ran directly: 1 passed.
- IDK-008 §4 RDB-static-review wording: still not shipped -- `grep -rln "RDB\|relational" server/src/yuno/modules/hands_on/` returns nothing; the only "RDB" strings anywhere are incidental test-fixture data in `test_diagnostics_repository.py:62,126`, unrelated to review wording. Recorded as "not shipped" (nothing yet for the requirement to apply to), not "mismatch," consistent with the prior round.

## Runner disabled state — re-verified, not inherited

- `runner_enabled=False` -- confirmed via `Settings()` default and live `policy_ready()` call: `False`.
- **Correction to the prior round's count**: the prior gate document (and the review instructions carrying its language forward) state "the nine `runner_*` policy fields are `None`." Measured directly against the current `Settings` class: **11**, not 9, `runner_*` fields default to `None` (`runner_environment_policy_version`, `runner_limits_config_version`, `runner_confirmation_ttl_seconds`, `runner_wall_time_seconds`, `runner_cpu_seconds`, `runner_memory_bytes`, `runner_process_limit`, `runner_file_bytes`, `runner_javac_command`, `runner_java_command`, `runner_java_version_prefix` -- `config.py:94-104`). `policy_ready()` (`service.py:65-86`) actually requires 18 values non-`None`, because it also requires 7 further `runner_*` fields that carry fixed non-`None` `Literal` defaults (`runner_input_files_limit=100`, `runner_input_bytes_limit`, `runner_stdout_bytes_limit`, `runner_stderr_bytes_limit`, `runner_output_bytes`, `runner_temp_bytes`, `runner_temp_files_limit` -- `config.py:40-46`) plus `runner_enabled` itself. The net effect the prior document reported -- `policy_ready()` is `False` and stays `False` until every value is explicitly configured -- still holds; only the specific field count was wrong and is corrected here rather than repeated.
- `policy_ready()` is `False` -- confirmed live.
- `GET /runner/capabilities` reports `enabled: false` -- confirmed live on a fresh scratch DB migrated to head: `{'enabled': False, 'disabled_reason': 'Blocked by IDK-005 and IDK-007: approved toolchain matrix and complete limits are required.', ..., 'capabilities': []}`.
- `runner_confirmations`/`runner_records` are empty in both real local databases (read-only `SELECT`, no writes):
  - `server/yuno.db`: `runner_confirmations`=0, `runner_records`=0, `jobs`=0.
  - `server/.e2e.db`: `runner_confirmations`=0, `runner_records`=0.
- No Settings-exposed path to toggle `runner_enabled`: `grep -rn "runner_enabled\|desired_enabled" server/src/yuno/api/contracts.py server/src/yuno/modules/settings_data/*.py` → zero matches.

## Database revision note

- `server/yuno.db` is at Alembic revision `4747447ccaa3`. Current head (`uv run alembic heads`) is `be4d11f03666`. The chain between them is `4747447ccaa3 -> 4cb74877e4ba -> be4d11f03666` -- **exactly two revisions behind head**, confirming the instruction's claim.
- `server/.e2e.db` is at `a9d4e6f1b208`, materially further behind head and untouched by this inspection (read-only `SELECT` only).
- Both databases were read via plain `sqlite3 <path> "SELECT ..."`; no write statement was issued against either file at any point in this inspection.

## Blocking findings

### 1. No row-specific residual-statement copy for Appendix C rows 3, 4, and 6 (B15, still open)

- What is missing: PRD Appendix C rows 3 ("process permissions define remaining host access risk"), 4 ("local machine policy remains outside product control"), and 6 ("OS failures may require manual recovery") each specify a distinct residual statement. The shipped learner-facing UI surfaces one generic string everywhere a runner disclosure appears: `HandsOnLab.tsx:102`, `:116`, and `OperationalPages.tsx:314` all read verbatim **"Controlled subprocess execution only. This is not a sandbox or hostile-code isolation, and it is not proof of production or AWS behavior."** (the confirmation dialog at `:116` and the disclosure at `:102` additionally append "Runtime execution is separate and cannot create hands-on evidence."). The one row-6-adjacent string that exists, `HandsOnLab.tsx:105`'s cleanup-failure message, is a conditional error-state notice, not a standing residual disclosure, and has no counterpart for rows 3 or 4.
- Owning ticket: IDK-406
- Evidence of absence: `grep -rn "host access\|machine policy\|manual recovery" src/` finds exactly one hit (the conditional `HandsOnLab.tsx:105` string, which does not satisfy the requirement for the reason above); rows 3 and 4 have zero hits of any kind.
- What would clear it: Either ship per-row residual copy at the points where each risk is decision-relevant (e.g., alongside the workspace-cleanup/limit-breach/cancellation UI states), or -- the alternative the prior round explicitly named and this round confirms remains untaken -- record a new approved decision stating that the single consolidated disclosure is intended to subsume all six Appendix C rows. That decision does not yet exist (`git log --since 2026-08-14 -- docs/decisions/ docs/runner/` shows no such record); inventing the row-specific learner-facing wording here would itself be the fabrication this review is instructed not to commit, so it is recorded as open rather than resolved either way.

## Notes and residual risk

- **B16 (platform-detection gap) is closed.** This is the one genuine change in disposition versus the prior round: shipped detection now matches IDK-005 §1/§3 exactly, verified both by reading `platform_probe.py`/`adapters.py` and by re-running `detect_command` live on this exact macOS host, which now correctly reports `incompatible`/`unsupported-platform` instead of the prior round's `supported`. 17 platform-specific tests plus the full 45-test `test_runner.py` suite pass.
- **B14 (UoW-before-422 ordering) is closed for the named route.** Independently re-measured, not merely re-read: zero UoWs, zero SQL statements, zero pool checkouts on the retired-relational-signature request. The owner-id caching mechanism that makes this possible was independently checked for safety (no update/delete path for `OwnerRow` exists anywhere in the codebase) rather than taken on the commit message's word.
- **B15 (missing per-row residual copy) remains open**, exactly as the closing commit (`6bebd2f`) itself declared it should. This is not a regression or a new gap -- it is a decision the codebase cannot correctly resolve for itself, and inventing the wording would violate this review's own instructions. It is the sole reason this gate's disposition remains `blocking-finding` rather than `inspection-passed-pending-attestation`.
- The runner remains disabled end-to-end today: `runner_enabled=False`, `policy_ready()=False`, both real local databases show 0 rows in every runner-related table. No learner Java process has run against either database. This gate continues to be reviewed, correctly, against a disabled runner, as `IDK-406-execution-deferral.md` itself anticipates.
- `IDK-406-execution-deferral.md`'s accepted-risk record (POSIX `rlimit`s in place of the IDK-007 cgroup/namespace/syscall-filter boundary) is unchanged by this round's commits and is not reopened here; Appendix C row 2's disposition above notes it exists and is already the accepted, recorded position, not a fresh finding.
- One factual correction is carried in this record rather than silently inherited: the "nine `runner_*` policy fields" figure repeated from the prior round is wrong against the current tree -- it is 11 nullable fields (plus 7 further fixed-default fields also required by `policy_ready()`). The substantive conclusion (fail-closed, all required values absent, `policy_ready()` false) is unaffected.
- IDK-008's RDB-static-review wording requirement remains untestable because no RDB hands-on content has shipped; unchanged from the prior round, re-confirmed by direct `grep`.
