# IDK-503 rerun (2026-08-15, round 4) gate 5 — Representative assessment scenarios and rubric versions

- Gate: Assessment scenarios and rubric versions (HND-03, DEP-03)
- Reviewer role required: designated editorial approver
- Inspection date: 2026-08-15
- Disposition: blocking-finding
- Attestation: pending -- designated editorial approver has not signed this gate. No attestation was sought or recorded during this inspection.

This is round 4, a fresh independent re-inspection bound to `HEAD = 0f3219f6a8b2a2436fce6d36c918307655a53c84` (`0f3219f`), confirmed via `git rev-parse HEAD`. It is not a diff against round 3 and not an amendment of `docs/approvals/IDK-503/gate-5-rubrics-scenarios.md` (round 1), `docs/approvals/IDK-503-rerun-2026-08-15/gate-5-rubrics-scenarios.md` (round 2), or `docs/approvals/IDK-503-rerun-2026-08-15-b/gate-5-rubrics-scenarios.md` (round 3, `HEAD=7b805bd`). All three stand untouched. Round 3's file was read only to recover scope and the open-findings list, per instruction; every citation below — line numbers, row counts, test results — was independently re-derived against the current tree by direct reads, greps, a freshly migrated scratch database, and real test runs, not copied forward. Two earlier rounds already used the calendar date 2026-08-15; this round is identified by the tree it inspects, not the date, and takes the `-c` suffix. This gate was itself inspected in three passes within the round: first at `825a01f`, delta-checked against `636bf26` after a citation-repair commit landed, then delta-checked again against `0f3219f` after a test-declaration repair landed — see "HEAD delta check" below.

Decision under review: `docs/decisions/IDK-009-assessment-and-derived-state.md`, decision version 1.0, approved 2026-08-13 (content/assessment owner; §12). Implementing tickets referenced: IDK-201 (DEP-03), IDK-204, IDK-205, IDK-302, IDK-303, IDK-405.

## What changed since round 3 (`git log 93103cd..825a01f`)

Four commits: `4493208` (rubric manifest loader), `90e23c3` (source registration entry point), `2b5ebc2` (snapshot janitor), `825a01f` (staleness re-check). Determined independently which bear on gate 5, not assumed from commit subjects:

- **`4493208`** (`git show --stat 4493208`): touches `server/pyproject.toml` (+8), `server/scripts/load_rubric_manifest.py` (new, 247 lines), `server/src/yuno/modules/evidence_evaluation/service.py` (+172/-1), `server/tests/integration/test_load_rubric_manifest_script.py` (new, 404 lines). This is squarely gate-5 territory (evidence_evaluation, rubric loading) and is assessed in full below.
- **`90e23c3`**: `git show --stat` touches only `server/scripts/register_source.py`, `server/src/yuno/modules/provenance/service.py`, and a new provenance test file. No `evidence_evaluation/`, `hands_on/`, or IDK-009 content path. **Does not bear on gate 5** — it is gate-3/gate-4 territory (B4).
- **`2b5ebc2`**: `git show --stat` touches only `server/src/yuno/api/app.py`, `server/src/yuno/modules/provenance/adapters.py`, and a new provenance test file. **Does not bear on gate 5.**
- **`825a01f`** (this round's original HEAD at this gate's first pass; superseded by `636bf26`, see "HEAD delta check" below): `git show --stat` touches only `server/src/yuno/api/app.py`, `server/src/yuno/modules/provenance/service.py`, and a new provenance test file. **Does not bear on gate 5.**

Confirmed independently (not just from `--stat`): `git diff 93103cd..825a01f -- server/src/yuno/modules/evidence_evaluation/ server/src/yuno/modules/hands_on/` shows changes only inside `evidence_evaluation/service.py`, sourced entirely from `4493208`; `git diff 93103cd..825a01f -- IMPLEMENTATION_TICKETS.md docs/decisions/ docs/assessment/` returns no output — none of the four commits touches ticket text, decision documents, or the `is_critical` assessment record. Alembic head is unchanged: a fresh `alembic upgrade head` this round still lands at `be4d11f03666` (matching round 3 exactly — no migration shipped in this delta).

## HEAD delta check (`825a01f` → `0f3219f`)

This gate's own first pass (against `825a01f`) found a drifted citation in `docs/assessment/IDK-009-critical-dimension-exposure.md` (`service.py:384` should read `:389` — see the dedicated section below). Two follow-up commits landed after that pass, each verified directly rather than trusted from its message or the coordinator's summary.

### `825a01f` → `636bf26`: citation-only repair

- **Scope of the delta, confirmed by running it.** `git show --stat 636bf26` → four files, `5 insertions(+), 5 deletions(-)`: `docs/assessment/IDK-009-critical-dimension-exposure.md` (1 line), `docs/runner/IDK-406-execution-deferral.md` (1 line), `server/src/yuno/modules/evidence_evaluation/service.py` (2 lines), `server/tests/integration/test_runner.py` (1 line). `git diff 825a01f 636bf26` read in full: every one of the five changed lines swaps only a cited line number inside prose (a docstring comment or a code comment) — no code logic, no test assertion, no schema, and no decision-document content changed anywhere in the diff. This is citation-text-only, confirmed by reading the diff myself, not by accepting the commit message's own characterization.
- **The exact drift this gate's own inspection found is one of the five, confirmed corrected.** `docs/assessment/IDK-009-critical-dimension-exposure.md:13` now reads `service.py:389` (was `:384`). Re-derived independently at current `HEAD`: `grep -n "is_critical" server/src/yuno/modules/evidence_evaluation/service.py` → the carry-site assignment (`dimensions_by_stable_id[item.dimension_id].is_critical,`) is at line **389**, matching the corrected citation exactly. This line was untouched by `636bf26` itself (the commit only edited the assessment doc's prose and two unrelated docstring lines in the same file, not this executable line), so the number I re-derived during my first pass and the number now written in the corrected doc agree independently.
- **The two citations inside `load_rubric_manifest`'s own docstring, verified.** `636bf26` changes `evidence_evaluation/service.py:939` (was `provenance/service.py:190-191`, now `:270-271`) and `:950` (was `provenance/service.py:174-186`, now `:253-266`), both describing `withdraw_source`'s grant check and rationale. Read `provenance/service.py:222-294` (`withdraw_source`'s full body) directly at current `HEAD`:
  - `:270-271` is `grants = uow.owners.grants(owner_id)` / `RolePolicy.require(grants, Role.DESIGNATED_EDITORIAL_APPROVER)` — exactly the grant check the citation claims. **Correct.**
  - `:253-266` is the docstring paragraph beginning "IDK-003 §8 reserves `withdrawn` for entry 'only by explicit editorial action'..." through "...a distinct `actor_owner_id` would be a parameter nothing could ever supply a different value for." — exactly the rationale block the citation claims. **Correct.**
  - Both numbers are right. The commit message's own explanation — `825a01f` (the staleness re-check) inserted lines ahead of `withdraw_source` inside `provenance/service.py`, shifting it down from round 3's `:190-191`/`:174-186` — is consistent with what I read: `withdraw_source` now begins at `provenance/service.py:222`, well past where it sat in round 3's tree.
- **My own file had the same drift, now fixed.** Re-derivation surfaced that my own B21 section (below) cited `provenance/service.py:174-186` for `withdraw_source`'s rationale — accurate at round 3's `HEAD` but stale at `636bf26` for the identical reason `636bf26` itself corrects. Updated in place to `:253-266`, re-verified against the same `provenance/service.py:222-294` read above.
- **The other two corrected files are outside gate 5's territory** (`docs/runner/IDK-406-execution-deferral.md`, an `api/app.py` line for `LocalRunnerProcessPort` wiring; `server/tests/integration/test_runner.py`, a comment citing `api/app.py`'s dispatcher-thread start line) — both belong to gate 7 (runner posture), not assessment scenarios/rubrics. Not re-verified here as out of scope; noted only so this file does not silently omit that `636bf26` touched files beyond the two in gate 5's remit.
- **No schema, validation logic, or IDK-009 content moved.** `git diff 825a01f 636bf26 -- server/src/yuno/migrations/ server/src/yuno/modules/evidence_evaluation/models.py server/src/yuno/modules/evidence_evaluation/domain.py docs/decisions/` returns no output — confirmed empty. `_validate_rubric_dimension_structure` and `load_rubric_manifest`'s actual write/gate logic (`service.py:882-1013,1016-1044`) are untouched; the two `service.py` line changes are both inside `load_rubric_manifest`'s docstring, well away from its code. **B11, B10/B12, B18, and B19 are therefore unaffected by this delta** — none of their evidence cites a line this commit moved, and the underlying rows/tables/tests those findings depend on are unchanged. B21's own text is unaffected in substance; only my file's citation of `withdraw_source`'s rationale range needed the same correction `636bf26` made to `load_rubric_manifest`'s docstring, for the identical underlying reason.
- **Conclusion:** `636bf26` is exactly what it claims to be — a citation-only repair, verified by reading the diff and independently re-deriving three of its five corrected numbers (the two in gate 5's own territory, plus my own file's parallel drift). No finding, verdict, or disposition in this report changed as a result of this commit — only the citations that were stale now read correctly.

### `636bf26` → `0f3219f`: a declaration-parity test repaired, not a functional change

`0f3219f` ("fix: restore the import-boundary contract-declaration test to green") touches the `independence`-contract exemption question my B11-mechanism verification depends on (`4493208`'s new `ignore_imports` entry), so it was checked directly rather than accepted from the coordinator's framing:

- **Scope, confirmed by running it.** `git show 0f3219f` → one file, `server/tests/architecture/test_import_boundaries.py`, `7 insertions(+), 0 deletions(-)`. The diff adds a five-line rationale comment plus two string literals — `"yuno.modules.provenance.service -> yuno.modules.identity.**"` and `"yuno.modules.evidence_evaluation.service -> yuno.modules.identity.**"` — to the expected-set list `test_pyproject_declares_the_required_contracts` asserts against. `git diff 636bf26 0f3219f -- server/src/yuno/modules/evidence_evaluation/ server/scripts/load_rubric_manifest.py server/pyproject.toml` returns no output, confirmed empty: no exemption was added to or altered in `pyproject.toml` itself, no loader code changed. This is a test-file-only fix, exactly as claimed.
- **What it fixes, independently understood, not just read from the commit message.** `test_pyproject_declares_the_required_contracts` asserts set equality between `pyproject.toml`'s declared `ignore_imports` list (the `independence` contract) and a separate literal list hardcoded in the test — a declaration must appear in *both* places or the test fails, even though `lint-imports` itself only checks the `pyproject.toml` side and would report the contract satisfied regardless. Two entries were present in `pyproject.toml` but absent from the test's literal: `provenance.service -> identity.**` (added to `pyproject.toml` by round 3's `796fa30` without the matching test-literal update) and `evidence_evaluation.service -> identity.**` (added by this round's `4493208`, same omission repeated). Both are the same exemptions my B11-mechanism and B21 sections already verified exist and are load-bearing (the grant checks inside `withdraw_source` and `load_rubric_manifest`) — `0f3219f` does not add, remove, or alter what is exempted; it only declares the second one where the test can see it.
- **Re-run for real, not accepted from the commit's own reported numbers.**
  - `uv run --directory server pytest tests/architecture/ -q` → **822 passed**, matching the commit message's own count exactly. Ran the specific test by name too: `pytest tests/architecture/ -q -k test_pyproject_declares_the_required_contracts` → **1 passed, 821 deselected**.
  - `./server/.venv/bin/lint-imports --config server/pyproject.toml` → **4 contracts kept, 0 broken**, unchanged from every earlier check this round — confirming the commit's own claim that the underlying contract was satisfied throughout and only the declaration-parity test was ever red.
- **No effect on B11's content half, B10/B12, B18, or B19** — none of their evidence touches `test_import_boundaries.py` or the `ignore_imports` list's membership, only this one test's ability to see what was already there.

**Conclusion:** `0f3219f` is a real, verified fix to a genuinely red test (`test_pyproject_declares_the_required_contracts`), not a change to the contract, the loader, or any finding's substance. `HEAD` is updated to `0f3219f` throughout this file.

## Inspected artifacts

| Artifact | What it is | How inspected |
| --- | --- | --- |
| Scratch DB (`gate5_scratch_clean.db`) | Fresh SQLite DB, this inspection only, never written by hand | `cd server && YUNO_DATABASE_URL="sqlite+pysqlite:////private/tmp/.../scratchpad/gate5c/gate5_scratch_clean.db" uv run alembic upgrade head` → reached `be4d11f03666`, confirmed via `alembic heads`. Queried read-write with plain `sqlite3` (my own scratch file). |
| Scratch DB (`gate5_scratch.db`) | A second scratch DB, used only to manually probe the `rubrics` UniqueConstraint at the SQL level | Same migration path; two rows inserted by hand with identical `(owner_id, capability, version)` but different `body_hash` to confirm the DB alone does not reject the redefinition (see B11 mechanism section). This file now carries 2 leftover rows because `rubrics` has a `BEFORE DELETE` trigger (`trg_rubrics_no_delete`, "rubrics header is immutable") that refused my own cleanup attempt — noted so the artifact isn't mistaken for a clean count; `gate5_scratch_clean.db` is the one cited for row counts. |
| `server/yuno.db`, `server/.e2e.db` | Read-only review artifacts | Not written to. `sqlite3 -readonly yuno.db "SELECT version_num FROM alembic_version"` → `4747447ccaa3` (behind head, unchanged since round 3); `rubrics`/`rubric_dimensions`/`hands_on_work` all `0` there too, read-only, confirming the scratch-DB result independently. `.e2e.db` at `a9d4e6f1b208`, not queried further (out of scope for this gate). |
| `server/src/yuno/modules/evidence_evaluation/service.py:882-1044` | New `load_rubric_manifest`, `_validate_rubric_dimension_structure` | Read in full |
| `server/src/yuno/modules/evidence_evaluation/domain.py:586-631` | `Rubric`, `CRITICAL_STABLE_DIMENSION_IDS`, `RubricDimension` (incl. `name`/`description`/`evaluation_guidance` fields the loader never inspects) | Read |
| `server/src/yuno/modules/evidence_evaluation/models.py:160-236` | `RubricRow` (`UniqueConstraint("owner_id","body_hash","capability","version")`), `RubricBodyRow`, `RubricDimensionRow` | Read; schema re-confirmed against the scratch DB's `.schema rubrics` |
| `server/scripts/load_rubric_manifest.py` | The offline CLI entry point | Read in full |
| `server/tests/integration/test_load_rubric_manifest_script.py` | Full file, 11 tests | `uv run pytest tests/integration/test_load_rubric_manifest_script.py -q` → **11 passed** |
| `server/pyproject.toml:194-233` | Import-linter `modules`/`ignore_imports` for the module-independence contract | Read; new entry at line 221, `"yuno.modules.evidence_evaluation.service -> yuno.modules.identity.**"`, confirmed |
| `./server/.venv/bin/lint-imports --config server/pyproject.toml` | Real contract check, not `pnpm test`/`ruff` | Run directly → **4 contracts kept, 0 broken** (same 4 as round 3; the new `ignore_imports` entry does not break the contract, it is the mechanism that keeps it kept) |
| `server/src/yuno/modules/hands_on/service.py:77-135` (`prepare_submission`, `add_work` call at :120) | The hands-on write primitive and its caller | Read; unchanged since round 3 (`git log 7b805bd..825a01f -- server/src/yuno/modules/hands_on/` empty) |
| `server/src/yuno/api/routes/hands_on.py:27,159` | Import and call site of `prepare_submission` | Read; confirms a real HTTP route reaches `add_work` |
| `server/src/yuno/migrations/versions/*.py` (all four files containing the string `scenario`) | Checked for a scenario-registry table | `grep -rln "scenario" server/src/yuno/migrations/versions/*.py` → 4 files; every hit is a column on `hands_on_work`/`hands_on_work_bodies` or the `topic_relations.relation_type` CHECK's `'scenario'` literal, never a `CREATE TABLE`/`__tablename__` |
| `grep -rn "__tablename__" server/src/yuno/modules` | Scenario-table existence check | Zero hits containing "scen" |
| `docs/decisions/IDK-009-assessment-and-derived-state.md:4,50-58,212-224` | §4 scenario registry contract, §6 rubric/dimension table | Read |
| `IMPLEMENTATION_TICKETS.md:1015-1067,1683-1738` | IDK-204, IDK-405 ticket bodies in full | Read |
| `docs/decisions/IDK-002-editorial-approval-criteria.md:25`, `docs/decisions/IDK-003-source-licensing-and-snapshot-policy.md:191-206` | D1's `designated_editorial_approver` grant scope; IDK-003 §13/§14 | Read, for the B21-consolidation judgment |
| `server/src/yuno/modules/identity/domain.py:1-30` | `Role` enum, docstring attributing it to "spec §4.2, D1" | Read |
| `docs/assessment/IDK-009-critical-dimension-exposure.md` | The `is_critical` non-exposure record round 3 assessed | Re-read in full; every citation re-verified against the current tree (see dedicated section below) |
| Per-ID `grep` for `hands-on-rubric-v1`/`practice-rubric-v1`/`mock-rubric-v1` | Confirms content-authoring status | Run individually, repo-wide |
| `server/tests/unit/test_derived_progress.py`, `server/tests/integration/test_evidence_evaluation.py`, `server/tests/integration/test_hands_on_api.py`, `server/tests/unit/test_learning_content.py -k reverse` | Re-run fresh this round, not copied from round 3 | **7 passed**, **10 passed**, **7 passed**, **1 passed, 1 deselected** respectively — all match round 3's counts exactly, confirming these files are unaffected by this round's delta |

## B11 mechanism half: closed

`load_rubric_manifest` (`service.py:882-1013`) and `_validate_rubric_dimension_structure` (`service.py:1016-1044`) are real, and `server/scripts/load_rubric_manifest.py` is a genuine production entry point — the same offline-CLI shape as `withdraw_source.py`/`publish_canonical.py` (argparse, no FastAPI/ASGI import, confirmed by reading the file: `from yuno.config import get_settings`, `create_engine_for`, `require_single_head`, no `fastapi`/`starlette` import anywhere).

**Version gate, verified two ways:**

1. **On my own scratch DB, at the SQL level.** `rubrics`' schema (`sqlite3 gate5_scratch.db ".schema rubrics"`) confirms `UniqueConstraint("owner_id","body_hash","capability","version")` exactly as the code docstring claims. Inserting two rows by hand with identical `(owner_id="t-owner", capability="implement", version="v1")` but `body_hash` `'hashA'` vs `'hashB'` **succeeded for both** — the DB constraint alone does not reject a same-`(capability,version)` redefinition when the body differs, confirming the gap `load_rubric_manifest` claims to close in application code is real, not a strawman.
2. **Via the named test, run for real.** `test_reloading_same_capability_version_with_different_body_exits_1_and_leaves_original_untouched` (`test_load_rubric_manifest_script.py:239-273`) loads a manifest, then loads a second manifest with the same `(capability, version)` but a different `task_context` (hence a different `body_hash`, confirmed via `repository.py:303,323,408,646`'s `hash_payload(body)` calls). Ran as part of the file's full suite: **11 passed**, including this one. Exit code 1, rubric count stays 1, and the original `task_context` is unchanged — the gate is genuinely enforced end to end through the real CLI `main()`, not merely asserted in the domain layer.

The `RolePolicy.require(grants, Role.DESIGNATED_EDITORIAL_APPROVER)` check (`service.py:990`) sits inside `load_rubric_manifest` itself, inside the `with uow_factory() as uow:` block, before any read of existing rubrics — so no future caller (HTTP route, another script) could bypass it by calling a lower-level path. `test_load_refused_when_actor_lacks_the_grant_exits_1_and_writes_nothing` covers this and passed.

**Verdict: the mechanism claim holds.** This closes only the mechanism half of B11, exactly as its own docstring says.

**A quality note, added after `0f3219f` landed.** `4493208` shipped its new `evidence_evaluation.service -> identity.**` exemption in `pyproject.toml` without adding the matching literal to `test_import_boundaries.py`'s `test_pyproject_declares_the_required_contracts` — leaving that test red from the moment `4493208` landed until `0f3219f` fixed it (see "HEAD delta check" above), and repeating, inside this same round, the identical omission round 3's own `796fa30` had already made for `provenance.service`'s exemption. `lint-imports` alone — the command this gate was specifically directed to run — reported the contract satisfied throughout and could not have caught this; only a full `tests/architecture/` run surfaces it, and neither my own inspection nor `4493208`'s own commit ran that broader suite before landing. This does not change the functional verdict above: the version gate and grant check are real and independently verified by me through direct testing, not through this now-fixed test. But it is a genuine process gap in `4493208` — a second occurrence of a pattern already flagged once this project — and a reminder that `lint-imports`'s "kept" result is necessary but not sufficient evidence a contract change was fully landed; a reader relying on my B11-mechanism verdict should know I checked the substance carefully but the completeness of the commit's own test coverage needed a later commit to close.

## B11 content half: still open

Fresh scratch DB (`gate5_scratch_clean.db`, migrated to `be4d11f03666`, never hand-written): `rubrics` = 0, `rubric_dimensions` = 0. `server/yuno.db` (read-only, `-readonly` flag) independently shows the same: `rubrics`/`rubric_dimensions`/`hands_on_work` all 0.

Per-ID repo-wide grep for the three manifest names, re-derived fresh (not copied from round 3, whose "zero hits outside `docs/`" claim needs updating — see below):

```
hands-on-rubric-v1, practice-rubric-v1, mock-rubric-v1 →
  docs/decisions/IDK-009-assessment-and-derived-state.md
  docs/approvals/IDK-503*                                (review prose, all rounds)
  IMPLEMENTATION_TICKETS.md                               (prose)
  server/scripts/load_rubric_manifest.py:28-29            (docstring prose, new this round)
  server/src/yuno/modules/evidence_evaluation/service.py:899 (docstring prose, new this round)
```

**Note on drift from round 3's citation:** round 3 reported these three IDs confined to `docs/`-and-review-prose with "zero hits in `server/`". That is no longer exactly true — `4493208` added two docstrings that name the three manifests as examples of content this ticket does *not* author. Both hits are prose inside a function/module docstring, not data (`load_rubric_manifest.py:28`, "the three approved IDK-009 v1 manifests (`hands-on-rubric-v1`, `practice-rubric-v1`, `mock-rubric-v1`) still need a content owner to author them"), and neither changes the finding's substance — restated precisely rather than silently carried forward, per this round's own discipline. **To be unambiguous for whoever inspects this next: neither `load_rubric_manifest.py:28-29` nor `service.py:899` is a loaded, seeded, or persisted rubric — both are comment text inside a docstring describing what the loader does *not* yet do, in a file that ships no manifest content. A future round's grep for these three IDs will find these two comment lines; they are not evidence B11's content half has moved, and should not be read as such. The only authoritative measure of content-half status remains a fresh `rubrics`/`rubric_dimensions` row count on a freshly migrated database, which this round found at 0/0.**

**Verdict: B11's content half is unchanged and stays open.** The three manifests are still IDK-009's own approved content and have not been authored anywhere in the tree outside prose describing their absence.

### What would clear it

A content owner authors the three manifest JSON files (`hands-on-rubric-v1`, `practice-rubric-v1`, `mock-rubric-v1`) matching IDK-009 §6's table verbatim — six dimensions each, correct `name`/`description`/`evaluation_guidance` text, the first two marked critical by `stable_dimension_id` — and someone holding `designated_editorial_approver` runs `scripts/load_rubric_manifest.py` against each with `status: "approved"`. This gate then re-inspects the live rows in a freshly migrated database and confirms the loaded `name`/`description`/`evaluation_guidance` text against IDK-009 §6's table field-by-field (the loader never checks this, by design — see next section).

## Structural validation: faithful to IDK-009 §4/§6, does not encode approved copy

`_validate_rubric_dimension_structure` (`service.py:1016-1044`) checks exactly four things: `len(dimensions) == 6`; `ordinals == [1,2,3,4,5,6]` (sorted, no duplicates); `stable_dimension_id`s distinct; and `CRITICAL_STABLE_DIMENSION_IDS.issubset(stable_ids)`. Read the full function body directly — it never references `.name`, `.description`, or `.evaluation_guidance` on any `RubricDimension`, even though those fields exist on the dataclass (`domain.py:613-620`) and are exactly the fields IDK-009 §6's table would need to be checked against to verify a manifest matches the decision's approved copy.

Cross-checked against the decision text itself:

- "All six dimensions of the referenced rubric are required for every v1 scenario" — `docs/decisions/IDK-009-assessment-and-derived-state.md:58`, verbatim. Matches the `len == 6` check.
- IDK-009 §6's table (`:216-223`) lists exactly six stable dimensions with exactly two marked `Critical: Yes` — `factual-and-mechanical-correctness` and `assumptions-and-constraints` — matching `CRITICAL_STABLE_DIMENSION_IDS` (`domain.py:607-609`) exactly.
- Criticality is derived from `stable_dimension_id` alone (`RubricDimension.is_critical`, `domain.py:622-631`), independent of `ordinal` — so the structural check correctly does not also require the two critical dimensions to sit at ordinal 1/2 specifically; IDK-009's table row order is presentational, not a positional contract, and nothing downstream (`derive_progress`'s precedence rule) depends on ordinal position for correctness.

**Verdict: the validation is faithful and appropriately scoped — neither over-reaching into IDK-009's approved copy (it never inspects `name`/`description`/`evaluation_guidance`) nor under-reaching on the structural invariants IDK-009 §4/§6 actually state** (six dimensions, both critical stable IDs present, distinct ordinals 1..6). No gap found here.

## B10/B12 (scenarios): no registry table, `add_work` has a real caller — both re-verified

Two claims, checked independently rather than accepted from the brief:

1. **No scenario registry table exists anywhere.** `grep -rn "__tablename__" server/src/yuno/modules` has zero hits containing "scen". Every migration file mentioning the string "scenario" (`87af9746aec1_canonical_graph.py`, `be4d11f03666_hands_on_scenario_status_and_id.py`, `e10d1a0c0100_...py`, `e7a405b1c2d3_hands_on_lifecycle.py` — found via `grep -rln "scenario" server/src/yuno/migrations/versions/*.py`) was read in full for its scenario-related lines: every one is a column (`scenario_title`, `scenario_prompt`, `scenario_status`, `scenario_source`, `scenario_id`) on `hands_on_work`/`hands_on_work_bodies`, or the `'scenario'` literal inside `topic_relations.relation_type`'s CHECK constraint. None creates a table. Confirmed independently. The scratch DB's full `.tables` listing (86 tables) contains nothing scenario-named.
2. **`add_work` already has a production caller.** `hands_on/repository.py:44` defines `add_work`; `hands_on/service.py:120` calls it, inside `prepare_submission` (defined at `service.py:77`); `api/routes/hands_on.py:27,159` imports and calls `prepare_submission` from a real request handler. This chain is untouched by any of this round's four commits (`git log 7b805bd..825a01f -- server/src/yuno/modules/hands_on/` is empty). Confirmed independently.

Both premises hold. On the reasoning built on them — that a scenario loader was deliberately not built because IDK-009 §4 (`:54`, verbatim: "the mapping may not be inferred from prose") and IDK-405's own Scope (`IMPLEMENTATION_TICKETS.md:1695`, verbatim: "consume IDK-204's approved rubric registry and exact canonical-topic mappings") both make scenario activation depend on a `topic_binding_key → canonical topic stable ID` mapping that IDK-009 §4 (`:56`) itself says IDK-204 persists "consuming the approved graph published by IDK-102 after IDK-001" — this is not an inference, it is the decision and ticket text stated plainly. B1 (`docs/approvals/IDK-503-content-and-safety-review-rerun-2026-08-15-b.md:70`: "No production `canonical_graph_versions` row exists... IDK-102 (production seed run)... still open") is confirmed still open — the scratch DB's `canonical_graph_versions` table was not separately re-queried this round (out of gate-5 scope; B1 belongs to gate 1, whose round-4 file exists at `docs/approvals/IDK-503-rerun-2026-08-15-c/gate-1-curriculum-boundary.md`) but nothing in this round's diff touches `canonical/` and round 3's B1 citation was not disputed by any commit in this delta.

**I agree with this reasoning.** Building a scenario loader today would either hardcode a `topic_binding_key → topic stable ID` mapping IDK-009 §4 explicitly forbids inferring from prose, or block on B1 anyway. Declining to build it is the correct call given the current tree, not a gap engineering could have closed.

### What would clear it

Two things, in order: (1) IDK-102 performs the production seed run publishing the IDK-001-approved canonical graph (clears B1); (2) IDK-204 persists the approved `topic_binding_key → canonical topic stable ID` mapping against that real graph, and IDK-405/IDK-302/IDK-303 author and load the twelve approved scenario records (six hands-on, three practice, three mock, per IDK-009 §4's table) with `scenario_status='approved'` and correct `scenario_id`s. This gate then confirms the persisted rows against IDK-009 §4/§5's normative text field-by-field, on a real database, not a scratch one.

## B18 and B19: re-derived fresh, both unchanged, still need the approver's decision

Neither is a code defect; both require the designated editorial approver, not engineering work. Re-derived independently this round rather than copied:

- **B18** — `docs/decisions/IDK-009-assessment-and-derived-state.md` never names a `scenario_status` field or a non-fixture literal for one: `grep -n "scenario_status" docs/decisions/IDK-009-assessment-and-derived-state.md` → zero hits (file unchanged since round 3, confirmed via empty `git diff 93103cd..825a01f` on `docs/decisions/`). The migration (`be4d11f03666_hands_on_scenario_status_and_id.py:8-13`) and a test (`test_hands_on_api.py:308-312`) both self-disclose this, unchanged.
- **B19** — IDK-009 §2 states verbatim: "The only persisted classification values are `likely_known`, `partial`, `unverified`, and `new`." Every shipped occurrence still spells it `likely-known` (hyphen). Re-grepped every site fresh this round: `domain.py:12,48` (two enums), `models.py:605` (memo CHECK), `roadmap/domain.py:59`, `roadmap/models.py:203,283`, `diagnostics/service.py:303`, three migrations (`6834679f0af4:40,127,339`; `e205f6a2c4d1:34`; `e10d1a0c0100:575,579,583,587`), three generated TS union types (`schema.d.ts:3461,3847,4495`), two learner-facing `<select>` controls (`CorePages.tsx:411,462`), and two test assertions (`test_evidence_transfer_delete.py:258`, `test_derived_progress.py:183`). Every citation matches round 3's exactly — none of the touched files (`domain.py`, `models.py`, `roadmap/`, `diagnostics/`, migrations, `schema.d.ts`, `CorePages.tsx`, both test files) appears in this round's four-commit diff, so no drift was expected and none was found. `likely_known` (underscore) still appears only in `PRD.md:262,360` and `IMPLEMENTATION_TICKETS.md:844,1083` prose, never in code.

## B21 consolidation: agree on the root cause, but the two call sites are not identical in kind

`load_rubric_manifest` gates on `Role.DESIGNATED_EDITORIAL_APPROVER` (`service.py:990`) with the same rationale `withdraw_source` (`provenance/service.py:253-266`, round 3's B21 — cited at round 3's `HEAD` as `:174-186`; re-derived at this round's `HEAD` per the "HEAD delta check" above) and `register_source` (`provenance/service.py:363-374`, this round's `90e23c3`, same rationale again) already use: `owner_role_grants.role` admits only `learner`/`designated_editorial_approver` (`identity/domain.py:28-30`), IDK-003 §13 (`:200`, verbatim: "`owner_role_grants.role` (`learner`, `designated_editorial_approver`) has no distinct content-owner value... unlike D1's explicit grant for canonical publication") records that no distinct content-owner grant exists, and the code reuses the only editorial grant available rather than either inventing an unapproved role or shipping an ungated write path. On that axis — **same root cause, same borrowed grant, same missing role** — I agree the reuse is the identical fact pattern B21 already names, and does not need a new finding number.

**But I looked past the code's own framing, as asked, and found a real difference worth naming precisely.** `withdraw_source` and `register_source` gate actions IDK-003 itself governs and explicitly requires an editorial actor for: §8 (`:111`, verbatim) says `withdrawn` is "entered only by explicit editorial action"; §12 item 7 (cited at `provenance/service.py:363`) attributes source *registration* specifically to "a content-owner role." Both call sites are gating an action inside the decision document that demands gating. `load_rubric_manifest` is different: it gates an action IDK-009 governs, and IDK-009 contains **zero** occurrences of "editorial," "approver," or "content owner" anywhere in its text (`grep -in "editorial\|approver\|content.owner" docs/decisions/IDK-009-assessment-and-derived-state.md` → only the document's own top-of-file "Owner and approver: content/assessment owner" approval-record line and the Appendix approver table — neither is a requirement that *loading* be gated by anyone). IDK-009 never says rubric loading requires an editorial actor at all; that requirement is engineering's own choice (a reasonable one, to avoid an ungated write path), not something the decision text demands the way IDK-003 §8 demands one for withdrawal.

A second, sharper instance of the same pattern: `load_rubric_manifest`'s docstring (`service.py:935-936`) says inventing a content-owner role here "would be an unapproved vocabulary change under IDK-003 §14's change control" — but IDK-003 §14 (`:204-206`, read in full) is scoped entirely to `source-policy-v1`'s own content: "Adding or removing a registry row, changing a tier, the excerpt cap, the automatic-`unavailable` threshold, or the `withdrawal_reason` vocabulary requires a new decision version." It says nothing about rubrics, and `register_source`'s use of the identical §14 citation (`provenance/service.py:368`) is on much firmer ground there, because registering a source *is* "adding a registry row" — the exact thing §14 names. Borrowing §14 to constrain rubric-manifest loading reaches outside the decision it is written to govern.

**Judgment: the reuse is the same class of gap (no content-owner role) surfacing a third time, and B21 as filed is broad enough to cover it — I would not open a separate numbered finding.** But the approver resolving B21 should be told explicitly that it now has three call sites (`withdraw_source`, `register_source`, `load_rubric_manifest`) crossing two different decisions' authority (IDK-002's D1 grant, reasoned into IDK-003's and now IDK-009's territory), not one. A resolution that only names "which role may withdraw a source" would leave `load_rubric_manifest`'s and `register_source`'s identical gate unresolved by omission.

## The `is_critical` exposure record: accurate; one drifted citation found by this gate's first pass, now corrected

`docs/assessment/IDK-009-critical-dimension-exposure.md` was re-checked claim-by-claim against the current tree, since `evidence_evaluation/service.py` grew by 172 lines this round. This section reports the first-pass finding and its subsequent correction together, since the correcting commit (`636bf26`) landed within this same round — see "HEAD delta check" above for how that commit was verified.

- `grep -c is_critical server/openapi.json src/shared/api/schema.d.ts` → `0`, `0`, reproduced. Repo-wide `grep -rn is_critical src/` and `server/src/yuno/api/` also both zero. Unaffected — `4493208` never touches `api/` or `src/`.
- `AssessmentDimensionResponse` (`contracts.py:1016-1021`) — reproduced exactly, unchanged (`contracts.py` not touched by this round's diff).
- `models.py:406` for the persisted `is_critical` column — reproduced exactly (`models.py` not touched by `4493208`).
- `domain.py:622-631` (`RubricDimension.is_critical`), `domain.py:705-722` (`AssessmentDimensionResult.is_critical`), `domain.py:378` (`derive_progress`'s precedence check), `domain.py:601-609` (`CRITICAL_STABLE_DIMENSION_IDS`) — all reproduced exactly (`domain.py` not touched by this round's diff).
- `repository.py:318`/`:795` — reproduced exactly (`repository.py` not touched).

**One citation had drifted, found at this gate's first pass (against `825a01f`).** The record's line 13 cited `service.py:384` for the site where `service.py` "copies [`is_critical`] from the rubric dimension onto the result at assessment time." Re-derived at that pass: `grep -n "is_critical" server/src/yuno/modules/evidence_evaluation/service.py` showed this assignment at **line 389**, not 384. Cause, traced directly: `4493208`'s diff adds imports near the top of the file — `from collections.abc import Sequence` (new), `from dataclasses import asdict, replace` (was `asdict` alone, +1 line net), `CRITICAL_STABLE_DIMENSION_IDS,` in the `evidence_evaluation.domain` import block (+1), `Rubric,`/`RubricDimension,` in the same block (+2), and `from yuno.modules.identity.domain import Role, RolePolicy` (+1) — five lines added before the original line 384's position, landing the same statement at 389. Read directly: line 384 is now `assessment_id,` (a constructor argument), and the actual `is_critical` assignment (`dimensions_by_stable_id[item.dimension_id].is_critical,`) sits at line 389. The substance of the record's claim was untouched throughout — it is still a "carry the value" site, not a "decide" site, and `domain.py:378` is still correctly the sole deciding site — only the line number was stale.

**Corrected within this same round, and still correct at final `HEAD`.** `636bf26` rewrites `docs/assessment/IDK-009-critical-dimension-exposure.md:13` to read `service.py:389`, and `0f3219f` (the round's final commit) does not touch this file, so the correction stands unchanged. Re-verified directly: `git diff 825a01f 636bf26 -- docs/assessment/IDK-009-critical-dimension-exposure.md` shows exactly this one-line change, and the current file now reads `:389`, matching the line I independently re-derived. This is exactly the class of drift this round's brief warned would happen downstream of `service.py`'s growth, and it surfaced and closed within the same round — recorded here per this round's citation-accuracy discipline (mirroring what round 3's own inspection did for round 2's citations, and what `636bf26` itself did for `7b805bd`'s predecessor). I do not own `docs/assessment/IDK-009-critical-dimension-exposure.md` and did not edit it.

## Test and contract evidence, run fresh this round

| Command | Result |
| --- | --- |
| `alembic upgrade head` (scratch DB) | Reached `be4d11f03666`, confirmed via `alembic heads` — unchanged from round 3, no migration in this delta |
| `uv run pytest tests/integration/test_load_rubric_manifest_script.py -q` | **11 passed** (new file; includes the version-gate and grant-refusal tests) |
| `uv run pytest tests/unit/test_derived_progress.py -q` | **7 passed** |
| `uv run pytest tests/integration/test_evidence_evaluation.py -q` | **10 passed** |
| `uv run pytest tests/integration/test_hands_on_api.py -q` | **7 passed** |
| `uv run pytest tests/unit/test_learning_content.py -k reverse -q` | **1 passed, 1 deselected** |
| `./server/.venv/bin/lint-imports --config server/pyproject.toml` | **4 contracts kept, 0 broken** (222 files, 1244 dependencies analyzed) — the new `ignore_imports` entry for `evidence_evaluation.service -> identity.**` is exercised and the module-independence contract still holds |
| `uv run --directory server pytest tests/architecture/ -q` (re-run at `0f3219f`, see "HEAD delta check") | **822 passed** — includes `test_pyproject_declares_the_required_contracts`, run individually too (`-k test_pyproject_declares_the_required_contracts` → **1 passed, 821 deselected**), confirming the declaration-parity fix actually landed |

All five pytest counts in the first block match round 3's exactly, confirming none of this round's four commits touched the files those tests exercise (independently confirmed via `git diff`, not inferred from matching numbers alone).

## Findings

| # | Decision requirement | Shipped reality (re-derived, with citation) | Verdict |
| --- | --- | --- | --- |
| 1 | Three approved rubric manifests, loaded and version-gated (IDK-009 §1, §6; IDK-204) | Mechanism real and tested (11/11 passing, including the version-gate test); content still absent — `rubrics`/`rubric_dimensions` = 0/0 on a fresh scratch DB and on read-only `yuno.db`; the three IDs exist only in decision/review prose plus two new code-comment mentions (`load_rubric_manifest.py:28-29`, `service.py:899`), neither of which is loaded data | mechanism shipped, content not shipped (B11 — mechanism half closed, content half open) |
| 2 | Twelve approved `scenario_id` records, content revision `idk009-v1-r1` (IDK-009 §4-§5; IDK-405/302/303) | No scenario registry table exists (confirmed: zero `__tablename__` hits, all migration "scenario" hits are columns/CHECK literals); `add_work` has a real production caller (`hands_on/service.py:120` → `api/routes/hands_on.py:159`), confirmed unchanged since round 3; deliberately not built pending B1 (IDK-102 production graph publish), per IDK-009 §4's own text, not inference | not shipped — unchanged (B12) |
| 3 | Structural validation matches IDK-009 §4/§6 without encoding approved copy | Confirmed: exactly six dimensions, ordinals 1..6, distinct stable ids, both critical ids present; never inspects `name`/`description`/`evaluation_guidance` | faithful, appropriately scoped — no gap found |
| 4 | `'approved'` `scenario_status` literal traces to decision text (B18) | `docs/decisions/IDK-009-assessment-and-derived-state.md` never names `scenario_status`; migration and test both self-disclose this; file unchanged since round 3 | confirmed unreviewed naming choice — unchanged (B18) |
| 5 | Persisted classification values exactly `likely_known`/`partial`/`unverified`/`new` (IDK-009 §2) | Still `likely-known` (hyphen) at every one of the fourteen sites round 3 named, re-verified fresh at identical line numbers (no drift — none of the underlying files touched this round) | mismatch — unchanged (B19) |
| 6 | `designated_editorial_approver` grant reuse authority (B21) | Same root cause (no content-owner role) now has three call sites (`withdraw_source`, `register_source`, `load_rubric_manifest`); consolidates on the "no role exists" axis but the third site's own IDK-003 §14 citation reaches outside that decision's scope | consolidates under B21, with the scope note above — not a new numbered finding |
| 7 | `is_critical` non-exposure record accurate at current HEAD | Five of five factual claims reproduce exactly at `636bf26`. One line citation (`service.py:384`) had drifted to `:389` (found at this gate's own first pass, against `825a01f`, due to new imports `4493208` added above it) and is now corrected in the tree by `636bf26`, re-verified | accurate — the drift this gate found this round is now closed |

## Blocking findings

### 1. No approved rubric manifests shipped (B11 — mechanism half now closed, content half still open)

- What is missing: `rubrics`/`rubric_dimensions` rows for `hands-on-rubric-v1`, `practice-rubric-v1`, `mock-rubric-v1` with `status='approved'` and IDK-009 §6's six stable dimensions, first two marked critical.
- Owning ticket: IDK-204, still `Content incomplete` (`IMPLEMENTATION_TICKETS.md:1018`, unchanged since round 3 — confirmed via empty `git diff` on this file).
- Evidence of absence: fresh scratch DB at head `be4d11f03666` — `rubrics` = 0, `rubric_dimensions` = 0 (`gate5_scratch_clean.db`); read-only `server/yuno.db` shows the same; repo-wide per-ID grep for the three rubric IDs finds them only in prose (decision text, approval-review files, and two new docstrings this round, neither of which is loaded data).
- What would clear it: a content owner authors the three manifest JSON files matching IDK-009 §6 verbatim and someone holding `designated_editorial_approver` runs `scripts/load_rubric_manifest.py` against each with `status: "approved"`; this gate then re-inspects the live rows on a freshly migrated database and checks the loaded `name`/`description`/`evaluation_guidance` text against IDK-009 §6's table field-by-field (the loader itself never checks this, by design).

### 2. Twelve approved scenario records are still unauthored, and no registry loader exists to load them into (B12 — schema half closed, content half still open)

- What is missing: the twelve `scenario_id` records from IDK-009 §4-§5, content revision `idk009-v1-r1`, persisted as real rows with `scenario_status='approved'`.
- Owning tickets: IDK-405, IDK-302, IDK-303, still `Content incomplete` (`IMPLEMENTATION_TICKETS.md`, unchanged since round 3). Confirmed this round: no scenario-registry table exists in any migration, and building one now would either hardcode a topic-binding mapping IDK-009 §4 forbids inferring from prose or sit unusable pending B1 — so its continued absence is a deliberate, correctly-reasoned deferral, not an oversight.
- Evidence of absence: per-ID repo-wide grep for all twelve scenario IDs and `idk009-v1-r1` finds them only in decision/review prose; `grep -rn "__tablename__" server/src/yuno/modules` has no scenario-named hits; every synthesized `HandsOnWork` row still hardcodes the fixture triple (`hands_on/service.py:105-119`, unchanged).
- What would clear it: IDK-102 performs the production seed run publishing the IDK-001-approved canonical graph (clears B1); IDK-204 persists the approved `topic_binding_key → canonical topic stable ID` mapping against that real graph; IDK-405/IDK-302/IDK-303 author and load the twelve approved scenario records with `scenario_status='approved'` and correct `scenario_id`s. This gate then confirms the persisted rows against IDK-009 §4/§5's normative text field-by-field on a real (non-scratch) database.

### 3. `'approved'` scenario_status literal, the `likely-known`/`likely_known` mismatch, and the `designated_editorial_approver` reuse question all need the approver's decision, not code (B18, B19, B21 — carried forward, not elevated to new numbered defects)

- **B18** and **B19**: unchanged from round 3, re-derived fresh this round at every citation (see Findings 4-5 above); neither is a code defect an owning ticket can close.
- **B21**: now has a third call site (`load_rubric_manifest`, this round) alongside `withdraw_source` (round 3) and `register_source` (this round, `90e23c3`). All three reuse `designated_editorial_approver` — D1/IDK-002's canonical-publication grant (`identity/domain.py:28-30`, docstring: "spec §4.2, D1") — to gate actions in decisions (IDK-003, IDK-009) that do not themselves define that authority. See the dedicated section above for why the three are not perfectly uniform (two gate actions their own governing decision explicitly requires an editorial actor for; the third does not) even though they share the same root cause and the same missing role.
- None of the three is something engineering can resolve unilaterally; all three require the designated editorial approver's decision.

## Notes and residual risk

- **The `is_critical` exposure record's drift was found and closed inside this same round.** This gate's first pass (against `825a01f`) found `docs/assessment/IDK-009-critical-dimension-exposure.md:13` citing `service.py:384` when the actual site had moved to `:389`, a direct, traced consequence of `4493208` adding five lines of new imports above the original site. `636bf26` corrected it before this file was finalized; re-verified directly against `git diff 825a01f 636bf26` and the current tree. The claim's substance was never in question, only the pointer. `docs/assessment/IDK-009-critical-dimension-exposure.md` is outside this gate's ownership (I own only this one gate-5 file) and was not edited by me — `636bf26` fixed it independently.
- **My own file carried the identical class of drift, also now fixed.** The B21 section's citation of `withdraw_source`'s rationale (`provenance/service.py:174-186`, accurate at round 3's `HEAD` but stale at this round's, for the same reason `636bf26` corrects `load_rubric_manifest`'s docstring) was caught during the `636bf26` delta check and corrected in place to `:253-266`. See "HEAD delta check" above.
- **B11's mechanism half is a genuine, tested close** — not merely claimed. The version gate was verified both by hand (a manual SQL insert against a scratch DB proving the bare `UniqueConstraint` permits the redefinition the code claims to block) and by running the named test for real (11/11 passing). This is the kind of claim this round's brief specifically asked not to take on faith, and it held up.
- **`4493208` shipped with its own architecture test suite red, caught only by `0f3219f`.** The new `evidence_evaluation.service -> identity.**` exemption went into `pyproject.toml` without the matching literal in `test_import_boundaries.py`, repeating — within this same round — the identical omission round 3's `796fa30` made for `provenance.service`'s exemption. `lint-imports` (4 kept, 0 broken throughout) could not and did not catch this; only `uv run --directory server pytest tests/architecture/ -q` (822 passed, after `0f3219f`) does. See the B11-mechanism section's quality note above for the full assessment — it does not change the functional verdict, but it is a real completeness gap in how `4493208` landed.
- **B21 is broader than its filing text currently states.** It was raised for one call site (`withdraw_source`) and has now been reused, with essentially identical language, for two more (`register_source`, `load_rubric_manifest`) across two different decisions' territory (IDK-003, IDK-009). The approver resolving it should be shown all three, not just the one it was originally raised against, so a partial resolution does not silently leave the other two gated on an authority nobody has confirmed.
- **The scenario-loader non-decision is sound, and traceable to primary text, not inference.** IDK-009 §4's "the mapping may not be inferred from prose" and IDK-405's Scope text ("consume IDK-204's approved rubric registry and exact canonical-topic mappings") were both read in full and quote exactly as this round's brief characterized them. Building a scenario loader today would necessarily either violate the first or sit unusable pending B1; not building one is correct engineering judgment given the current tree, not a gap.
- **Five of the seven test/contract commands re-run this round produced identical counts to round 3** (`test_derived_progress.py`, `test_evidence_evaluation.py`, `test_hands_on_api.py`, the DEP-03 reversal test, and `lint-imports`'s "4 contracts kept"). This is expected, not incidental: none of this round's four commits touches the files those suites exercise, confirmed by `git diff`, not assumed from the matching numbers.
- **Migration to head.** `cd server && YUNO_DATABASE_URL="sqlite+pysqlite:////private/tmp/.../scratchpad/gate5c/gate5_scratch_clean.db" uv run alembic upgrade head` reached `be4d11f03666`, confirmed current via `alembic heads`. No write was made to `server/yuno.db` or `server/.e2e.db` — both were only read, with `-readonly` where used.
- Nothing in this round's inspection could not be verified; every claim above traces to a file:line, a command actually run this round, or a named test actually executed this round.
