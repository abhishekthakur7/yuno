# IDK-503 rerun (2026-08-15, round 4) — gate 1: Curriculum boundary (CUR-01/CUR-02/CUR-03)

- Gate: Curriculum boundary (CUR-01/02/03)
- Reviewer role required: designated editorial approver
- Inspection date: 2026-08-15
- Disposition: blocking-finding
- Attestation: pending -- designated editorial approver has not signed this gate. No attestation was sought or recorded during this inspection.
- Decision under review: `docs/decisions/IDK-001-mvp-curriculum-spine.md` — Status: approved, Decision version `1.0`, Policy identifier `mvp-curriculum-spine-v1`, Approval date 2026-08-14 (per the document's own header, `grep -n "Status:\|Decision version\|Policy identifier\|Approval date" docs/decisions/IDK-001-mvp-curriculum-spine.md` → lines 3, 5, 7, 9). Confirmed byte-identical to round 3's copy this round: `git diff 93103cd..825a01f -- docs/decisions/IDK-001-mvp-curriculum-spine.md` produced zero lines of output, so this inspection did not edit it, and its content was re-spot-checked (boundary/subjects at line 17, status header lines 3-9) rather than re-read cover to cover, since an empty diff already proves no round-3 content moved.
- Tree inspected: `HEAD` = `0f3219f6a8b2a2436fce6d36c918307655a53c84` (`git rev-parse HEAD`). Inspection began at `825a01f` and was re-run against `0f3219f` after it landed mid-inspection — see "HEAD delta check" below. Round 3's gate-1 file (`docs/approvals/IDK-503-rerun-2026-08-15-b/gate-1-curriculum-boundary.md`, inspected `HEAD=7b805bd`) was read only to recover scope and the open-findings list, per instruction. Every citation, count, and test result below was independently re-derived against `HEAD=825a01f`, not copied forward, and re-confirmed unaffected by the delta to `0f3219f`. This round takes the `-c` suffix because two prior rounds already share the calendar date 2026-08-15 (round 2 and round 3); a round is identified by the tree it inspected, not the day.
- Implementing tickets: IDK-102 (offline canonical publisher) — `IMPLEMENTATION_TICKETS.md:557`, `Status: Complete` (checked at line 560); IDK-201 (topic layer authoring) — `IMPLEMENTATION_TICKETS.md:878`, `Status: Complete` (checked at line 881). Both status lines and both "Approval gate"/scope notes are unchanged text this round — `git diff 93103cd..825a01f -- IMPLEMENTATION_TICKETS.md` produced zero lines of output. Both remain mechanism-only: IDK-102's own "Approval gate" note (`IMPLEMENTATION_TICKETS.md:607`) states production seeding "additionally requires IDK-001 (curriculum content) and IDK-002 (approval criteria)"; IDK-201's own scope note (`:894`) states it "consumes whatever `generated_artifacts`/authored `content_revisions` exist, it does not produce them."

## What changed since round 3 (`git log 93103cd..825a01f`) — assessed against this gate

Round 3 inspected final `HEAD=7b805bd`. Four commits landed since: `4493208`, `90e23c3`, `2b5ebc2`, `825a01f`. I read each commit's full `--stat` diff and checked file paths and content against this gate's scope (CUR-01 curriculum boundary, CUR-02 DSA-scenario relations and Go rejection, CUR-03 bounded-coverage claims, the `canonical` module, `docs/decisions/IDK-001-mvp-curriculum-spine.md`, `src/selected/`).

- **`4493208`** — "give rubric manifest loading a production entry point" (IDK-503 B11 mechanism). Touches `server/pyproject.toml` (+8), new `server/scripts/load_rubric_manifest.py` (247 lines), `server/src/yuno/modules/evidence_evaluation/service.py` (+172/-1), new `server/tests/integration/test_load_rubric_manifest_script.py` (404 lines). `evidence_evaluation` is the rubric/assessment module (gate 5 territory), not `canonical`. Does not touch this gate.
- **`90e23c3`** — "give source registration a production entry point" (IDK-503 B4 mechanism). Touches new `server/scripts/register_source.py` (261 lines), `server/src/yuno/modules/provenance/service.py` (+120), new `server/tests/integration/test_register_source_script.py` (381 lines). `provenance` is the sources/licensing module (gate 3 territory), not `canonical`. Does not touch this gate.
- **`2b5ebc2`** — "ship the 20-per-source snapshot janitor" (IDK-003 §12 item 9, janitor half). Touches `server/src/yuno/api/app.py` (+69/-14), `server/src/yuno/modules/provenance/adapters.py` (+152), new `server/tests/integration/test_provenance_snapshot_janitor.py` (633 lines). Snapshot retention under `provenance`, not `canonical`. Does not touch this gate.
- **`825a01f`** — "ship the 180-day source staleness re-check" (IDK-003 §12 item 9, cadence half). Touches `server/src/yuno/api/app.py` (+141/-1), `server/src/yuno/modules/provenance/service.py` (+78), new `server/tests/integration/test_provenance_staleness_recheck.py` (577 lines). Same `provenance` module. Does not touch this gate.

**Verified mechanically, not just by per-commit reasoning:**

```
git diff 93103cd..825a01f --stat -- server/src/yuno/modules/canonical server/alembic docs/decisions src/selected
```
produced zero output — no file under the `canonical` module, no Alembic migration, no decision document, and no `src/selected/` UI file was touched by any of the four commits.

```
git diff 93103cd..825a01f | grep -niE "canonical_graph_versions|topic_relations|scope_tags|topics\b|editorial_approvals|content_revisions"
```
also matched nothing (exit 1) — none of the four commits' diff text even mentions the canonical-graph tables or `scope_tags`, confirming none publishes a canonical graph version or alters curriculum-boundary surfaces, per this round's brief.

**Conclusion: none of the four round-4 commits (`4493208`, `90e23c3`, `2b5ebc2`, `825a01f`) bears on gate 1.** All four are `provenance`/`evidence_evaluation`/`api/app.py` server-side CLI-entry-point and background-janitor work, exactly as the round's brief anticipated, confirmed here rather than assumed.

## HEAD delta check (`825a01f` → `0f3219f`)

The coordinator reported HEAD moved two further commits after the analysis above was drafted. I did not restamp silently: I ran `git diff 825a01f 0f3219f --stat` myself and checked both path scope and keyword scope against this gate.

- **Commits:** `636bf26` "fix: correct five line citations this round's own commits invalidated" and `0f3219f` "fix: restore the import-boundary contract-declaration test to green" (`git log --oneline 825a01f..0f3219f`).
- **Files touched (`git diff 825a01f 0f3219f --stat`):** `docs/assessment/IDK-009-critical-dimension-exposure.md` (+1/-1), `docs/runner/IDK-406-execution-deferral.md` (+1/-1), `server/src/yuno/modules/evidence_evaluation/service.py` (+2/-2), `server/tests/architecture/test_import_boundaries.py` (+7), `server/tests/integration/test_runner.py` (+1/-1). No file under `server/src/yuno/modules/canonical`, no Alembic migration, no `docs/decisions/**` file, and no `src/selected/**` file appears — confirmed by re-running the same path-scoped diff this file's earlier section used: `git diff 825a01f 0f3219f --stat -- server/src/yuno/modules/canonical server/alembic docs/decisions src/selected` produced zero output.
- **Keyword scope:** `git diff 825a01f 0f3219f | grep -niE "canonical_graph_versions|topic_relations|scope_tags|topics\b|editorial_approvals|content_revisions|ALLOWED_SUBJECTS|_GO_TOKENS|_validate_curriculum_boundary|_validate_no_go_nodes|_validate_dsa_scenario_relations"` matched nothing (exit 1) — the delta's diff text never mentions the canonical-graph tables, `scope_tags`, or any boundary-validator symbol this gate cites.
- **What the two commits actually are, per the coordinator's own description and consistent with the diff stat:** `636bf26` is a citation-text-only fix (five stale line-number references corrected across `evidence_evaluation/service.py`, an assessment doc, an integration test, and a runner-deferral doc) — none of the five touched files is one this gate cites, and no line count changed anywhere in the repo, so none of this file's own line citations (`validation.py`, `CorePages.tsx`, `IMPLEMENTATION_TICKETS.md`) could have drifted. `0f3219f` adds two entries to an expected-set literal in `server/tests/architecture/test_import_boundaries.py` for `provenance.service -> identity.**` and `evidence_evaluation.service -> identity.**` — an architecture-test declaration-parity fix, not a change to any canonical/topic table, migration, or curriculum-boundary code path.
- **Row counts and validator/UI line citations not re-queried:** the delta carries no migration and touches no `canonical`-module, `src/selected/`, or decision-document file, so re-running the database queries or the `validation.py`/`CorePages.tsx` `grep -n`s would reconfirm the same evidence already recorded above at `HEAD=825a01f`, not test anything the diff could have changed.
- **Conclusion: `636bf26`/`0f3219f` do not change this gate's disposition, findings, or blocking finding.** B1 (no production `canonical_graph_versions` row) and every other finding above are unaffected — nothing in this delta bears on gate 1's territory.

## Database state — verified myself this round, both files, read-only, plus a fresh scratch DB

`YUNO_DATABASE_URL` (not `DATABASE_URL`) is the app's env var; used `sqlite3 -readonly` against `server/yuno.db` and `server/.e2e.db`, nothing written.

```
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM canonical_graph_versions;"  -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM topics;"                    -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM topic_identities;"          -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM topic_relations;"           -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM editorial_approvals;"       -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM content_revisions;"         -> 0
sqlite3 -readonly server/yuno.db "SELECT * FROM alembic_version;"                  -> 4747447ccaa3

sqlite3 -readonly server/.e2e.db "SELECT count(*) FROM canonical_graph_versions;"  -> 0
sqlite3 -readonly server/.e2e.db "SELECT count(*) FROM topics;"                    -> 0
sqlite3 -readonly server/.e2e.db "SELECT * FROM alembic_version;"                  -> a9d4e6f1b208
```

Identical to round 3's counts — re-run today, not copied forward. Per the task's instruction, I did not treat either file as current-head evidence for schema claims; instead I built a fresh scratch DB:

```
YUNO_DATABASE_URL="sqlite+pysqlite:////private/tmp/claude-501/-Users-abhishekthakur-Developer-i-dont-know/9c868a29-735f-4258-a923-0ad512c3073a/scratchpad/gate1_scratch.db" uv run --directory server alembic upgrade head
```

applied cleanly, ending at `be4d11f03666` (confirmed via `uv run --directory server alembic heads` → `be4d11f03666 (head)`). This is the **same** real code head round 3 recorded — the four round-4 commits carry no new migration, confirmed both by the empty `alembic upgrade head` chain surprise (none appeared) and directly: `find` for any file under `server/alembic/versions` newer than round 3's approval directory returned nothing, and the `--stat` diffs above show no `server/alembic/**` path touched.

Against the fresh scratch DB: `canonical_graph_versions`/`topics`/`topic_relations`/`editorial_approvals`/`content_revisions` are all `0` rows (empty-schema state, not a fixture-population artifact — the schema was just created by `alembic upgrade head`, nothing seeds it). `.schema topics` shows no `CHECK` constraint on `level_tag` or `target_capability`:

```
CREATE TABLE topics (
	graph_version_id TEXT NOT NULL,
	stable_id TEXT NOT NULL,
	title TEXT NOT NULL,
	subject TEXT NOT NULL,
	scope_tags TEXT NOT NULL,
	level_tag TEXT NOT NULL,
	target_capability TEXT NOT NULL,
	recommended_layer TEXT NOT NULL,
	checkpoint_start INTEGER NOT NULL,
	checkpoint_end INTEGER NOT NULL,
	CONSTRAINT pk_topics PRIMARY KEY (graph_version_id, stable_id),
	CONSTRAINT fk_topics_graph_version_id_canonical_graph_versions FOREIGN KEY(graph_version_id) REFERENCES canonical_graph_versions (id),
	CONSTRAINT fk_topics_stable_id_topic_identities FOREIGN KEY(stable_id) REFERENCES topic_identities (stable_id)
);
```

— matching the decision document's own §15 gap ("`topics.level_tag`/`target_capability` carry no database `CHECK` constraint today"), re-confirmed against a real migrated schema rather than assumed. `server/yuno.db`'s `alembic_version` (`4747447ccaa3`) remains two revisions behind the real head (`be4d11f03666`); neither missing migration (`4cb74877e4ba` sources license/withdrawal columns, `be4d11f03666` `hands_on_work` scenario widening) touches a canonical/topic table, so this does not change gate 1's disposition — same conclusion as round 3, re-verified rather than carried forward.

## Inspected artifacts

| Artifact | What it is | How inspected |
| --- | --- | --- |
| `server/yuno.db`, `server/.e2e.db` | Local dev / e2e SQLite databases | Read-only `sqlite3 -readonly ... "SELECT count(*) FROM ..."` against `topics`, `topic_identities`, `topic_relations`, `canonical_graph_versions`, `editorial_approvals`, `content_revisions`; `SELECT * FROM alembic_version` |
| `/private/tmp/.../9c868a29.../scratchpad/gate1_scratch.db` | Fresh scratch DB, migrated to real head this round | `uv run --directory server alembic upgrade head` (ended at `be4d11f03666`, confirmed via `alembic heads`); `.schema topics` and row counts, all `0` |
| `server/src/yuno/modules/canonical/validation.py` | Canonical manifest validator (CUR-01/CUR-02 rules) | Re-`grep -n`'d this round: `ALLOWED_SUBJECTS` (39-48), `_GO_TOKENS` (50), `compute_manifest_hash` (113), `validate_manifest` (161), `_validate_curriculum_boundary` (323-335), `_validate_no_go_nodes` (338-350), `_validate_dsa_scenario_relations` (353-376) — identical line numbers to round 3 since this file is untouched by any of the four new commits (confirmed via the `--stat`/path-scoped diff above) |
| `server/tests/unit/test_canonical_validation.py` | Unit tests for validation rules | Ran `uv run --directory server pytest tests/unit/test_canonical_validation.py -k "go_node" -q` → 1 passed; full file `uv run --directory server pytest tests/unit/test_canonical_validation.py -q` → 24 passed, this round |
| `server/tests/fixtures/canonical/data/invalid_go_node.json` | Synthetic fixture proving Go rejection | Read file contents in full this round; unchanged content (`subject: "go"`, explicit non-production `description` field) |
| `docs/decisions/IDK-001-mvp-curriculum-spine.md` | The approved curriculum-boundary decision | Confirmed byte-identical to round 3 via `git diff 93103cd..825a01f` (0 lines); boundary/subjects re-checked at line 17, header fields at lines 3-9 |
| `src/selected/core/CorePages.tsx` | Curriculum-facing UI copy (Roadmap, Practice) | `grep -rniE "comprehensive\|full coverage\|entire curriculum\|complete curriculum\|all topics\|every topic"` over `src/` (no matches, exit 1); re-derived render-branch line numbers fresh via `grep -n` this round: 438, 450, 1009 (see Findings table) |
| `src/selected/core/ProfileGoalsPages.test.tsx`, `src/selected/core/PracticeProviderStatus.test.tsx` | Frontend tests touching `Roadmap`/`Practice` | `source ~/.nvm/nvm.sh && nvm use 24.19.0 && npx vitest run src/selected/core/ProfileGoalsPages.test.tsx src/selected/core/PracticeProviderStatus.test.tsx` → 2 files passed, 25 tests passed |
| `IMPLEMENTATION_TICKETS.md:557` (IDK-102), `:878` (IDK-201) | Ticket status/scope fields | Confirmed byte-identical to round 3 via `git diff 93103cd..825a01f -- IMPLEMENTATION_TICKETS.md` (0 lines); status/scope lines re-read at 560/607 and 881/894 |
| `docs/decisions/IDK-003-source-licensing-and-snapshot-policy.md` §7, §12 | Product-owner note re: licence rights (context check) | Read §7 (`:90-100`) and confirmed field 5 is the citation "License identifier" attribution requirement — `provenance`/Sources-display territory, not curriculum boundary; see "Notes" below |
| `git log --oneline 93103cd..825a01f`, `git show --stat`/path-scoped diff for `4493208`, `90e23c3`, `2b5ebc2`, `825a01f` | Round-4 commit history | Read commit messages and `--stat` diffs for each; ran path- and keyword-scoped diffs against this gate's scope; see "What changed since round 3" above |

## Findings

| # | Decision requirement | Shipped reality (with citation) | Verdict |
| --- | --- | --- | --- |
| 1 | A production `canonical_graph_versions` row seeded from `mvp-curriculum-spine-v1` exists, carrying the 53 topics/74 relations of §7-§8, so `scope_tags` can be compared row-by-row against the approved boundary. | `sqlite3 -readonly server/yuno.db "SELECT count(*) FROM canonical_graph_versions"` = `0`; `... FROM topics"` = `0`; `... FROM topic_relations"` = `0`; `... FROM editorial_approvals"` = `0`. Identical zero counts in `server/.e2e.db` and in a fresh scratch DB migrated to the real head `be4d11f03666`, built and queried this round. None of the four commits landed since round 3 writes to `canonical_graph_versions`, `topics`, `topic_relations`, or `editorial_approvals` (confirmed via path- and keyword-scoped `git diff 93103cd..825a01f`, see above). | not shipped — still open |
| 2 | CUR-02: validation actively rejects any Go node. | `_validate_no_go_nodes` (`server/src/yuno/modules/canonical/validation.py:338-350`) checks `topic.subject`/`scope_tags` against `_GO_TOKENS = {"go","golang","go_aws"}` (line 50), appends `ViolationCode.GO_NODE_PRESENT`. Exercised by `server/tests/unit/test_canonical_validation.py` against fixture `server/tests/fixtures/canonical/data/invalid_go_node.json` (topic `subject: "go"`). Re-run today: `pytest tests/unit/test_canonical_validation.py -k "go_node" -q` → 1 passed; full file → 24 passed. | matches (validation logic proven at unit level, re-confirmed this round) |
| 3 | CUR-02: the shipped MVP graph itself contains no Go node. | No shipped MVP graph exists to check (finding 1). The only Go-labeled data anywhere is the synthetic `invalid_go_node.json` fixture, whose own `description` field states the overlap with `OUT_OF_BOUNDARY_CURRICULUM_TAG` is "expected/correct behaviour, not a fixture bug" — i.e., explicitly non-production. | not shipped (nothing to verify absence against; not a pass) |
| 4 | CUR-01: `ALLOWED_SUBJECTS` matches the decision's six subjects exactly. | `server/src/yuno/modules/canonical/validation.py:39-48`: `{"java", "spring_boot", "aws", "system_design", "rdb", "dsa"}` — identical to `docs/decisions/IDK-001-mvp-curriculum-spine.md:17`'s six subjects. `_validate_curriculum_boundary` (lines 323-335) rejects any topic whose `subject` is outside this set. File is byte-identical to round 3's (no commit since touches it) — re-confirmed via fresh `grep`, not assumed. | matches |
| 5 | CUR-03 / bounded claims: shipped curriculum-facing copy does not overstate coverage. | `grep -rniE "comprehensive\|full coverage\|entire curriculum\|complete curriculum\|all topics\|every topic"` over `src/` today (at `HEAD=825a01f`) returns no matches (exit code 1). Re-derived fresh via `grep -n "roadmap is unavailable\|No roadmap topics are available\|Approved practice content is not available" src/selected/core/CorePages.tsx`: `:438` renders `"Your roadmap is unavailable"` (in `Roadmap`, when `!goal \|\| (roadmap.roadmap.isError && !roadmap.roadmap.data)`); `:450` renders `"No roadmap topics are available"` (when `topics.length === 0`); `:1009` renders `"Approved practice content is not available yet"` (in `Practice`, when `!testScenario`). All three line numbers are **unchanged from round 3** — `git diff 93103cd..825a01f --stat -- src/selected` is empty, so `CorePages.tsx` was not touched by any of the four new commits and the `:1004→:1009` correction round 3 made stands without further drift. No stale citation to correct this round. | matches — no overstatement found, but this remains a weak signal since only empty/unavailable states exist to check (finding 1) |
| 6 | The shipped graph version is the one IDK-001 approves (version id, publish provenance, `basis_ref`). | No `canonical_graph_versions` row exists (finding 1), so there is no version id or `editorial_approvals.basis_ref` to check provenance against. `docs/decisions/IDK-001-mvp-curriculum-spine.md:389,415` (§17, §20) states this directly; document confirmed byte-identical to round 3 this round. | not shipped |
| 7 | IDK-102 (publisher) / IDK-201 (topic layers) ticket status accurately reflects what's shipped. | `IMPLEMENTATION_TICKETS.md:557`: IDK-102 `Status: Complete` (`:560`) — scoped to the *mechanism*; its own "Approval gate" text (`:607`) states production seeding "additionally requires IDK-001 ... and IDK-002." `IMPLEMENTATION_TICKETS.md:878`: IDK-201 `Status: Complete` (`:881`) — scoped to the *read/serve pathway*, consuming `content_revisions` (currently 0 rows) rather than authoring it (`:894`, "it does not produce them"). Both status lines and their surrounding scope text are byte-identical to round 3 (`git diff 93103cd..825a01f -- IMPLEMENTATION_TICKETS.md` → 0 lines). Neither "Complete" is a claim that production curriculum content exists; both remain consistent with finding 1. | matches (tickets do not overstate; the gap is real, not a ticket-status error) |
| 8 | CUR-03 / graph-absence UI behaviour is test-covered, not just source-read. | `npx vitest run src/selected/core/ProfileGoalsPages.test.tsx src/selected/core/PracticeProviderStatus.test.tsx` → 2 files passed, 25 tests passed (real run this round, node v24.19.0 via nvm). However, `grep -n "it("` over both files shows no test case named around the empty-roadmap (`topics.length === 0`), roadmap-unavailable, or practice-`!testScenario` branches specifically — the passing suites cover *other* `Roadmap`/`Practice` behaviour (stale-proposal handling, background-refresh failure, retryable-failure retry action), not these three exact copy branches. | partial — components are tested and passing, but the specific empty/unavailable-state branches cited in finding 5 are verified by source-reading, not by a named test assertion; not a new blocking finding (the branches render correct, unexciting copy) but the CUR-03 graph-absence UI path is not test-locked |

## Blocking findings

### 1. No production canonical graph has been published — CUR-01 boundary and CUR-02 graph-absence cannot be inspected against shipped content

- **What is missing:** A `canonical_graph_versions` row (with its `topics`/`topic_relations`/`topic_identities`/`content_revisions`/`editorial_approvals`, per IMPLEMENTATION_SPEC §4.3) seeded from `mvp-curriculum-spine-v1`. This is the artifact gate 1's scope requires: the shipped canonical graph's `scope_tags`, checked row-by-row against IDK-001 §7's 53 topics and §12's exclusions.
- **Owning ticket:** IDK-102's production seed run, distinct from its (Complete) mechanism build. `IMPLEMENTATION_TICKETS.md:607` names IDK-001 and IDK-002 as additional prerequisites for that run; the decision document's own §14 (`docs/decisions/IDK-001-mvp-curriculum-spine.md:355-362`) lists six unmet conditions: content authored by IDK-201, sources registered per IDK-003 §12, a real manifest passing `validate_manifest` with a computed `manifest_hash`, IDK-002's checklist completed against that manifest and recorded in a valid `basis_ref`, the D1 publisher run, and a resulting `editorial_approvals` row.
- **Evidence of absence, verified today:** `sqlite3 -readonly server/yuno.db "SELECT count(*) FROM topics"` = `0`; `... FROM canonical_graph_versions"` = `0`; `... FROM editorial_approvals"` = `0`; `... FROM content_revisions"` = `0`. Identical zero counts in `server/.e2e.db` and in a fresh scratch DB migrated to the real code head `be4d11f03666` (`/private/tmp/claude-501/-Users-abhishekthakur-Developer-i-dont-know/9c868a29-735f-4258-a923-0ad512c3073a/scratchpad/gate1_scratch.db`) — so the zero counts are not an artifact of either database being behind head; the schema is empty by construction, not by migration lag. The only topic/relation data anywhere in the repo lives in `server/tests/fixtures/canonical/data/*.json`, explicitly labelled synthetic/non-production by the fixtures themselves and by the decision document's §3. None of the four commits landed since round 3 (`4493208`, `90e23c3`, `2b5ebc2`, `825a01f`) writes to `canonical_graph_versions`, `topics`, `topic_relations`, or `editorial_approvals`, or runs `server/scripts/publish_canonical.py` against a real manifest — confirmed this round via path-scoped and keyword-scoped `git diff 93103cd..825a01f` (see "What changed since round 3" above). All four commits instead ship `provenance`/`evidence_evaluation`/`api/app.py` CLI entry points and janitor jobs unrelated to canonical publication.
- **What would clear it:** IDK-201-authored `content_revisions` for the 53 topics, IDK-003 §12's source registration, a manifest built from IDK-001 §7-§9 passing `validate_manifest` with zero violations, IDK-002's checklist completed by the designated editorial approver against that exact manifest and recorded in a valid `basis_ref`, and IDK-102's D1 publisher run producing a matching `canonical_graph_versions` + `editorial_approvals` pair. Gate 1 must then be re-run against that shipped version, row-by-row against §7's 53 topics and §12's exclusions — the validator mechanism alone (finding 2/4 above) is not a substitute.
- **Nature of this finding:** partly content (IDK-201's authoring — an editorial/content-owner obligation, not closable by engineering alone) and partly implementation (the publish run itself, which is engineering work gated on that content existing). Neither half can be closed by this review, and this round's four commits did not move either half — they shipped source-registration, rubric-manifest-loading, snapshot-janitor, and staleness-recheck mechanisms in the `provenance`/`evidence_evaluation` modules instead, none of which is a prerequisite this finding's clearance list names.

## Notes and residual risk

- The validation *mechanism* (CUR-01 boundary check, CUR-02 Go rejection, CUR-02 DSA-scenario requirement, manifest hashing) is real, shipped, and unit-tested (24/24 passing today, re-run this round) — a positive finding, but explicitly not a substitute for inspecting shipped curriculum content, since gate 1 requires reviewing the decision against the shipped canonical graph's `scope_tags`, not against the validator alone. Unchanged since round 3, re-verified rather than assumed.
- I explicitly checked all four round-4 commits against this gate's scope, both by reading each `--stat` diff and by running path-scoped and keyword-scoped `git diff` across all four at once, and found none touches it — see "What changed since round 3" above.
- No line citation required correction this round: `src/selected/core/CorePages.tsx` was untouched by any of the four commits (`git diff 93103cd..825a01f --stat -- src/selected` is empty), so the `:438`, `:450`, `:1009` citations round 3 left (after its own `:1004→:1009` fix) are re-derived fresh here via `grep -n` and hold at the same numbers. This round's "treat every inherited line number as suspect" instruction was followed — the numbers were re-run, not trusted — and happened to confirm no drift occurred.
- New this round (finding 8): the two `Roadmap`/`Practice`-touching frontend test files pass in full (25/25), but no test case specifically exercises the `topics.length === 0` empty-roadmap branch, the `!goal || isError` roadmap-unavailable branch, or the `!testScenario` practice-unavailable branch cited in finding 5. This is not elevated to a blocking finding — the rendered copy is correct and unexciting, and CUR-03's requirement is about not overstating coverage, which the grep-based check in finding 5 still satisfies — but it means the graph-absence UI path is verified by source-reading, not by a regression-locking test, and a future change to that copy would not be caught by CI.
- `server/yuno.db`'s `alembic_version` (`4747447ccaa3`) is two migrations behind the real code head (`be4d11f03666`) — re-verified this round via a fresh scratch-DB migration (`alembic upgrade head` → `alembic heads`), not by reading the stale database's own claims. Neither missing migration touches a canonical/topic table, so this does not affect gate 1's disposition.
- The `topics.level_tag`/`target_capability` columns still carry no database `CHECK` constraint at the real migration head, confirmed against a freshly migrated scratch DB (`.schema topics`) rather than against either stale review-artifact database. This is a pre-existing gap the decision document itself names (§15) and is unchanged this round.
- Context check on the product-owner licence-rights note in this round's brief: IDK-003 §7 field 5 ("License identifier" in the citation attribution contract, `docs/decisions/IDK-003-source-licensing-and-snapshot-policy.md:94`) and §12 item 3 are both `provenance`/Sources-display requirements — they govern what a rendered citation must show, not `scope_tags`, `ALLOWED_SUBJECTS`, `topics`, or any curriculum-boundary surface. Confirmed by reading §7 in full this round. This is gate 3's territory as the brief anticipated; it does not touch gate 1 and is recorded here only as the requested context check, not as a finding.
- The decision document transparently states this gate's central gap itself (§14 "Known gaps," §17 "Stop point," §20 "Approval statement") — not a new discovery, but still a blocking condition for gate 1 as scoped.
- This finding blocks gate 1 sign-off only; it does not by itself block unrelated MVP surfaces that don't depend on a published graph.
- I could not verify anything beyond what is recorded above — every citation in this file was produced by running the cited command or reading the cited file at `HEAD=825a01f` during this inspection.
