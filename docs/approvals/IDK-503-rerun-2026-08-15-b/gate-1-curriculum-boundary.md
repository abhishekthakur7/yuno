# IDK-503 rerun (2026-08-15, round 3) — gate 1: Curriculum boundary (CUR-01/CUR-02/CUR-03)

- Gate: Curriculum boundary (CUR-01/02/03)
- Reviewer role required: designated editorial approver
- Inspection date: 2026-08-15
- Disposition: blocking-finding
- Attestation: pending -- designated editorial approver has not signed this gate. No attestation was sought or recorded during this inspection.
- Decision under review: `docs/decisions/IDK-001-mvp-curriculum-spine.md` — Status: approved, Decision version `1.0`, Policy identifier `mvp-curriculum-spine-v1`, Approval date 2026-08-14 (per the document's own §19/§20). Full re-read this round, all 20 sections.
- Tree inspected: `HEAD` = `7b805bdd72d5740eb8180f3e6fde9c33af2a672f` (`git rev-parse HEAD`). Inspection began at `796fa306b6eb69745c4aad3b3a3ac64997c845d6` and was re-run against `7b805bd` after it landed mid-inspection — see "HEAD delta check" below. Round 2's gate-1 file was read only to recover scope, per instruction; every citation, count, and test result below was re-derived against this HEAD, not copied forward.
- Implementing tickets: IDK-102 (offline canonical publisher) — `IMPLEMENTATION_TICKETS.md:557-608`, `Status: Complete`; IDK-201 (topic layer authoring) — `IMPLEMENTATION_TICKETS.md:878-921`, `Status: Complete`. Both statuses are unchanged text this round (confirmed by diff, see below) and both remain mechanism-only: IDK-102's own "Approval gate" note (`IMPLEMENTATION_TICKETS.md:606-607`) and IDK-201's (`:920-921`) each state that real production content additionally requires IDK-001 and IDK-002, and that this ticket's own acceptance does not require them.

## What changed since round 2 (`git log 2621d29..HEAD`) — assessed against this gate

Round 2 inspected `HEAD=2621d29`. Five commits landed since: `0a6e7c9`, `bd05da6`, `cbd4c16`, `5596654`, `796fa30`. I read each commit's full diff (`git show --stat` and, where non-trivial, `git show`) and checked it against gate 1's scope (CUR-01 curriculum boundary, CUR-02 DSA-scenario relations, CUR-03 bounded-coverage claims, the `canonical` module, `docs/decisions/IDK-001-mvp-curriculum-spine.md`).

- **`0a6e7c9`** — records the `is_critical` API-exposure decision (`docs/assessment/IDK-009-critical-dimension-exposure.md`, new file), fixes a stale comment in `server/src/yuno/modules/evidence_evaluation/models.py`, and reconciles `IMPLEMENTATION_TICKETS.md` status for IDK-204/302/303/405 from `Complete` to a newly-defined `Content incomplete` (diff: `git log -p 2621d29..HEAD -- IMPLEMENTATION_TICKETS.md`, lines 92-114 of that log). I checked this does not touch this gate: `is_critical` is a rubric-dimension/assessment field (gate 5 territory), `evidence_evaluation/models.py` is not the `canonical` module, and the four reconciled tickets are IDK-204/302/303/405 — none is IDK-102 or IDK-201. I confirmed directly that IDK-102's and IDK-201's status lines are untouched by this commit (`grep -n "^### IDK-102\|^### IDK-201" IMPLEMENTATION_TICKETS.md` plus the `git log -p` diff above shows no hunk inside either ticket's body). I also checked whether IDK-102/201 *should* have received the same "Content incomplete" treatment: no — unlike IDK-204/302/303/405 (which read `Complete` with no caveat before this commit), IDK-102 and IDK-201 already carry their own accurate "Approval gate" caveat naming IDK-001/IDK-002 as additional requirements, so their `Complete` status is not a misrepresentation this reconciliation needed to fix. Does not touch this gate.
- **`bd05da6`** — renders IDK-003 §7 citation attribution on `TopicTools`' resources tab via `ArtifactProvenanceDetails`/`useArtifactProvenance` (`src/selected/core/CorePages.tsx:696,706,781` at this HEAD). This is the round-2 gate-3 finding B5's "third surface," about citation/provenance display on generated Sources-layer content, not about curriculum topic boundary, `scope_tags`, or DSA-scenario relations. Confirmed by reading the touched lines: no `scope_tags`, `ALLOWED_SUBJECTS`, or coverage-claim copy appears in the diff. Does not touch this gate.
- **`cbd4c16`** — onboarding's `targetLevel` state and `BundleEditor.createBundle` now require an explicit level selection instead of defaulting to `'Senior'` (`src/selected/core/CorePages.tsx`, diff verified via `git show cbd4c16`). This is IDK-004 §4 level-vocabulary/fail-closed-selection territory (gate 4/6-adjacent), not IDK-001 curriculum-spine topic/relation/scope-tag content. No `topics`, `topic_relations`, `canonical`, or curriculum-boundary code is touched. Does not touch this gate.
- **`5596654`** — ships `server/scripts/withdraw_source.py`, an offline editorial CLI, with a `Role.DESIGNATED_EDITORIAL_APPROVER` grant check inside `provenance/service.py` (gate-3 finding B7 remainder, source withdrawal). Checked: `grep -n "canonical\|topics\|topic_relations\|scope_tags" server/scripts/withdraw_source.py` returns only comments describing it as "modelled on" / "mirrors" `publish_canonical.py`'s ordering pattern — it does not call into or modify the `canonical` module, and it operates on `sources`, not `topics`/`topic_relations`. Does not touch this gate.
- **`796fa30`** — two follow-up fixes: (a) `server/pyproject.toml` adds an import-linter `ignore_imports` entry for `yuno.modules.provenance.service -> yuno.modules.identity.**`, needed because `5596654`'s grant check made `provenance.service` import `identity.domain`; the existing `canonical.publisher -> identity` edge (cited in the same comment) is untouched — no new or modified entry for the `canonical` module. (b) `src/selected/core/CorePages.tsx` collapses `TopicTools`' six optional/defaulted props into one required `sourcesProvenance` prop — pure UI-plumbing cleanup of the `bd05da6` change, same resources-tab/citation surface, not curriculum-boundary data. Does not touch this gate.

**Conclusion: no round-3 commit up to `796fa30` bears on gate 1.** None writes to `canonical_graph_versions`, `topics`, `topic_relations`, `topic_identities`, `content_revisions`, or `editorial_approvals`; none edits `server/src/yuno/modules/canonical/**`, a canonical-table migration, `docs/decisions/IDK-001-mvp-curriculum-spine.md`, or curriculum-boundary-facing copy. Round 2's finding is re-verified below at this HEAD rather than assumed unchanged.

## HEAD delta check (`796fa30` → `7b805bd`)

The coordinator reported HEAD moved one further commit, `7b805bd` "fix: three defects the round-3 gate inspections found," after the analysis above was drafted. I did not restamp silently: I ran `git diff 796fa30 7b805bd --stat` myself and read the full diff of every file it reported changed.

- **Files touched (confirmed via `git diff 796fa30 7b805bd --stat`):** `src/selected/core/CorePages.tsx` (+8/-3), `src/selected/core/InterviewHub.test.tsx` (+8/-3), `server/tests/integration/test_runner.py` (+18/-2), `docs/assessment/IDK-009-critical-dimension-exposure.md` (+2/-2). No migration file, no `server/src/yuno/modules/canonical/**` file, and no `docs/decisions/IDK-001-mvp-curriculum-spine.md` appears in the diff.
- **`src/selected/core/CorePages.tsx`, read in full via `git diff 796fa30 7b805bd -- src/selected/core/CorePages.tsx`:** a hunk starting at old line 813, inside `BundleEditor` (the same interview-bundle-creation component `cbd4c16` touched last round). It adds a `createName` state, requires `createName.trim()` before `createBundle` fires, replaces the generated `` `${createLevel} backend interview` `` default with the learner-typed `createName.trim()`, and adds a "Bundle name" `<input>`. The commit's own inline comment cites "IDK-503 round 3, gate 4" — this is a gate-4 (role/copy) fix to a not-inventing-learner-facing-copy defect, not a curriculum-boundary/`scope_tags`/`topics` change. It touches no code path this gate inspects.
- **Line-number impact on my own citations:** my gate-1 citations of `src/selected/core/CorePages.tsx:438,450,1004` (the roadmap-unavailable, no-roadmap-topics, and practice-unavailable render branches, Finding 5) needed re-checking because the edit landed at old line 813, upstream of at least the 1004 citation. I re-ran `grep -n "roadmap is unavailable\|No roadmap topics are available\|Approved practice content is not available" src/selected/core/CorePages.tsx` at `HEAD=7b805bd`: lines **438** and **450** are unchanged (both are above the edit's insertion point relative to file-start offsetting — the net +5 lines land only after line 813, and 438/450 precede it), and line **1004** has shifted to **1009** (the net +5 lines from this commit's insertions-minus-deletions land between the two). All three citations below are corrected to the re-derived numbers; 1004 is retired.
- **`src/selected/core/InterviewHub.test.tsx`:** test coverage for the same `BundleEditor` change (asserts the create button stays disabled without a typed name, per `git diff 796fa30 7b805bd -- src/selected/core/InterviewHub.test.tsx`). Same gate-4 territory, no bearing here.
- **`server/tests/integration/test_runner.py`:** adds a `threading`-based filter so the flaky-assertion fix excludes `jobs-<lane>` background-dispatcher threads from the `statements`/`checkouts` listeners used by `test_retired_relational_language_rejected_before_route_or_uow` (the runner-posture test `2621d29` introduced). This is a test-infrastructure fix for a gate-7 (runner posture) test's flakiness — it does not touch the `canonical` module, any canonical/topic table, or a curriculum-boundary code path. No bearing here.
- **`docs/assessment/IDK-009-critical-dimension-exposure.md`:** two citation corrections — `models.py:401` → `models.py:406` for `assessment_dimension_results.is_critical`'s persisted-column line, and a rewritten sentence distinguishing the one `is_critical` consumer that branches (`derive_progress`, `domain.py:378`) from three read-only carriers (`service.py:384`, `repository.py:318`, `repository.py:795`). This is gate 5's `is_critical`-exposure decision document (the same one `0a6e7c9` created last round) — rubric-dimension/assessment territory, not `docs/decisions/IDK-001-mvp-curriculum-spine.md` or any file this gate governs. No bearing here.
- **Re-ran what my findings rest on, at `7b805bd`:** `grep -rniE "comprehensive|full coverage|entire curriculum|complete curriculum|all topics|every topic"` over `src/` — still no matches (exit 1), so Finding 5's "no overstatement" conclusion is unchanged at the corrected line numbers. `uv run pytest tests/unit/test_canonical_validation.py -q` — still 24 passed (this file is untouched by `7b805bd`, re-run for completeness rather than assumed). Database row counts and `alembic_version` were not re-queried, since `7b805bd` carries no migration and touches no `canonical`-module or database file — re-querying would re-confirm the same zero counts already recorded above at `796fa30`, not test anything the diff could have changed.
- **Conclusion: `7b805bd` does not change this gate's disposition, findings, or blocking finding.** It fixes gate-4, gate-5, and gate-7 defects respectively; the only effect on this file is the corrected `CorePages.tsx` line-1009 citation below (previously 1004) and the restamped HEAD. Nothing else in this document was copied forward without re-checking against `7b805bd`.

## Database state — verified myself this round, both files, read-only

`YUNO_DATABASE_URL` (not `DATABASE_URL`) is the app's env var; used `sqlite3 -readonly` against `server/yuno.db` and `server/.e2e.db`, nothing written.

```
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM canonical_graph_versions;"  -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM topics;"                    -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM topic_identities;"          -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM topic_relations;"           -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM editorial_approvals;"       -> 0
sqlite3 -readonly server/yuno.db "SELECT count(*) FROM content_revisions;"         -> 0
sqlite3 -readonly server/yuno.db "SELECT * FROM alembic_version;"                  -> 4747447ccaa3

sqlite3 -readonly server/.e2e.db "SELECT count(*) FROM canonical_graph_versions;"  -> 0
sqlite3 -readonly server/.e2e.db "SELECT count(*) FROM topics;"                    -> 0
sqlite3 -readonly server/.e2e.db "SELECT * FROM alembic_version;"                  -> a9d4e6f1b208
```

Per the task's instruction, I did not treat either file as current-head evidence for schema claims; instead I built a fresh scratch DB: `YUNO_DATABASE_URL="sqlite+pysqlite:////private/tmp/claude-501/-Users-abhishekthakur-Developer-i-dont-know/a6bd149b-804a-4c2e-b4c5-e72f75eeb97e/scratchpad/gate1_scratch.db" uv run --directory server alembic upgrade head` — applied cleanly, ending at `be4d11f03666`, confirmed via `uv run alembic heads` = `be4d11f03666 (head)`. Against that fresh DB: `topics`/`canonical_graph_versions`/`editorial_approvals` all `0` rows (same empty-schema state, not a fixture-population artifact), and `.schema topics` shows no `CHECK` constraint on `level_tag` or `target_capability` — matching the decision document's own §15 gap ("`topics.level_tag`/`target_capability` carry no database `CHECK` constraint today"), re-confirmed against a real migrated schema rather than assumed.

`server/yuno.db`'s `alembic_version` (`4747447ccaa3`) remains two revisions behind the real head: `alembic history` (run against the real app config) shows the chain `... fb1c910aedc7 -> 4747447ccaa3 -> 4cb74877e4ba -> be4d11f03666 (head)` — `yuno.db` is missing `4cb74877e4ba` (sources license/withdrawal columns) and `be4d11f03666` (`hands_on_work` scenario widening). Neither missing migration touches a canonical/topic table, so this does not change gate 1's disposition, but it means `yuno.db` cannot be treated as "current" without this check — same conclusion round 2 reached, re-verified independently this round rather than carried forward. `server/.e2e.db` remains at `a9d4e6f1b208`, unchanged.

## Inspected artifacts

| Artifact | What it is | How inspected |
| --- | --- | --- |
| `server/yuno.db`, `server/.e2e.db` | Local dev / e2e SQLite databases | Read-only `sqlite3 -readonly ... "SELECT count(*) FROM ..."` against `topics`, `topic_identities`, `topic_relations`, `canonical_graph_versions`, `editorial_approvals`, `content_revisions`; `SELECT * FROM alembic_version` |
| `/private/tmp/.../scratchpad/gate1_scratch.db` | Fresh scratch DB, migrated to real head | `uv run --directory server alembic upgrade head` (ended at `be4d11f03666`, confirmed via `alembic heads`); `sqlite3 ... ".schema topics"` and row counts |
| `server/src/yuno/modules/canonical/validation.py` | Canonical manifest validator (CUR-01/CUR-02 rules) | Read in full this round; re-confirmed line ranges: `ALLOWED_SUBJECTS` (39-48), `_GO_TOKENS` (50), `compute_manifest_hash` (113), `validate_manifest` (161), `_validate_curriculum_boundary` (323-335), `_validate_no_go_nodes` (338-350), `_validate_dsa_scenario_relations` (353-374) |
| `server/tests/unit/test_canonical_validation.py` | Unit tests for validation rules | Ran `uv run pytest tests/unit/test_canonical_validation.py -k "go_node" -q` → 1 passed; full file `uv run pytest tests/unit/test_canonical_validation.py -q` → 24 passed, this round |
| `server/tests/fixtures/canonical/data/invalid_go_node.json` | Synthetic fixture proving Go rejection | Read file contents in full this round; unchanged content (`subject: "go"`, explicit non-production `description` field) |
| `docs/decisions/IDK-001-mvp-curriculum-spine.md` | The approved curriculum-boundary decision | Full read, all 20 sections, this round |
| `src/selected/core/CorePages.tsx` | Curriculum-facing UI copy (Roadmap, Topic Studio, Practice) | `grep -rniE "comprehensive\|full coverage\|entire curriculum\|complete curriculum\|all topics\|every topic"` over `src/` (no matches, exit 1); read `unavailable`/`empty` render branches at their current line numbers (438, 450, 1009 — re-derived at `HEAD=7b805bd` after `BundleEditor`'s edit shifted the third; see "HEAD delta check" above) |
| `IMPLEMENTATION_TICKETS.md:557-608` (IDK-102), `:878-921` (IDK-201) | Ticket status/scope fields | Read in full this round; `git log -p 2621d29..HEAD -- IMPLEMENTATION_TICKETS.md` to confirm neither section's body or status line changed |
| `git log --oneline 2621d29..HEAD`, `git show --stat <sha>` / full `git show` for `0a6e7c9`, `bd05da6`, `cbd4c16`, `5596654`, `796fa30`, `7b805bd` | Round-3 commit history | Read commit messages and full diffs for each; see "What changed since round 2" and "HEAD delta check" above |

## Findings

| # | Decision requirement | Shipped reality (with citation) | Verdict |
| --- | --- | --- | --- |
| 1 | A production `canonical_graph_versions` row seeded from `mvp-curriculum-spine-v1` exists, carrying the 53 topics/74 relations of §7-§8, so `scope_tags` can be compared row-by-row against the approved boundary. | `sqlite3 -readonly server/yuno.db "SELECT count(*) FROM canonical_graph_versions"` = `0`; `... FROM topics"` = `0`; `... FROM topic_relations"` = `0`; `... FROM editorial_approvals"` = `0`. Identical zero counts in `server/.e2e.db` and in a fresh scratch DB migrated to the real head `be4d11f03666`. Verified today at `HEAD=796fa30`, not carried forward; re-confirmed unaffected at final `HEAD=7b805bd` (no migration or canonical-module file in that commit's diff — see "HEAD delta check"). | not shipped |
| 2 | CUR-02: validation actively rejects any Go node. | `_validate_no_go_nodes` (`server/src/yuno/modules/canonical/validation.py:338-350`) checks `topic.subject`/`scope_tags` against `_GO_TOKENS = {"go","golang","go_aws"}` (line 50), appends `ViolationCode.GO_NODE_PRESENT`. Exercised by `server/tests/unit/test_canonical_validation.py` against fixture `server/tests/fixtures/canonical/data/invalid_go_node.json` (topic `subject: "go"`). Re-run today: `pytest tests/unit/test_canonical_validation.py -k "go_node" -q` → 1 passed; full file → 24 passed. | matches (validation logic proven at unit level, re-confirmed this round) |
| 3 | CUR-02: the shipped MVP graph itself contains no Go node. | No shipped MVP graph exists to check (finding 1). The only Go-labeled data anywhere is the synthetic `invalid_go_node.json` fixture, whose own `description` field states the overlap with `OUT_OF_BOUNDARY_CURRICULUM_TAG` is "expected/correct behaviour, not a fixture bug" — i.e., explicitly non-production. | not shipped (nothing to verify absence against; not a pass) |
| 4 | CUR-01: `ALLOWED_SUBJECTS` matches the decision's six subjects exactly. | `server/src/yuno/modules/canonical/validation.py:39-48`: `{"java", "spring_boot", "aws", "system_design", "rdb", "dsa"}` — identical to `docs/decisions/IDK-001-mvp-curriculum-spine.md:17`'s six subjects. `_validate_curriculum_boundary` (lines 323-335) rejects any topic whose `subject` is outside this set. Unchanged since round 2 — re-read in full this round, not assumed. | matches |
| 5 | CUR-03 / bounded claims: shipped curriculum-facing copy does not overstate coverage. | `grep -rniE "comprehensive\|full coverage\|entire curriculum\|complete curriculum\|all topics\|every topic"` over `src/` today (at `HEAD=7b805bd`) returns no matches (exit code 1). `src/selected/core/CorePages.tsx:438` renders `"Your roadmap is unavailable"`; `:450` renders `"No roadmap topics are available"`; `:1009` renders `"Approved practice content is not available yet"`. Same three states as round 2, at shifted line numbers (unrelated commits `bd05da6`/`cbd4c16`/`796fa30`/`7b805bd` each inserted lines above/around these — the third citation moved from 1004 to 1009 across this last commit specifically, re-derived via `grep -n` at final HEAD rather than assumed) — re-read and re-confirmed this round, not assumed unchanged. | matches — no overstatement found, but this remains a weak signal since only empty/unavailable states exist to check (finding 1) |
| 6 | The shipped graph version is the one IDK-001 approves (version id, publish provenance, `basis_ref`). | No `canonical_graph_versions` row exists (finding 1), so there is no version id or `editorial_approvals.basis_ref` to check provenance against. `docs/decisions/IDK-001-mvp-curriculum-spine.md:389,415` (§17, §20) states this directly. | not shipped |
| 7 | IDK-102 (publisher) / IDK-201 (topic layers) ticket status accurately reflects what's shipped. | `IMPLEMENTATION_TICKETS.md:557-608`: IDK-102 `Status: Complete` — scoped to the *mechanism*; its own "Approval gate" text (`:606-607`) states production seeding "additionally requires IDK-001 ... and IDK-002" and carries no estimate. `IMPLEMENTATION_TICKETS.md:878-921`: IDK-201 `Status: Complete` — scoped to the *read/serve pathway and checkpoint contract*, consuming `content_revisions` (currently 0 rows) rather than authoring it (`:894`, "Out of scope"). Both status lines are byte-identical to round 2 (confirmed via `git log -p 2621d29..HEAD -- IMPLEMENTATION_TICKETS.md`, which shows no hunk touching either ticket's body — the only edits in this window are the IDK-204/302/303/405 rows and the vocabulary appendix, `0a6e7c9`). Neither "Complete" is a claim that production curriculum content exists; both remain consistent with finding 1. | matches (tickets do not overstate; the gap is real, not a ticket-status error) |

## Blocking findings

### 1. No production canonical graph has been published — CUR-01 boundary and CUR-02 graph-absence cannot be inspected against shipped content

- **What is missing:** A `canonical_graph_versions` row (with its `topics`/`topic_relations`/`topic_identities`/`content_revisions`/`editorial_approvals`, per IMPLEMENTATION_SPEC §4.3) seeded from `mvp-curriculum-spine-v1`. This is the artifact gate 1's scope requires: the shipped canonical graph's `scope_tags`, checked row-by-row against IDK-001 §7's 53 topics and §12's exclusions.
- **Owning ticket:** IDK-102's production seed run, distinct from its (Complete) mechanism build. `IMPLEMENTATION_TICKETS.md:606-607` names IDK-001 and IDK-002 as additional prerequisites for that run; the decision document's own §14 (`docs/decisions/IDK-001-mvp-curriculum-spine.md:355-362`) lists six unmet conditions: content authored by IDK-201, sources registered per IDK-003 §12, a real manifest passing `validate_manifest` with a computed `manifest_hash`, IDK-002's checklist completed against that manifest and recorded in a valid `basis_ref`, the D1 publisher run, and a resulting `editorial_approvals` row.
- **Evidence of absence, verified today:** `sqlite3 -readonly server/yuno.db "SELECT count(*) FROM topics"` = `0`; `... FROM canonical_graph_versions"` = `0`; `... FROM editorial_approvals"` = `0`; `... FROM content_revisions"` = `0`. Identical zero counts in `server/.e2e.db` and in a fresh scratch DB migrated to the real code head `be4d11f03666` (`/private/tmp/claude-501/-Users-abhishekthakur-Developer-i-dont-know/a6bd149b-804a-4c2e-b4c5-e72f75eeb97e/scratchpad/gate1_scratch.db`) — so the zero counts are not an artifact of either database being behind head; the schema is empty by construction, not by migration lag. The only topic/relation data anywhere in the repo lives in `server/tests/fixtures/canonical/data/*.json`, explicitly labelled synthetic/non-production by the fixtures themselves and by the decision document's §3. None of the six commits landed since round 2 (`0a6e7c9`, `bd05da6`, `cbd4c16`, `5596654`, `796fa30`, `7b805bd`) writes to `canonical_graph_versions`, `topics`, `topic_relations`, or `editorial_approvals`, or runs `server/scripts/publish_canonical.py` against a real manifest — confirmed by reading each commit's full diff this round (see "What changed since round 2" and "HEAD delta check" above).
- **What would clear it:** IDK-201-authored `content_revisions` for the 53 topics, IDK-003 §12's source registration, a manifest built from IDK-001 §7-§9 passing `validate_manifest` with zero violations, IDK-002's checklist completed by the designated editorial approver against that exact manifest and recorded in a valid `basis_ref`, and IDK-102's D1 publisher run producing a matching `canonical_graph_versions` + `editorial_approvals` pair. Gate 1 must then be re-run against that shipped version, row-by-row against §7's 53 topics and §12's exclusions — the validator mechanism alone (finding 2/4 above) is not a substitute.
- **Nature of this finding:** partly content (IDK-201's authoring — an editorial/content-owner obligation, not closable by engineering alone) and partly implementation (the publish run itself, which is engineering work gated on that content existing). Neither half can be closed by this review, and this round's five commits did not move either half.

## Notes and residual risk

- The validation *mechanism* (CUR-01 boundary check, CUR-02 Go rejection, CUR-02 DSA-scenario requirement, manifest hashing) is real, shipped, and unit-tested (24/24 passing today, re-run this round) — a positive finding, but explicitly not a substitute for inspecting shipped curriculum content, since gate 1 requires reviewing the decision against the shipped canonical graph's `scope_tags`, not against the validator alone. Unchanged since round 2, re-verified rather than assumed.
- I explicitly checked all six round-3 commits (through the mid-inspection HEAD move to `7b805bd`) against this gate's scope and found none touches it — see "What changed since round 2" and "HEAD delta check" above for the per-commit reasoning, not a bare "unchanged" assertion.
- `server/yuno.db`'s `alembic_version` (`4747447ccaa3`) is two migrations behind the real code head (`be4d11f03666`) — verified this round via `alembic history`/`alembic heads` and a fresh scratch-DB migration, not by reading the stale database's own claims. Neither missing migration touches a canonical/topic table, so this does not affect gate 1's disposition, but claims that `yuno.db` is "current" should not be taken at face value elsewhere in this rerun without the same check.
- No overstated-coverage copy was found in shipped UI source (finding 5), but this remains a weak signal: with no graph published, curriculum-facing surfaces render only `empty`/`unavailable` states, not actual topic listings, so CUR-03's bounded-claims check has still not been exercised against real rendered content.
- The `topics.level_tag`/`target_capability` columns still carry no database `CHECK` constraint at the real migration head, confirmed against a freshly migrated scratch DB (`.schema topics`) rather than against either stale review-artifact database, per the task's instruction. This is a pre-existing gap the decision document itself names (§15) and is unchanged this round.
- The decision document transparently states this gap itself (§14 "Known gaps," §17 "Stop point," §20 "Approval statement") — not a new discovery, but still a blocking condition for gate 1 as scoped.
- This finding blocks gate 1 sign-off only; it does not by itself block unrelated MVP surfaces that don't depend on a published graph.
- I could not verify anything beyond what is recorded above — every citation in this file was produced by running the cited command or reading the cited file at either `HEAD=796fa30` (database, validator, ticket, and decision-document evidence, unaffected by the later move) or the final `HEAD=7b805bd` (the `CorePages.tsx` line numbers and the copy-overstatement/unit-test re-checks) during this inspection.
