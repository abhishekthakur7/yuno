# IDK-503 rerun (2026-08-15, round 3) gate 4 — Learner-facing role copy and taxonomy

- Gate: Learner-facing role copy and taxonomy
- Reviewer role required: designated editorial approver
- Inspection date: 2026-08-15
- Disposition: blocking-finding
- Attestation: pending -- designated editorial approver has not signed this gate. No attestation was sought or recorded during this inspection.

This is round 3, a fresh independent re-inspection originally conducted against HEAD `796fa306b6eb69745c4aad3b3a3ac64997c845d6`, not a diff against round 2. The coordinator then advanced the tree by one commit while this file was in progress: current HEAD is `7b805bdd72d5740eb8180f3e6fde9c33af2a672f` (`7b805bd`, "fix: three defects the round-3 gate inspections found"), which acted directly on this gate's own blocking finding 2. This file was updated in place — see "HEAD delta check" below for that re-derivation, done by reading the actual diff and re-running the affected tests, not by trusting the commit message. `docs/approvals/IDK-503-rerun-2026-08-15/gate-4-role-copy.md` (round 2, HEAD `2621d29`) was read only to recover scope, per instruction; none of its citations were copied forward — every line number and test result below was re-derived against the relevant HEAD. Round 1 (`docs/approvals/IDK-503/`) and round 2 were not edited.

## What changed since round 2 and whether it is mine

`git log --oneline 2621d29..HEAD`:

| Commit | Summary | In this gate's scope? |
| --- | --- | --- |
| `cbd4c16` | fix: require an explicit target level at first-use setup (IDK-503 B17) | Yes — directly targets round 2's finding 2 for this gate |
| `bd05da6` | feat: render IDK-003 §7 attribution on the resources tab (IDK-503 B5) | No — `git show bd05da6 -- src/selected/core/CorePages.tsx` touches only `Topic`'s `sourcesProvenance` wiring and `TopicTools`'s new provenance props; no `ROLE_LEVEL_COPY`, `Onboarding`, `BundleEditor`, or `InterviewHub` line is touched |
| `796fa30` | fix: restore module-independence contract, drop `TopicTools` prop defaults | No — `git show 796fa30 --stat` touches `server/pyproject.toml` (import-linter) and `TopicTools`'s prop shape (collapsing six optional provenance props into one `sourcesProvenance` prop); same resources-tab feature, not role copy |
| `0a6e7c9` | docs: `is_critical` exposure decision, ticket-status reconciliation | No — `IMPLEMENTATION_TICKETS.md`, a new `docs/assessment/` file, and an `evidence_evaluation/models.py` comment; confirmed via `git show 0a6e7c9 --stat` |
| `5596654` | feat: production entry point for source withdrawal (IDK-503 B7) | No — `server/scripts/withdraw_source.py` and `provenance/service.py`; confirmed via `git show 5596654 --stat` |

Only `cbd4c16` touches this gate's surfaces.

## HEAD delta check (`796fa30` → `7b805bd`)

`git log --oneline 796fa30..7b805bd` shows one commit, `7b805bd` ("fix: three defects the round-3 gate inspections found"), addressing one finding each from gates 4, 5, and 7 in a single commit. Only the gate-4 portion is inspected here; the gate-5 (`docs/assessment/IDK-009-critical-dimension-exposure.md`) and gate-7 (`server/tests/integration/test_runner.py`) portions are out of this gate's scope and were not re-inspected. No `tests/e2e/*` file changed in this delta.

`git diff 796fa30 7b805bd -- src/selected/core/CorePages.tsx src/selected/core/InterviewHub.test.tsx` (read in full, not just the stat):

- `CorePages.tsx:816-819` (new): a `createName` state (`useState('')`) with a comment citing this gate's round-3 finding by name.
- `CorePages.tsx:828-829`: `createBundle`'s guard becomes `if (!createLevel || !createName.trim()) return`; the mutation's `name` field becomes `name: createName.trim()` (`:832`), replacing the former `` `${createLevel} backend interview` `` template entirely — the template string is gone from the file, not just unused.
- `CorePages.tsx:859`: a new `<label>Bundle name<input value={createName} onChange={event => setCreateName(event.target.value)} /></label>` in the empty-state creation form, with no `placeholder` attribute and no default value.
- `CorePages.tsx:861`: the create button's `disabled` expression gains `|| !createName.trim()`.
- `InterviewHub.test.tsx:209-241`: the bundle-creation fail-closed test was rewritten. It now asserts the create button stays **disabled after a level is chosen** (`:228-231`) — proving the product supplies no default name once a level is picked — and only enables after `userEvent.type(screen.getByLabelText('Bundle name'), 'My backend prep')` (`:233-234`). The POST body and the rendered bundle both show the learner-typed string verbatim (`:239-240`), never a generated one.

**Verdict on blocking finding 2: closed.** My original finding was that `` `${createLevel} backend interview` `` shipped two new, unreviewed sentences (`'Mid-level backend interview'`, `'Staff backend interview'`) and that the `Staff` case used the bare enum instead of the approved `Staff-level` label. The fix does not pick either of my two suggested remedies (get editorial sign-off, or compose from `ROLE_LEVEL_COPY[level].label`) — both of which still end at "someone must approve a string." It removes the string-generation step entirely. `createBundle` (`:828-842`) no longer derives, templates, or defaults the bundle's `name` from `createLevel` at all; the only value that ever reaches the API is whatever the learner typed into the `Bundle name` field, and creation is blocked until that field is non-empty. There is no default value, no placeholder text, and no fallback string anywhere in the diff.

Answering the coordinator's three specific questions:
1. **Is any new learner-facing string introduced?** No. The input at `CorePages.tsx:859` carries no `placeholder`, `defaultValue`, or fallback — `createName` starts as `''` (`:819`) and the button stays disabled until the learner supplies content (`:861`), confirmed live by the rewritten test refusing to enable the button on level-selection alone (`InterviewHub.test.tsx:228-231`). The only string this diff introduces onto the screen is the word "Bundle name" itself, addressed next.
2. **Is "Bundle name" genuinely already-shipped copy, or does reusing it in a second place need review?** It is verbatim reuse of the identical label already shipped at `CorePages.tsx:866` (the existing-bundle editor's own `<label>Bundle name<input value={name} .../></label>`), unchanged by this diff and unchanged since before IDK-004 existed as a decision. It is not new prose, not level-derived, and not in IDK-004's jurisdiction at all — IDK-004 §2's "Exact learner-facing copy" list covers the level/capability taxonomy copy only, never a generic form-field label. The two renders are mutually exclusive (`bundles.length === 0` at `:858` vs. `selected &&` at `:863`), so there is no risk of the string reading inconsistently on one screen. Judgment: labeling a text input "Bundle name" in a second, structurally identical place is ordinary UI composition, not copy authorship, and does not need separate review.
3. **Does requiring a name change any IDK-004 requirement, or is it orthogonal?** Orthogonal. IDK-004 §4's fail-closed rule is written about the **level** selection specifically ("fails closed to a required selection rather than silently becoming Senior or another level") and says nothing about a bundle's name. The commit message's framing — "extends the same fail-closed rule IDK-004 section 4 sets for level to the name beside it" — is a design analogy the author drew, not something IDK-004 itself requires. Requiring a non-empty name before creating a bundle is a sound, independent product decision that happens to reuse the same disabled-button mechanism; it neither satisfies nor violates any IDK-004 text on its own, and I am crediting it only for what it actually does — eliminate the unreviewed-copy problem — not for extending IDK-004 compliance it was never asked to extend.

Nothing else cited in the original report changed in this delta. `Onboarding` (`CorePages.tsx:197-416`), `ROLE_LEVEL_COPY` (`:48-68`), and everything about B10/hands-on content sit before or outside the touched hunk; re-confirmed unchanged by direct grep (`ROLE_LEVEL_HEADING`, the `targetLevel` state, and the `beginDiagnostic` guard all still sit at `:51`, `:204`, `:245` respectively). Every citation at or after `CorePages.tsx:812` elsewhere in this file has been re-derived against the new line numbers (the insertion at `:816-819` shifts everything after it in `BundleEditor` and `InterviewHub` down by 5 lines relative to the previous version of this report).

## Inspected artifacts

| Artifact | What it is | How inspected |
| --- | --- | --- |
| `docs/decisions/IDK-004-role-level-competencies.md` (unchanged since round 2, confirmed via `git log 2621d29..HEAD -- docs/decisions/IDK-004-role-level-competencies.md` → no output) | Approved `role-competency-copy-v1` copy, capability ladder, §4 ambiguity/history rules | Read source |
| `docs/decisions/IDK-009-assessment-and-derived-state.md` (unchanged since round 2, same empty-log check) | Evaluator-calibration table, 12-record scenario registry | Read source |
| `src/selected/core/CorePages.tsx:48-68` | `ROLE_LEVEL_COPY` registry (`role-competency-copy-v1`) | Read source; byte-compared to IDK-004 §2 by script |
| `src/selected/core/CorePages.tsx:197-416` (`Onboarding`) | First-use goal setup: level state (`:202-204`), submit gate (`:245`), heading/select/helper (`:389-392`), submit-button disable expression (`:399`) | Read source |
| `src/selected/core/CorePages.tsx:798-876` (`INTERVIEW_ROLE_LEVELS`, `BundleEditor`) | Interview Prep bundle role/level controls, empty-state creation gating for level and name (`:812-819`, `:828-842`, `:858-862`), existing-bundle editor level control (`:867`) | Read source |
| `src/selected/core/CorePages.tsx:917-967` (`InterviewHub`) | Interview Prep hub eyebrow derivation (`:949-950`) | Read source |
| `src/selected/core/ProfileGoalsPages.test.tsx:247-305` | Copy/association test and the new IDK-004 §4 fail-closed test | Read source; executed |
| `src/selected/core/InterviewHub.test.tsx:60-109`, `:209-241` | Association test, eyebrow-derivation tests (Mid-level, Staff), and the bundle-creation fail-closed test (rewritten by `7b805bd` to also require a learner-supplied name) | Read source; executed |
| `IMPLEMENTATION_TICKETS.md:1683-1722` (IDK-405) | Confirms IDK-405's status and owning-ticket claim for the still-open B10 content half | Read source |
| `server/src/yuno/modules/hands_on/service.py:95-119`, `domain.py:12-25` | Confirms the hands-on module is untouched since round 2 (`git log 2621d29..HEAD -- server/src/yuno/modules/hands_on/` → no output) and still synthesizes `scenario_status="fixture"` positionally | Read source |
| A fresh scratch SQLite database, migrated to head | Independent re-confirmation that `be4d11f03666`'s schema shape (CHECK admitting `'approved'`, `scenario_id` column, both immutability triggers) still holds, since nothing since round 2 touched it | `YUNO_DATABASE_URL=sqlite+pysqlite:////private/tmp/.../scratchpad/gate4_scratch.db uv run --directory server alembic upgrade head`, then `sqlite3 gate4_scratch.db ".schema hands_on_work"` |
| `server/yuno.db`, `server/.e2e.db` (read-only) | Confirms both remain behind head and both have 0 `hands_on_work` rows | `sqlite3 -readonly server/yuno.db "SELECT version_num FROM alembic_version"` / `"SELECT COUNT(*) FROM hands_on_work"`; same for `.e2e.db` |
| `git show cbd4c16 -- src/selected/core/CorePages.tsx` | The actual diff implementing B17's fix, read directly rather than trusted from the commit message | `git show` |
| A Python byte-comparison script | Confirmed the four exported copy constants and all three per-level label/description pairs match IDK-004 §2 exactly, character-for-character, including the U+2014 em dash in the title-variation helper (`ord()` check, not eyeballing) | `python3` inline script diffing regex-extracted decision-doc text against regex-extracted `CorePages.tsx` source |
| `grep -rni "beginner\|entry.level\|predict\|hiring\|promotion" src/selected server/src/yuno`, `sqlite3 -readonly server/yuno.db ".schema goal_workspaces"` | Re-confirms the no-beginner and non-prediction rules still hold | Executed |
| `grep -rn "Not sure" docs/decisions/IDK-004-role-level-competencies.md src/selected server/src/yuno` | Checks whether IDK-004 §4's "Not sure" helper text ships anywhere | Executed — only the decision doc matches |

Tests executed:
- `npx vitest run src/selected/core/ProfileGoalsPages.test.tsx src/selected/core/InterviewHub.test.tsx` at HEAD `796fa30` → **26 passed, 0 failed**.
- Same command, re-run at HEAD `7b805bd` after the delta above → **26 passed, 0 failed** (same file/test count; `InterviewHub.test.tsx`'s bundle-creation test was rewritten in place, not added, so the total is unchanged).

## Findings

| # | Decision requirement | Shipped reality (with citation) | Verdict |
| --- | --- | --- | --- |
| 1 (B8, re-verified) | Exact IDK-004 §2 copy ships verbatim, programmatically associated with the controls, across onboarding and Interview Prep. | `ROLE_LEVEL_HEADING`, `ROLE_LEVEL_AUDIENCE_NOTE`, `ROLE_LEVEL_TITLE_VARIATION_HELPER`, `TARGET_CAPABILITY_HELPER`, and the three `ROLE_LEVEL_COPY[...].label`/`.description` strings (`CorePages.tsx:51-67`) are byte-identical to IDK-004 §2, confirmed by script (not eyeballed), including the em dash (U+2014) in the title-variation helper. `aria-describedby` wiring intact: onboarding (`CorePages.tsx:390-392`), bundle create (`:860`), bundle edit (`:867`). `ProfileGoalsPages.test.tsx:247-283` and `InterviewHub.test.tsx:60-75` pass. Settings (`OperationalPages.tsx`) was not re-touched by any commit since round 2 and is out of this gate's changed-file scope for round 3, but was not re-read here; treat that surface as unverified this round rather than assumed still correct. | **B8 still holds** for the surfaces re-inspected this round |
| 2 (B9, re-verified) | Interview Prep eyebrow follows the goal's actual `target_level`. | `CorePages.tsx:949-950`: `const currentLevel = workspace.currentGoal?.target_level; const eyebrow = currentLevel ? \`Interview prep · ${ROLE_LEVEL_COPY[currentLevel].label}\` : 'Interview prep'`. No hardcoded literal. `InterviewHub.test.tsx:77-93` (Mid-level) and `:95-109` (Staff) pass; the Senior case remains structurally exercised by the default fixture but still has no dedicated string-match assertion — same minor test-coverage gap noted in round 2, unchanged. | **B9 still holds** |
| 3 (B17, this round's fix) | IDK-004 §4: no preselected level at first-use; explicit confirmation required; invalid/missing value fails closed rather than becoming Senior. | `CorePages.tsx:204`: `useState<GoalCreate['target_level'] \| ''>('')` — genuinely unselected. `:245`: `if (!graphVersion \|\| !targetLevel) return` gates submission on the diagnostic-create path itself, not just the button. `:399`: `disabled={... \|\| !targetLevel}` on the visible submit button. `:390`: `<option value="" />` is the first, initially-selected option. `:392`: the per-level description renders only `{targetLevel && ...}`. `ProfileGoalsPages.test.tsx:247-273` asserts the control starts at `value === ''` with no description shown; `:286-305` (new test, "cannot submit onboarding without an explicit target level choice") asserts the submit button is disabled after filling every other field and becomes enabled only after an explicit level choice. Both pass. | **Closed** — genuinely fails closed; a learner cannot reach a `Senior` (or any) goal without an explicit choice |
| 4 (B17, secondary instance) | Same fail-closed rule applied to `BundleEditor.createBundle`'s "recommended" bundle. | `CorePages.tsx:815`: `useState<InterviewLevel \| ''>('')`. `:828-829`: `if (!createLevel \|\| !createName.trim()) return` inside `createBundle`. `:861`: `disabled={... \|\| !createLevel \|\| !createName.trim()}` on the create button. `InterviewHub.test.tsx:209-241` (rewritten this delta) confirms the button starts disabled, stays disabled after only a level is chosen, and only enables once a name is also typed. Passes. | **Closed** — and strengthened this delta (see HEAD delta check) |
| 5 (new, copy question a) | Whether the empty-label placeholder option is an acceptable ship, given no approved placeholder string exists. | `CorePages.tsx:390` and `:860` both render `<option value="" />` — no text content at all, confirmed by `ProfileGoalsPages.test.tsx:261-266` asserting the first option's `.text` is `''`. IDK-004 §2's "Exact learner-facing copy" list (the only section using verbatim blockquotes) contains no placeholder string; inventing one (e.g. "Select a level") would violate the hard "never invent learner-facing copy" rule this inspection itself operates under. | **Not blocking, but flagged** — see discussion below |
| 6 (copy question b, resolved this delta) | Whether `BundleEditor.createBundle`'s parameterised name (`` `${createLevel} backend interview` ``) was "shipping approved strings verbatim" or new copy. | As of `796fa30`, `createLevel` (the raw stored enum, not `ROLE_LEVEL_COPY[createLevel].label`) was interpolated into a name template, producing `'Mid-level backend interview'` and `'Staff backend interview'` — two sentences no decision document had reviewed, the latter using the bare token `Staff` rather than the approved `Staff-level` wording. `7b805bd` removed the template entirely: `createBundle` now sends `createName.trim()` (`CorePages.tsx:832`), a value that originates only from the learner's own typing (`:816-819`, `:859`) and is never defaulted. See "HEAD delta check" above for the full re-derivation. | **Closed by `7b805bd`** — no learner-facing string is generated by the product at all now, so the original question ("verbatim or new copy?") no longer applies |
| 7 (new) | IDK-004 §4: `'Not sure' is helper text, not a fourth stored level: choose Mid-level for bounded component work, Senior for end-to-end flows, or Staff for cross-system and multi-team decisions.'` | `grep -rn "Not sure" docs/decisions/IDK-004-role-level-competencies.md src/selected server/src/yuno` matches only the decision document (line 63). No "Not sure" affordance, option, or equivalent guidance string exists anywhere in `src/selected`. | **Does not ship** — see discussion below |
| 8 (re-verified) | No-beginner rule: no shipped option or copy implies a beginner track. | `sqlite3 -readonly server/yuno.db ".schema goal_workspaces"` still shows `CHECK (target_level IN ('Mid-level','Senior','Staff'))`. `grep -rni "beginner\|entry.level\|entry-level" src/selected server/src/yuno` finds only the approved audience-note negation. | matches (still passes) |
| 9 (re-verified) | Non-prediction rule. | `grep -rni "predict\|hiring\|promotion" src/selected/**/*.tsx` returns only the approved title-variation-helper negation, the Interview Prep hub's negation (`CorePages.tsx:951`), and the unrelated `background_age_promotion_seconds` job-engine string (`OperationalPages.tsx:311`). | matches (still passes) |
| 10 (B10, content half, re-verified) | The 12 approved IDK-009 `idk009-v1-r1` scenario records must be authored/loaded. | `server/src/yuno/modules/hands_on/service.py:105-118` (line numbers shifted slightly from round 2's `95-119` because of an intervening `_goal, topic, role, level, constraints = scenario_for(...)` reformat, not a behavior change) still constructs every `HandsOnWork` positionally with `"fixture"`, `None`, `"fixture-pending-idk-009"` — confirmed against `domain.py:12-25`'s field order. `grep -rn "idk009-v1-r1\|scenario_id" server/src/yuno/modules/hands_on/*.py` finds no scenario-ID literals. `git log 2621d29..HEAD -- server/src/yuno/modules/hands_on/` is empty — this module was not touched this round. `HandsOnLab.test.tsx:14` still fixtures `status: 'fixture', scenario_id: null`. | **Still open — unchanged since round 2** |
| 11 (B10, schema half, re-verified) | `scenario_status` CHECK, `scenario_id` column, both immutability triggers. | Freshly migrated scratch DB (`gate4_scratch.db`, head `be4d11f03666`) shows `CONSTRAINT ck_hands_on_work_hands_on_scenario_status_valid CHECK (scenario_status IN ('fixture','approved'))`, nullable `scenario_id TEXT`, and both `hands_on_work_immutable_update`/`_delete` triggers present verbatim. | **Still closed — unchanged since round 2** |

## Blocking findings

### 1. Hands-on scenario content authorship remains unimplemented (B10, content half)
- What is missing: The 12 approved IDK-009 `idk009-v1-r1` scenario records are not loaded or bound to topics. Every synthesized `HandsOnWork` row is still hardcoded `scenario_status="fixture"`, `scenario_id=None`, `scenario_source="fixture-pending-idk-009"`.
- Owning ticket: IDK-405, status "Content incomplete" per `IMPLEMENTATION_TICKETS.md:1686` (re-read this round, not assumed from round 2's citation).
- Evidence of absence: `server/src/yuno/modules/hands_on/service.py:105-118`; zero matches for any IDK-009 scenario ID under `server/src/yuno/modules/hands_on/`; `server/yuno.db` and `server/.e2e.db` both hold 0 rows in `hands_on_work` (`sqlite3 -readonly ... "SELECT COUNT(*) FROM hands_on_work"`, this round); `HandsOnLab.test.tsx:14` still fixtures `status: 'fixture'`.
- What would clear it: Load the IDK-009 registry content, bind topics to approved scenario IDs/role/level/constraints text, set `scenario_status="approved"` and a real `scenario_id` for approved content, and re-inspect against a live `hands_on_work` row.

### Resolved this delta: `BundleEditor.createBundle`'s parameterised default name (former blocking finding 2)
This gate's round-3 report (against HEAD `796fa30`) recorded a second blocking finding: `` `${createLevel} backend interview` `` shipped unapproved, mechanically-generated copy, with the `Staff` case using the bare enum instead of the approved `Staff-level` label. `7b805bd` closed it by deleting the generated-name code path rather than approving a string — see "HEAD delta check" above for the full re-derivation (diff read in full, tests re-run, three specific questions answered). It is recorded here, not silently dropped, so the round's audit trail stays intact: the original defect existed, was independently found, and is now independently confirmed fixed by a different mechanism than either remedy this report originally suggested.

## Notes and residual risk

- **Copy question (a) — the empty-label placeholder option.** My judgment: not blocking. IDK-004 §4's actual requirement is behavioral (no preselection, explicit confirmation, fail-closed), and `CorePages.tsx:204/245/390/392/399` satisfy it regardless of what text, if any, labels the placeholder. Inventing a string like "Select a level" would itself violate the "never invent learner-facing copy" rule, so the empty label is the defensible choice under that constraint, not a shortcut around it. That said, it is a genuine open item: a completely blank `<option>` is a legitimate accessibility/UX question (what does a screen reader announce for an unlabeled option in the middle of a list?) that the editorial approver should resolve by supplying an approved placeholder string, or by explicitly deciding blank is acceptable. It does not, on its own, contradict any approved decision text, so it is not listed as a blocking finding.
- **Copy question (b) — the parameterised bundle name.** As originally shipped (`796fa30`), this was new copy needing approval, not "shipping approved strings verbatim" — the `Senior` case happened to reproduce the old literal by coincidence of the template's mechanics, but `Mid-level` and `Staff` were genuinely new, untested, unreviewed sentences, and `Staff` measurably diverged from the approved display-label vocabulary. `7b805bd` closed this by removing the generated name entirely rather than resolving which wording was correct — see "HEAD delta check" above. I judge this fully resolves the question rather than sidestepping it: there is no string left to approve.
- **IDK-004 §4's "Not sure" helper text does not ship anywhere** (finding 7). It sits under §4 "Ambiguity and history rules," not under §2 "Exact learner-facing copy" (the only section that uses blockquote-verbatim formatting), and there is no "Not sure" option or affordance anywhere in the onboarding or bundle-creation UI for this text to attach to — the level `<select>` offers only the three real levels plus the blank placeholder. I cannot tell from the decision text alone whether this sentence was meant to render as on-screen helper copy (e.g., next to the level selector, for a learner who is uncertain) or is meta-guidance constraining any future "not sure" affordance to never become a stored fourth level. Either reading is defensible; what is certain is that no on-screen equivalent — verbatim or paraphrased — exists today. I am not marking this blocking on my own initiative since IDK-004 §4's operative behavioral requirements (no preselection, fail-closed) are independently met without it, but the editorial approver should confirm whether this sentence was intended to ship as copy.
- Settings (`OperationalPages.tsx`'s goal-editor target-level/capability control) was verified in round 2 (B8) but was not touched by any commit since `2621d29` and was not re-read this round because it fell outside the gate's changed-file scope described in the task. Treat it as **not re-verified this round**, not as confirmed still-correct.
- `server/yuno.db` (alembic `4747447ccaa3`) and `server/.e2e.db` (alembic `a9d4e6f1b208`) both remain behind head (`be4d11f03666`) and both have 0 rows in `hands_on_work`, unchanged from round 2 — neither database is evidence for or against B10's schema-shape closure; the scratch DB is.
- B13, B14, B15, B16, and any IDK-504 measurement gaps are outside this gate's scope and were not re-inspected here.
- The e2e changes in `cbd4c16` (`tests/e2e/harness.ts:208-210`, `tests/e2e/selected-app.spec.ts:558-560`, making the shared setup helper choose an explicit level and adjusting a keyboard-typeahead test for the now-unselected placeholder) were read directly from the diff but not executed — the task scoped tests to the two vitest files above, and the e2e suite was not run.
