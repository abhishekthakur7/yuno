# IDK-503 gate 5, round 3 — Representative assessment scenarios and rubric versions

This is a fresh re-inspection, not an amendment of `docs/approvals/IDK-503/gate-5-rubrics-scenarios.md` (2026-08-14, round 1) or `docs/approvals/IDK-503-rerun-2026-08-15/gate-5-rubrics-scenarios.md` (2026-08-15, round 2, `HEAD=2621d29`). Both prior rounds stand untouched. Round 2's gate-5 file was read only to recover scope; every citation below (line numbers, row counts, test results) was independently re-derived against `HEAD=7b805bdd72d5740eb8180f3e6fde9c33af2a672f`, and several of round 2's own line-number citations turned out to have already drifted by the time of this inspection (noted below) — confirming why re-derivation, not copy-forward, is the right discipline. This gate was itself inspected in two passes at two commits within the same day: first at `796fa306b6eb69745c4aad3b3a3ac64997c845d6`, then delta-checked against `7b805bdd72d5740eb8180f3e6fde9c33af2a672f` after that commit corrected two citation errors this gate's first pass had found in `docs/assessment/IDK-009-critical-dimension-exposure.md` — see "HEAD delta check" below.

- Gate: Assessment scenarios and rubric versions (HND-03, DEP-03)
- Reviewer role required: designated editorial approver
- Inspection date: 2026-08-15
- Disposition: blocking-finding
- Attestation: pending -- designated editorial approver has not signed this gate. No attestation was sought or recorded during this inspection.

Decision under review: `docs/decisions/IDK-009-assessment-and-derived-state.md`, approved as decision version 1.0 on 2026-08-13 (content/assessment owner; §12). Implementing tickets referenced: IDK-201 (DEP-03), IDK-204, IDK-205, IDK-302, IDK-303, IDK-405.

## What changed since round 2 (`git log 2621d29..HEAD`)

Seven commits landed: `0a6e7c9`, `bd05da6`, `cbd4c16`, `5596654`, `570b6e2`, `1e65fd5`, `796fa30`. Of these:

- **`0a6e7c9`** ("record the `is_critical` exposure decision and reconcile four ticket statuses") is the only commit that bears directly on gate 5. It touches `docs/assessment/IDK-009-critical-dimension-exposure.md` (new), `IMPLEMENTATION_TICKETS.md`, and `server/src/yuno/modules/evidence_evaluation/models.py`. Assessed in full below.
- **`bd05da6`** (IDK-003 §7 attribution on the resources tab, IDK-503 B5), **`cbd4c16`** (first-use target-level fail-closed, IDK-503 B17), and **`5596654`** (source-withdrawal CLI entry point, IDK-503 B7) touch only frontend `src/selected/` files, `tests/e2e/`, and `server/scripts/withdraw_source.py` / `server/src/yuno/modules/provenance/service.py` — confirmed via `git show --stat` for each. None touch `server/src/yuno/modules/evidence_evaluation/`, `hands_on/`, `roadmap/`, or `diagnostics/`, and none touch `docs/decisions/IDK-009*` or any scenario/rubric content path. **These three do not bear on gate 5** (HND-03/DEP-03); they belong to gates 3 and 4.
- **`796fa30`** ("restore the module-independence contract and drop the TopicTools prop defaults") touches `server/pyproject.toml` (import-linter `ignore_imports` entry for `provenance.service` → `identity.domain`) and frontend `CorePages.tsx`/test files. Confirmed via `git show --stat` that it does not touch any evidence-evaluation, hands-on, or roadmap module. **Does not bear on gate 5.**
- `570b6e2` (Serena project config) and `1e65fd5` (docs re-run housekeeping) are non-functional and out of scope for every gate.

## HEAD delta check (`796fa30` → `7b805bd`)

This gate's own first pass (against `796fa30`) found two citation inaccuracies in `docs/assessment/IDK-009-critical-dimension-exposure.md` (see "The `is_critical` exposure decision, assessed on its merits" below). The coordinator reported that a follow-up commit, `7b805bdd72d5740eb8180f3e6fde9c33af2a672f` ("fix: three defects the round-3 gate inspections found"), corrected both. Rather than restamp that claim, it was re-derived directly:

- `git diff 796fa30 7b805bd -- docs/assessment/IDK-009-critical-dimension-exposure.md` shows exactly a 2-line change: the `models.py` citation changed from `:401` to `:406`, and the "read by exactly one consumer" sentence was rewritten to "Exactly one consumer *decides* anything on it: ... (`domain.py:378`). The other three read-sites only carry the value between layers — `service.py:384` copies it from the rubric dimension onto the result at assessment time, and `repository.py:318` / `repository.py:795` write and read the column. Nothing else branches on it." Both corrections were independently re-verified against the tree at `7b805bd` rather than trusted from the diff or the coordinator's message: `grep -n "is_critical: Mapped\[int\]" server/src/yuno/modules/evidence_evaluation/models.py` → `406:    is_critical: Mapped[int] = boolean_column(...)`, confirming `:406` is now correct. `grep -n "is_critical" server/src/yuno/modules/evidence_evaluation/{domain,service,repository}.py` confirms exactly four read/write sites of a `.is_critical` attribute in application code — `domain.py:378` (the only one inside a conditional that changes a classification outcome — `critical_negative = any(dimension.is_critical and dimension.outcome in negative_outcomes ...)`), `service.py:384` (assignment into a new `AssessmentDimensionResult(...)` constructor call, not a branch), `repository.py:318` (`values["is_critical"] = int(dimension.is_critical)`, a write), and `repository.py:795` (`bool(row.is_critical)`, a read on the way back out of the DB) — matching the corrected text's "one decides, three carry" framing exactly. Both corrections are accurate at `7b805bd`.
- `git diff 796fa30 7b805bd -- server/src/yuno/modules/evidence_evaluation/ server/src/yuno/modules/hands_on/ IMPLEMENTATION_TICKETS.md` is empty (confirmed by running it — no output). B11, B12, the ticket-status reconciliation, and every scratch-DB/grep result recorded below are therefore unaffected by this delta; none needed to be re-run.
- The commit also touched three files outside those directories, checked individually: `src/selected/core/CorePages.tsx` and `src/selected/core/InterviewHub.test.tsx` change only `BundleEditor` in the Interview hub — removing a generated bundle-name default (`` `${createLevel} backend interview` ``) in favor of a required learner-supplied name, with the commit message and code comment both citing this as a gate-4/IDK-004 §4 fix ("A generated default... would be learner-facing copy no decision document has reviewed — see IDK-503 round 3, gate 4"); confirmed by reading the diff directly, nothing in it touches hands-on scenarios, rubrics, or assessment content. `server/tests/integration/test_runner.py` adds a `threading`-based filter so `DurableJobDispatcher`'s background `jobs-<lane>` polling threads are excluded from a SQL-statement assertion in `test_retired_relational_language_rejected_before_route_or_uow` — a gate-7 (runner posture) flaky-test repair, confirmed by reading the diff and the commit message; unrelated to evidence evaluation, hands-on work, or IDK-009 content. **None of these three files bears on gate 5.**
- Conclusion: the corrections are accurate, the files gate 5's findings depend on are untouched by this commit, and the three other changed files belong to gates 4 and 7. HEAD is updated to `7b805bd` below; no finding, citation, or disposition in this report needed to change as a result of this delta beyond the `is_critical`-record assessment itself (updated in place, not restated as still-wrong).

## Inspected artifacts

| Artifact | What it is | How inspected |
| --- | --- | --- |
| Migrated scratch DB (`gate5_scratch.db`) | Fresh SQLite DB, this inspection only | `cd server && YUNO_DATABASE_URL="sqlite+pysqlite:////private/tmp/.../scratchpad/gate5_scratch.db" uv run alembic upgrade head` → reached `be4d11f03666` (`alembic heads` confirms this is current head). Inspected with plain `sqlite3` (this is my own scratch file, not `server/yuno.db`/`.e2e.db`, so the `-readonly` requirement does not apply; `-readonly` was tried first and failed to open a WAL-mode file under this sandbox, so plain `sqlite3` was used against the scratch copy only) — `SELECT count(*)`, `.schema` |
| `server/yuno.db`, `server/.e2e.db` | Read-only review artifacts | Not queried this round beyond confirming they remain untouched (`git status` shows the working tree clean of any write to them); the decision doc under review (`docs/assessment/IDK-009-critical-dimension-exposure.md`) itself cites a `server/yuno.db` read (`rubrics`/`rubric_dimensions`/`hands_on_work` = 0), consistent with the scratch-DB result obtained independently here |
| `server/src/yuno/modules/evidence_evaluation/models.py:220-233,392-406` | `RubricDimensionRow` (no `is_critical` column, by design), `AssessmentDimensionResultRow.is_critical` (persisted, CHECK'd) | Read; corrected comment from `0a6e7c9` verified against the actual schema |
| `server/src/yuno/modules/evidence_evaluation/domain.py:355-390,601-631,705-722` | Critical-dimension precedence (`derive_progress`), `CRITICAL_STABLE_DIMENSION_IDS`, `RubricDimension.is_critical` property, `AssessmentDimensionResult.is_critical` field | Read |
| `server/src/yuno/modules/evidence_evaluation/service.py:70-134` | `prepare_submission`/assessment-write path; site where `RubricDimension.is_critical` is read a second time (denormalization onto `AssessmentDimensionResult`) | Read |
| `server/src/yuno/modules/evidence_evaluation/repository.py:305-325,785-796` | ORM marshalling of `is_critical` to/from `AssessmentDimensionResultRow` | Read |
| `server/src/yuno/api/contracts.py:533-545,1016-1021` | `ProgressDimensionResponse`, `LearningStateExplanationResponse`, `AssessmentDimensionResponse` — confirms `is_critical` is absent from every response contract | Read |
| `docs/assessment/IDK-009-critical-dimension-exposure.md` | New decision record (`0a6e7c9`) | Read in full; every factual claim in it independently re-verified against the tree (see Findings) |
| `docs/decisions/IDK-009-assessment-and-derived-state.md:58,214-235,280-297` | §6 rubric/dimension table, §9.2 precedence rule | Read |
| `docs/decisions/IDK-004-role-level-competencies.md` | Checked for an approved "critical dimension" learner-facing label | `grep -in "critical"` → zero hits |
| `IMPLEMENTATION_TICKETS.md:1015-1163,1421-1469,878-921,2385-2442` | IDK-204/205/206/302/303/304/405/201 ticket bodies and Appendix 1 status vocabulary + dependency table | Read in full |
| `docs/approvals/IDK-503-content-and-safety-review-rerun-2026-08-15.md` | Round 2's consolidated record, cited by the new ticket-status lines | Confirmed the file exists and its B10/B11/B12/B18/B19 rows (lines 74-80) match what the ticket-status reconciliation cites |
| Repo-wide per-ID `rg`/`grep` for all twelve IDK-009 §4 `scenario_id`s, the three rubric IDs, `idk009-v1-r1`, `is_critical`, `likely_known`/`likely-known`, `scenario_status` | Confirms shipped presence/absence of every approved-content identifier | Run individually per ID across the whole repository, not as a single combined pattern |
| `server/tests/unit/test_derived_progress.py` | Full file | `rtk proxy uv run pytest tests/unit/test_derived_progress.py -q` → **7 passed** |
| `server/tests/integration/test_evidence_evaluation.py` | Full file | `rtk proxy uv run pytest tests/integration/test_evidence_evaluation.py -q` → **10 passed** (round 2 reported 17 for this same file; the file has been at 841 lines / 10 tests since commit `6bebd2f`, predating round 2 — round 2's count for this file does not check out) |
| `server/tests/integration/test_hands_on_api.py` | Full file | `rtk proxy uv run pytest tests/integration/test_hands_on_api.py -q` → **7 passed** |
| `server/tests/unit/test_learning_content.py -k reverse` | DEP-03 layer-reversal regression | `rtk proxy uv run pytest tests/unit/test_learning_content.py -k reverse -q` → **1 passed, 1 deselected** |
| `server/src/yuno/modules/hands_on/service.py:100-117` | Every synthesized `HandsOnWork` row's literal `scenario_status`/`scenario_id`/`scenario_source` values | Read; confirms the new IDK-405 ticket-status line's claim exactly |

Note on tooling: this machine's Claude Code hooks route ordinary shell commands through an `rtk` proxy (`~/.claude/RTK.md`) that summarizes/truncates output (observed: `grep` results silently capped with "+N more" written to a log file, and a `pytest --collect-only` run reported as "No tests collected" when tests plainly existed). Where a summarized result looked suspicious, `rtk proxy <cmd>` was used to get raw, unfiltered output before treating a result as verified — this is how the test-file-17-vs-10 discrepancy below was caught rather than propagated.

## Findings

| # | Decision requirement | Shipped reality (re-derived, with citation) | Verdict |
| --- | --- | --- | --- |
| 1 | Three approved rubric manifests with six named stable dimensions, loaded and version-gated (IDK-009 §1, §6; IDK-204) | Scratch DB at `be4d11f03666`: `rubrics` = 0 rows, `rubric_dimensions` = 0 rows. Per-ID `grep` for `hands-on-rubric-v1`/`practice-rubric-v1`/`mock-rubric-v1` across the whole tree: hits confined to `docs/decisions/IDK-009-assessment-and-derived-state.md`, `docs/approvals/IDK-503*` review prose, and `IMPLEMENTATION_TICKETS.md` (itself prose — the ticket's own new status line quoting the three names, `IMPLEMENTATION_TICKETS.md:1018`). Zero hits in `server/`, `src/`, `tests/`, or any migration/seed file. | not shipped — unchanged (B11) |
| 2 | Twelve approved `scenario_id` records, content revision `idk009-v1-r1` (IDK-009 §4-§5; IDK-405/302/303) | Per-ID `grep` for all twelve exact scenario IDs run individually: every hit confined to `docs/decisions/IDK-009-assessment-and-derived-state.md`, `docs/decisions/IDK-001-mvp-curriculum-spine.md`, and `docs/approvals/IDK-503*` review prose, plus one prose mention of `senior-zero-downtime-schema-practice-v1` inside IDK-302's own Scope text at `IMPLEMENTATION_TICKETS.md:1329` (a static-limitation example, not loaded content). `idk009-v1-r1` (content revision): same pattern — zero hits in `server/`, `src/`, `tests/`, or seed data. | not shipped — unchanged (B12) |
| 2a | `scenario_id` field and non-fixture `scenario_status` value exist structurally (IDK-405/302/303 consuming lines) | Scratch DB `.schema hands_on_work`: `scenario_id TEXT` (nullable), `CONSTRAINT ck_hands_on_work_hands_on_scenario_status_valid CHECK (scenario_status IN ('fixture','approved'))` — both present at head, matching `server/src/yuno/modules/hands_on/models.py:31,39` and migration `be4d11f03666_hands_on_scenario_status_and_id.py`. `test_scenario_status_check_admits_approved_and_scenario_id_is_a_nullable_header_column` and `test_approved_scenario_status_and_scenario_id_round_trip_through_repository` (`test_hands_on_api.py:305,380`) both ran green. Every code path that synthesizes a `HandsOnWork` row still hardcodes the fixture triple: `service.py:105-117` constructs `HandsOnWork(..., "fixture", None, "fixture-pending-idk-009", timestamp)` unconditionally when no prior work exists. | shipped — schema only, content still absent (unchanged) |
| 3 | `'approved'`, the `scenario_status` literal, must trace to decision text (B18) | `grep -n "scenario_status" docs/decisions/IDK-009-assessment-and-derived-state.md` → zero hits. Migration docstring (`be4d11f03666_hands_on_scenario_status_and_id.py:8-13`) states this itself: "`'approved'` is a naming choice this migration makes, not a literal IDK-009 specifies... IDK-009 never names a field called `scenario_status`." Test docstring (`test_hands_on_api.py:308-312`) repeats the same self-disclosure. Unchanged from round 2; still an unreviewed naming choice, still correctly self-disclosed, still not something engineering can close on its own. | confirmed unreviewed naming choice — unchanged (B18) |
| 4 | Closed five-value outcome vocabulary (IDK-009 §6) | `DimensionOutcome` (`domain.py:29-34`) has exactly five members. Scratch DB CHECK: `outcome IN ('pass','trade-off','factual-correction','not-demonstrated','ambiguity-unresolved')`. `test_not_demonstrated_is_the_fifth_closed_outcome_vocabulary_member` ran green as part of the 7-passed `test_derived_progress.py` run. | shipped — unchanged |
| 5 | IDK-009 §9.2 critical-dimension precedence: either critical dimension negative forces `unverified` ahead of every other row | `domain.py:373-380`: `critical_negative = any(dimension.is_critical and dimension.outcome in negative_outcomes for dimension in clear_dimensions)`, checked ahead of the plain-negative/`partial` branch. `test_critical_dimension_negative_outcome_forces_unverified_over_partial` (`test_derived_progress.py:59-102`) re-run green: constructs the adversarial case (critical-negative alongside a same-assessment positive on the other dimension) and its mirror (same negative, non-critical) and asserts `UNVERIFIED` vs. `PARTIAL` respectively. `test_not_demonstrated_outcome_persists_with_its_critical_dimension_flag` (`test_evidence_evaluation.py:328-454`, assertions at 449-454) re-run green, proving `is_critical` survives a real `perform_assessment` → repository → DB → readback round trip (`critical_result.is_critical is True`, `noncritical_result.is_critical is False`). `CRITICAL_STABLE_DIMENSION_IDS` (`domain.py:607-609`) = `{"factual-and-mechanical-correctness", "assumptions-and-constraints"}`, matching IDK-009 §6's table exactly. | shipped and re-verified — unchanged |
| 6 | Persisted classification values are exactly `likely_known`/`partial`/`unverified`/`new` (IDK-009 §2) | Still `likely-known` (hyphen) everywhere in code; `likely_known` (underscore) appears only in `PRD.md:262,360` and `IMPLEMENTATION_TICKETS.md:844,1083` (prose, never code). Re-confirmed independently at every site round 2 named, each now at a slightly shifted line number (a direct consequence of `0a6e7c9` and other commits editing the same files) — `domain.py:12,48` (two enums), `models.py:605` (memo CHECK, was `:600` in round 2), `roadmap/domain.py:59`, `roadmap/models.py:203,283`, `diagnostics/service.py:303`, three migrations (`6834679f0af4:40,127,339`; `e205f6a2c4d1:34`; `e10d1a0c0100:575-587`, unchanged — not touched since round 2), `schema.d.ts:3461,3847,4495`, `CorePages.tsx:411,462` (learner-facing `<option value="likely-known">`, shifted from round 2's `:409,460` by the `cbd4c16` onboarding commit editing the same file). Also newly noted: `test_evidence_transfer_delete.py:258` asserts `classification.value == "likely-known"` directly — an eighth code site round 2's list did not enumerate, though it is a test assertion rather than a new production surface. | mismatch — unchanged (B19) |
| 7 | DEP-03: a later mental-model layer may refine but never reverse an earlier claim | `learning_content/domain.py:335,338,363` (`MentalModelLayer.reverses_claim_ids`, `validate_layer_progression`). `test_later_layer_may_refine_but_never_reverse_an_earlier_claim` (`test_learning_content.py:36`) re-ran green. File untouched by any commit since round 2. | matches — unchanged |

### The `is_critical` exposure decision, assessed on its merits

`docs/assessment/IDK-009-critical-dimension-exposure.md` (added by `0a6e7c9`) directly answers round 2's residual note that `is_critical` "cannot currently be inspected or corrected via any API or UI." Every factual claim in it was independently re-checked:

- "`grep -c is_critical server/openapi.json src/shared/api/schema.d.ts` → 0 and 0" — reproduced exactly; a repo-wide `grep -rn is_critical src/` and `grep -rn is_critical server/src/yuno/api/` also both return zero hits.
- `AssessmentDimensionResponse` fields (`contracts.py:1016-1021`) — reproduced: `dimension_id`, `outcome`, `rationale`, `evidence_refs`, no `is_critical`.
- `RubricDimension.is_critical` as a derived property, not a stored column (`domain.py:622-631`; `rubric_dimensions` has no such column, `models.py:229-232`) — reproduced exactly.
- IDK-009 §6's invariance claim ("all three rubric versions use the same stable dimensions") and the "first two common dimensions are critical" line — both verbatim at `docs/decisions/IDK-009-assessment-and-derived-state.md:214,58` — reproduced.
- No approved learner-facing "critical dimension" label exists in IDK-009 or IDK-004 — `grep -in "critical"` against `docs/decisions/IDK-004-role-level-competencies.md` returns zero hits; IDK-009's only "critical" occurrences are the §6/§9.2 derivation-rule text quoted above, never UI copy.

**Two citation inaccuracies were found at `796fa30` (this gate's first pass) and corrected at `7b805bd`** — see "HEAD delta check" above for the re-derivation confirming both fixes are accurate:

1. The record originally cited `server/src/yuno/modules/evidence_evaluation/models.py:401` for the persisted `assessment_dimension_results.is_critical` column; the actual line was **406** — the comment `0a6e7c9` itself added is five lines longer than the one it replaced, which the record's original citation did not account for. `7b805bd` corrects the citation to `:406`, confirmed accurate by direct `grep -n` against the current tree.
2. The record originally stated `is_critical` "is read by exactly one consumer: `derive_progress`'s IDK-009 §9.2 precedence check (`domain.py:378`)," which undercounted two plumbing read-sites (`service.py:384`, `repository.py:318`/`:795`). `7b805bd` rewrites this to say one consumer *decides* anything on it (`domain.py:378`, the only conditional that changes a classification outcome) while the other three only carry the value between layers — confirmed accurate by re-deriving all four `is_critical` read/write sites directly (see "HEAD delta check" above).

**On soundness and completeness of the reasoning**, independent of those two citation slips: the decision is well-scoped and its argument holds. It correctly identifies `is_critical` as a fixed invariant of the approved decision (§6) rather than authored/settable data, correctly observes that a settable version would let a rubric manifest contradict IDK-009, correctly identifies that the *outcome* the flag drives (the topic/goal cell classification) is already exposed with its own `definition`/`uncertainty` fields (`contracts.py:533-545`, reproduced), and correctly finds no approved copy exists for a "critical dimension" label — citing `AGENTS.md`'s bar against speculative surfaces, which is apt. It also does not overclaim: it explicitly states it does **not** know whether the shipped `uncertainty` text actually names the responsible critical dimension for a learner, flags that as unverifiable in this tree (rubrics/rubric_dimensions/hands_on_work all hold zero rows — reproduced independently above), and names a concrete revisit trigger (IDK-204 shipping the rubric manifests). The record also correctly disclaims approval authority — "This record grants no approval and changes no approved decision document" — which is the right posture for what is an engineering/API-design decision about a value IDK-009 §6 already fixes, not a new content or policy decision requiring the editorial approver's signature. **It settles the narrow question it was written to settle** (should `is_critical` be API-surfaced — no, by design) and is honest about the one thing it leaves open (whether the classification's `uncertainty` text is legible enough to explain *why* to a learner) rather than papering over it.

The comment correction in the same commit (`server/src/yuno/modules/evidence_evaluation/models.py:398-405`, replacing a comment that cited a `rubric_dimensions.is_critical` column) is itself accurate: `rubric_dimensions` deliberately has no such column (`models.py:229-232`, confirmed), so the old comment was describing a column that does not exist, and the new comment's language ("the derived property, not a column") matches the actual code shape.

### Ticket-status reconciliation, verified

`IMPLEMENTATION_TICKETS.md`'s four new "Content incomplete" status lines (IDK-204 at line 1018, IDK-302 at 1314, IDK-303 at 1370, IDK-405 at 1686) and the Appendix 1 vocabulary addition (lines 2394-2396) plus dependency-table rows (2422, 2428, 2429, 2435) were checked against the tree directly, not against the ticket text's own framing:

- **IDK-204**: "`rubrics`/`rubric_dimensions` hold zero rows" — reproduced on my own freshly migrated scratch DB (0/0), not merely on the stale `server/yuno.db` the ticket cites. Correct.
- **IDK-302/IDK-303**: "the three approved Practice/Mock scenario records... exist nowhere outside `docs/`, and IDK-204's rubric registry it consumes is itself unshipped" — both halves reproduced (per-ID grep above; IDK-204 finding above). Correct.
- **IDK-405**: "every synthesized `HandsOnWork` row still hardcodes `scenario_status="fixture"`, `scenario_id=None`" — reproduced exactly at `service.py:105-117` (positional constructor arguments `"fixture"`, `None`, `"fixture-pending-idk-009"`), and "`hands_on_work` holds zero rows" reproduced on the scratch DB. Correct.
- **Appendix 1 vocabulary** (`IMPLEMENTATION_TICKETS.md:2394-2396`): the new `Content incomplete` definition — "the ticket's code paths are implemented and its Appendix 8 automated tests pass, but at least one acceptance criterion in the ticket's own `Scope` requires loading approved content that does not exist anywhere in the tree" — is coherent with, and cleanly distinct from, the existing `Complete` definition (which explicitly excludes manual-review completion but says nothing about missing content). It does not collide with or redefine any other vocabulary term.
- **Citation target**: `docs/approvals/IDK-503-content-and-safety-review-rerun-2026-08-15.md`, cited by all four new status lines, is a real file (confirmed to exist, 24.3K) — the round-2 consolidated record, not a broken or invented path. Its findings register (lines 74-76) attributes B10 (content half) to IDK-405, B11 to IDK-204, and B12 (content half) to IDK-405/IDK-302/IDK-303 — exactly matching how the four ticket lines attribute the findings to themselves.

**Are there other `Complete` tickets with the same defect?** Checked every ticket whose Appendix 1 dependency-table row names IDK-009 as a dependency: IDK-302, IDK-303, IDK-304, IDK-405 (plus IDK-503 itself, a review-gate ticket, not in scope for this check). IDK-304 ("Terminal-only consolidated Reports," `IMPLEMENTATION_TICKETS.md:1421-1469`) remains `Complete` and depends on IDK-009, but its own Scope (`:1431-1437`) is report-display machinery gated on `interview_runs.state='completed'` that consumes whatever assessment shape exists — it does not require the twelve scenario records or rubric manifests to exist for its own acceptance criteria or automated tests to pass; its one reference to real content is explicitly filed as a **Manual** test (`:1461`, "for at least one real (non-fixture) completed Mock run"), which the vocabulary's own `Complete` definition explicitly excludes from the bar. This is not the same defect. Also checked IDK-201 (`:878-921`, the other ticket gate 5's own scope references for DEP-03): its Approval gate note (`:917-918`) explicitly states "this ticket's workspace-contract acceptance uses fixture content and does not require [IDK-001/IDK-002 real content]" — self-scoped the same way, correctly. And IDK-206 (`:1117-1163`, owns `review-schedule-v1`): its Scope is the review/notebook scheduling mechanism, not scenario/rubric content, and operates on whatever assessment evidence is submitted regardless of whether that evidence traces to IDK-009 content. **No additional `Complete` ticket with the same defect was found within this check.** This is not an exhaustive audit of all ~45 tickets in the file — only those with a direct, table-declared IDK-009 dependency plus the two tickets gate 5's own scope already names — so a ticket outside that set with a hidden, undeclared content dependency would not have been caught by this check.

## Blocking findings

### 1. No approved rubric manifests shipped (B11 — still open)

- What is missing: `rubrics`/`rubric_dimensions` rows for `hands-on-rubric-v1`, `practice-rubric-v1`, `mock-rubric-v1` with `status='approved'` and IDK-009 §6's six stable dimensions, correctly marking the first two critical.
- Owning ticket: IDK-204, correctly now marked `Content incomplete` in `IMPLEMENTATION_TICKETS.md:1018` (reconciled by `0a6e7c9`, verified accurate above).
- Evidence of absence: scratch DB at head `be4d11f03666` — `rubrics` = 0, `rubric_dimensions` = 0; repo-wide per-ID grep for the three rubric IDs returns zero hits outside decision/approval-review prose.
- What would clear it: IDK-204 ships a migration/seed or boot-time loader inserting the three `status='approved'` rubric headers and their six-dimension sets matching IDK-009 §6 verbatim, then this gate re-inspects the live rows in a freshly migrated database.

### 2. Twelve approved scenario records are still unauthored (B12 — schema half closed, content half still open)

- What is missing: the twelve `scenario_id` records from IDK-009 §4-§5, content revision `idk009-v1-r1`, persisted as real rows with `scenario_status='approved'`.
- Owning tickets: IDK-405, IDK-302, IDK-303, correctly now marked `Content incomplete` (verified accurate above). The schema half genuinely shipped and stays shipped this round: `scenario_id`/`scenario_status IN ('fixture','approved')` confirmed on the scratch DB and exercised by two green tests (`test_hands_on_api.py:305,380`).
- Evidence of absence: per-ID repo-wide grep for all twelve exact scenario IDs and for `idk009-v1-r1` finds them only in decision/approval-review prose; zero hits in `server/`, `src/`, `tests/`, or any migration/seed; every synthesized `HandsOnWork` row still hardcodes the fixture triple (`hands_on/service.py:105-117`).
- What would clear it: IDK-405/IDK-302/IDK-303 author and load the twelve approved scenario records verbatim from IDK-009 §5 with `scenario_status='approved'` and correct `scenario_id`s, and this gate confirms the persisted rows against IDK-009 §5's normative text field-by-field.

### 3. `'approved'` scenario_status literal and the `likely-known`/`likely_known` mismatch need the approver's decision, not code (B18, B19 — both unchanged, not elevated to numbered defects by this gate)

- **B18**: `docs/decisions/IDK-009-assessment-and-derived-state.md` never names a `scenario_status` field or a non-fixture literal for one (`grep -n "scenario_status"` → zero hits). The migration and a test both self-disclose this (`be4d11f03666_hands_on_scenario_status_and_id.py:8-13`; `test_hands_on_api.py:308-312`). Note: the round-2 consolidated record (`docs/approvals/IDK-503-content-and-safety-review-rerun-2026-08-15.md:79`) files B18 under gate 4, though the literal in question is squarely the hands-on/gate-5 schema; this gate carries it forward regardless of which gate number owns the row, per this round's brief.
- **B19**: IDK-009 §2 states verbatim: "The only persisted classification values are `likely_known`, `partial`, `unverified`, and `new`." Every shipped occurrence still uses a hyphen (`likely-known`), confirmed unchanged across two domain enums, one denormalized-memo CHECK, two `roadmap` CHECKs, one bare diagnostics string, three migration CHECKs, three generated TypeScript union types, two learner-facing `<select>` controls, and one test assertion (`test_evidence_transfer_delete.py:258`, newly noted this round) — see Finding 6 above for the full re-derived citation list. `likely_known` (underscore) appears only in `PRD.md` and `IMPLEMENTATION_TICKETS.md` prose, never in code.
- Neither is a code defect this gate can assign to an owning ticket for a fix; both require the designated editorial approver to either accept the shipped spelling/naming or commission the (for B19, cross-table) migration work. Recorded here, not elevated to a numbered blocking finding, per this gate's brief.

## Notes and residual risk

- **`is_critical` exposure is now a recorded, reasoned decision, not an open gap** — see the dedicated assessment section above. It settles the question it addresses; it does not (and does not claim to) verify that a learner can trace an `unverified` classification back to a specific critical-dimension failure via the shipped `uncertainty` text, because no rubric content exists yet to test that against. The two citation slips this gate's first pass found in the record (`models.py:401`→406; "exactly one consumer" undercounting two plumbing read-sites) were corrected in `7b805bd` — re-derived and confirmed accurate, see "HEAD delta check" above. Neither slip, before or after correction, affected the record's underlying conclusion.
- **Ticket-status reconciliation is accurate.** All four new `Content incomplete` status lines and the Appendix 1 vocabulary/dependency-table changes were independently verified against the tree, not accepted from the commit message. `docs/approvals/IDK-503-content-and-safety-review-rerun-2026-08-15.md`, cited by all four, is a real, existing file whose findings register matches the citations. No other `Complete` ticket was found to share the defect within the set checked (IDK-201, IDK-206, IDK-302, IDK-303, IDK-304, IDK-405) — see the caveat above about this not being an exhaustive audit of the full ticket file.
- **Five of the six commits in this round's diff do not bear on gate 5** — `bd05da6`, `cbd4c16`, `5596654` (frontend/CLI, gates 3/4 territory), `796fa30` (import-linter contract + frontend prop cleanup), and `7b805bd`'s own non-`is_critical` changes (gate-4 bundle-name copy fix, gate-7 flaky-test repair — see "HEAD delta check" above). Confirmed via `git show --stat`/`git diff` that none touch `evidence_evaluation/`, `hands_on/`, `roadmap/`, `diagnostics/`, or any IDK-009 content path. Only `0a6e7c9` and the `docs/assessment/IDK-009-critical-dimension-exposure.md` slice of `7b805bd` bear on gate 5.
- **Round 2's test-count citation for `test_evidence_evaluation.py` does not check out.** Round 2 reported "17 passed" for this file; re-run this round gives 10 passed, and `git log` shows the file has been 841 lines / 10 tests since commit `6bebd2f`, which predates round 2's own `HEAD`. This is recorded as a gap in round 2's citation accuracy, not a regression — the file's content and behavior are unchanged and this round's 10-pass result is the one to trust going forward.
- **A local tool caveat.** This machine's shell hook routes commands through an `rtk` proxy that summarizes output; a `pytest --collect-only` run was mis-summarized as "No tests collected" for a file that plainly has tests, and `grep` results were silently truncated with output diverted to a log file. `rtk proxy <cmd>` was used to get raw output wherever a summarized result looked suspicious, and every test count and grep result quoted in this report was obtained that way. Anyone re-running these commands directly (without the proxy) should see the same raw counts reported here.
- **Migration to head.** `cd server && YUNO_DATABASE_URL="sqlite+pysqlite:////private/tmp/.../scratchpad/gate5_scratch.db" uv run alembic upgrade head` reached `be4d11f03666`, confirmed current via `alembic heads`. No write was made to `server/yuno.db` or `server/.e2e.db`.
- **DEP-03 remains the one clean sub-scope of this gate**, unaffected by every commit in this round's diff, re-run green.
- Nothing in this round's inspection could not be verified; every claim above traces to a file:line, a command actually run, or a named test actually executed this round.
