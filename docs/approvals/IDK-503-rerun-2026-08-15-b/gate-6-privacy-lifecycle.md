# IDK-503 gate 6 (round 3, `-b`) — Combined size/retention and export/delete/logging lifecycle

- Gate: Size/retention and export/delete/logging lifecycle
- Reviewer role required: product/privacy owner
- Inspection date: 2026-08-15
- Disposition: inspection-passed-pending-attestation
- Attestation: pending -- the product/privacy owner has not signed this gate. No attestation was sought or recorded during this inspection. Reaching "inspection-passed-pending-attestation" here means only that this engineering re-inspection found no blocking finding against `docs/decisions/IDK-010-data-lifecycle-policy.md`; it is not an approval.

Decision under review: `docs/decisions/IDK-010-data-lifecycle-policy.md`, policy version 1.0, approved 2026-08-13. HEAD at inspection: `7b805bdd72d5740eb8180f3e6fde9c33af2a672f` (moved from `796fa306b6eb69745c4aad3b3a3ac64997c845d6` during this inspection; see the delta-check section below). This is round 3, a fresh re-run over `git log 2621d29..HEAD` (6 commits since round 2's HEAD `2621d29`, plus the one delta commit `7b805bd`), not an amendment. Round 2's file (`docs/approvals/IDK-503-rerun-2026-08-15/gate-6-privacy-lifecycle.md`) was read once, only to recover scope; none of its citations are relied on below -- every line number, row count, and test result in this document was re-derived against the current tree.

## HEAD delta check (`796fa30` → `7b805bd`)

HEAD moved to `7b805bd` ("fix: three defects the round-3 gate inspections found") after the rest of this document's inspection was complete. Checked before restamping anything, rather than assumed:

- `git diff 796fa30 7b805bd --stat` → four files: `docs/assessment/IDK-009-critical-dimension-exposure.md` (4 lines), `server/tests/integration/test_runner.py` (20 lines), `src/selected/core/CorePages.tsx` (11 lines), `src/selected/core/InterviewHub.test.tsx` (11 lines).
- `git diff 796fa30 7b805bd -- server/src/` → **empty**. Nothing in the export/delete/logging/retention surface, `config.py`, the provenance module, or the license-revocation purge path moved. Every citation to `server/src/` earlier in this document is unaffected.
- The one server-side change, `server/tests/integration/test_runner.py`, is test-only: it adds a `_is_request_thread()` helper (checking `threading.current_thread().name.startswith("jobs-")`) so `test_retired_relational_language_rejected_before_route_or_uow`'s two SQLAlchemy event listeners (`_on_cursor_execute`, `_on_checkout`) stop attributing the background `DurableJobDispatcher` lanes' own queue-polling SQL to the request under test. Read the full diff (`git show 7b805bd -- server/tests/integration/test_runner.py`) directly: it touches only the test's own listener callbacks. It does not modify `DurableJobDispatcher`, `jobs_events/service.py`, the `jobs-<lane>` thread naming/polling behavior, or `_execute_external_cleanup` in any way -- the dispatcher's actual runtime behavior is unchanged, confirmed by reading the diff rather than the commit message alone. This is gate 7's fix for a flaky assertion the gate 7 inspection found (`assert statements == []` failing 3 of 6 runs because it was catching the dispatcher's own polling), not a change to what the dispatcher does. **Conclusion: my residual-risk note on the now-reachable purge's dependence on a live `DurableJobDispatcher` (either its hourly sweep or its post-job-dispatch `_execute_external_cleanup` call) needs no revision -- the mechanism this note describes is unchanged by `7b805bd`.**
- The other two changed files (`CorePages.tsx`, `InterviewHub.test.tsx`) are gate 4's fix (`BundleEditor.createBundle` no longer interpolates the chosen level into a generated bundle name; grepped the diff for `export|delete|retention|logging|purge|cleanup` -- no hits beyond the unrelated JS `export function` keyword, already the pattern seen in this round's other frontend commits). `docs/assessment/IDK-009-critical-dimension-exposure.md`'s 4-line change is gate 5's citation correction (a line-number fix and a consumer-count correction in a document about `is_critical`/rubric assessment). Read both diffs directly; neither touches export, delete, logging, or retention.
- Net effect on this gate: none. No finding, no citation, and no test result recorded earlier in this document changed. Disposition remains `inspection-passed-pending-attestation` on the same evidence as before the delta.

## Inspected artifacts

| Artifact | What it is | How inspected |
| --- | --- | --- |
| `git log 2621d29..HEAD` (6 commits: `5596654`, `0a6e7c9`, `bd05da6`, `cbd4c16`, `796fa30`) | Every commit shipped since round 2 | Read each commit's full diff (`git show --stat`, targeted `git show -- <path>`) and classified against this gate's scope |
| `server/src/yuno/modules/provenance/service.py:142-202` (`withdraw_source`) | New grant check (`5596654`): `grants = uow.owners.grants(owner_id)` / `RolePolicy.require(grants, Role.DESIGNATED_EDITORIAL_APPROVER)` at lines 190-191, before any read or write | Read full diff (`git show 5596654 -- server/src/yuno/modules/provenance/service.py`) and resulting source |
| `server/scripts/withdraw_source.py` (new, 175 lines) | Offline editorial CLI; the first production caller of `withdraw_source` | Read in full |
| `server/src/yuno/modules/identity/domain.py:28-53` (`Role`, `RolePolicy.require`), `server/src/yuno/modules/identity/repository.py:83-86` (`grants`), `server/src/yuno/shared/domain/errors.py:189` (`RoleNotGrantedError`, 422) | The grant-check mechanics `withdraw_source` now depends on | Read in full |
| `server/tests/integration/test_withdraw_source_script.py` (new, 7 tests), `server/tests/integration/test_provenance_withdrawal.py` (1 new test) | CLI-level and service-level coverage of the new grant check, including the fail-closed negative case | Ran `uv run pytest tests/integration/test_provenance_license_purge.py tests/integration/test_provenance_withdrawal.py tests/integration/test_withdraw_source_script.py -q` from `server/` → **23 passed**, exit 0 |
| `server/tests/integration/test_provenance_availability_transitions.py`, `test_provenance_source_updates.py` | Withdrawal state machine and source-update tests, unrelated to the grant check but part of the same module | Ran `uv run pytest tests/integration/test_provenance_availability_transitions.py tests/integration/test_provenance_source_updates.py -q` → **18 passed**, exit 0 |
| `server/tests/unit/test_config.py`, `test_structured_logging.py`, `test_data_lifecycle.py`, `test_evidence_transfer_delete.py`, `test_policy_retention_repository.py` | Size/retention config, export/tombstone, delete-preflight, and logging tests -- confirms the core lifecycle machinery this gate governs is untouched and still green | Ran `uv run pytest tests/unit/test_config.py tests/integration/test_structured_logging.py tests/integration/test_data_lifecycle.py tests/integration/test_evidence_transfer_delete.py tests/integration/test_policy_retention_repository.py -q` → **35 passed**, exit 0 (same count as round 2) |
| `server/tests/integration/test_alembic_representative_upgrades.py` | Representative-DB migration upgrade coverage | Ran → **34 passed**, exit 0 |
| `uv run lint-imports` | The "Module independence" import-linter contract, which `796fa30` amended to let `provenance.service` import `identity.domain` | Ran from `server/` → **"Contracts: 4 kept, 0 broken."** |
| `git log 2621d29..HEAD -- src/yuno/config.py src/yuno/modules/data_lifecycle/ src/yuno/modules/settings_data/ src/yuno/modules/evidence_evaluation/service.py src/yuno/shared/infrastructure/structured_logging.py src/yuno/modules/provenance/` | Confirms which shipped commits touched this gate's core surfaces | Empty result for every path except `src/yuno/modules/provenance/` (only `5596654`, and only `service.py` within it -- `provenance/adapters.py`, the purge primitive itself, is untouched) |
| `server/src/yuno/modules/data_lifecycle/service.py:116-141` (`execute_pending_cleanup`), `:199-221` (`_resolve_cleanup_path`), `:37-46` (`ApprovedCleanupRoots`) | The out-of-transaction cleanup-intent machinery `purge_license_revoked_snapshot_bodies` writes into | Read in full; confirmed byte-for-byte unchanged since round 2 (see git-log check above) |
| `server/src/yuno/api/app.py:153,252-254,293-297,316-319,868` | `RETENTION_INTERVAL_SECONDS = 3600`; `cleanup_roots` wiring (`source=resolved_settings.source_snapshot_root`); `apply_external_cleanup`; `periodically_apply_retention`; the `asyncio.create_task` that starts it | Read in full |
| `server/src/yuno/modules/jobs_events/service.py:601-608` | `_execute_external_cleanup(row.owner_id)`, called after every job's dispatch (success or failure), not only on a timer | Read in full |
| `server/src/yuno/modules/provenance/adapters.py:292-378` (`purge_license_revoked_snapshot_bodies`) | Unchanged purge primitive (still `provenance.adapters`, not touched by any commit since round 2) | Read in full |
| Scratch DB migrated to head: `/private/tmp/.../scratchpad/gate6_scratch.db` | Confirms schema reality independent of the stale `server/yuno.db`/`server/.e2e.db` review artifacts | `YUNO_DATABASE_URL=sqlite+pysqlite:////private/tmp/.../scratchpad/gate6_scratch.db uv run --directory server alembic upgrade head` → succeeds cleanly through `be4d11f03666`; `sqlite3` schema queries below |
| `docs/decisions/IDK-003-source-licensing-and-snapshot-policy.md` §6 | The sibling decision naming the license-revocation purge category (IDK-010 has no jurisdiction over `sources`/`source_snapshots`) | Read; grepped `IDK-010-data-lifecycle-policy.md` for `source_snapshot`/`sources` -- only unrelated hits (the word "sources" as in citations, and a `Sources:` document-reference line) |
| `git merge-base --is-ancestor 018ecd8 HEAD`; `git show --stat 018ecd8` | Confirms the carried-forward B20 drift note is still live and unmodified | Ran; `018ecd8` is an ancestor of HEAD, commit unmodified |

## Findings

| # | Decision requirement | Shipped reality (with citation) | Verdict |
| --- | --- | --- | --- |
| 1 | Size/count and retention config values still match IDK-010 §14.1/§14.2 | `git log 2621d29..HEAD -- src/yuno/config.py` is empty -- the file is byte-for-byte unchanged since round 2, which itself confirmed every `Literal[...]` value against §14.1/§14.2 | matches (unchanged, re-confirmed by empty diff rather than re-reading every value) |
| 2 | Export inventory/tombstone marking, delete-preflight immutability, log allowlist/redaction/rotation, and their tests | `git log 2621d29..HEAD` touches none of `settings_data/`, `evidence_evaluation/service.py`, `shared/infrastructure/structured_logging.py`, `data_lifecycle/`; re-ran the same five-file test set round 2 ran → 35 passed, identical count | matches |
| 3 | New (`5596654`): `withdraw_source` now has a production, non-test caller -- does the purge it gates now actually fire outside a test? | Yes. `server/scripts/withdraw_source.py` is a real offline CLI (`argparse`, `require_single_head`, `create_engine_for`/`create_session_factory`, a genuine `main()` entry point at module scope, `if __name__ == "__main__"`). `test_license_revoked_withdrawal_purges_body_and_retains_snapshot_metadata` (`test_withdraw_source_script.py:160`) drives `main([...])` itself (via `importlib`, not a direct `withdraw_source` call) and asserts the `source_snapshot_bodies` pointer row count drops from 1 to 0 while `source_snapshots` metadata (`content_hash`/`retrieved_at`/`status`) is retained -- passed | confirmed reachable |
| 4 | New: does the grant check fail closed -- no write on a missing/insufficient grant? | `withdraw_source` (`provenance/service.py:190-191`) calls `uow.owners.grants(owner_id)` then `RolePolicy.require(...)` **before** `uow.provenance.get_source(...)` and before any write, inside the same `with uow_factory() as uow:` block -- no read or write of `sources`/`source_snapshot_bodies` happens if the grant is absent. `uow.owners.grants` (`identity/repository.py:83-86`) is a plain owner-scoped `SELECT`; an owner with no matching role row returns an empty `frozenset`, which `RolePolicy.require` (`identity/domain.py:50-53`) turns into `RoleNotGrantedError` (`errors.py:189`, HTTP 422, a `YunoError` subclass). Two independent tests exercise this: `test_withdrawal_is_refused_when_the_actor_lacks_the_approver_grant` (`test_provenance_withdrawal.py`, calls `withdraw_source` directly, asserts `RoleNotGrantedError` and that the source is left `available` with no `withdrawal_reason`) and `test_withdrawal_refused_when_actor_lacks_the_grant_exits_1` (`test_withdraw_source_script.py:265-294`, drives the CLI's `main()`, asserts exit code 1, `"[role_not_granted]"` in stderr, and the same untouched-source assertions). Both passed. | fails closed, confirmed at both the service and CLI layer |
| 5 | New: is the grant check itself bypassable by a future caller? | The check lives inside `withdraw_source` (the one function every caller goes through), not only in the CLI wrapper -- confirmed by reading `provenance/service.py:142-202` directly; the CLI (`scripts/withdraw_source.py`) contains no independent authorization logic of its own, it only supplies `--actor-owner-id`. No HTTP route calls `withdraw_source` (`rg "withdraw_source\|withdraw" server/src/yuno/api/routes/*.py` → one unrelated hit in `provenance.py:129`, a `ConflictError` message string, not a call site) | not bypassable by a second caller; still zero HTTP surface |
| 6 | New: does this break the "Module independence" import-linter contract, and if so how was it resolved? | `provenance.service` importing `identity.domain` (for `Role`/`RolePolicy`) broke the contract; `796fa30` added `"yuno.modules.provenance.service -> yuno.modules.identity.**"` to `server/pyproject.toml`'s `ignore_imports`, mirroring the pre-existing `canonical.publisher -> identity` edge for the identical reason (an offline tool checking the acting owner's grant). Ran `uv run lint-imports` myself → "Contracts: 4 kept, 0 broken." | resolved, independently re-verified by running the linter (not by trusting the commit message) |
| 7 | Now-reachable purge: does it change anything about IDK-010 §5-9's approved values? | No. `git log 2621d29..HEAD -- src/yuno/config.py` and every §5-9 enforcement surface (`data_lifecycle/`, `settings_data/`, `evidence_evaluation/service.py`, `structured_logging.py`) is empty -- no size, count, retention, export, delete, or logging value changed. `purge_license_revoked_snapshot_bodies` (`provenance/adapters.py:292-378`) itself is also untouched this round; only its caller (`withdraw_source`) gained a caller of its own. IDK-010 still names no category for `sources`/`source_snapshots` at all (grepped the whole decision doc for `source_snapshot`/`sources`; only unrelated hits) -- unchanged from round 2's finding 9, re-confirmed | no change to any IDK-010 §5-9 value |
| 8 | Now-reachable purge: does it change anything about the `file_cleanup_intents` mechanism itself? | The mechanism is unchanged code (`data_lifecycle/service.py:116-141` `execute_pending_cleanup`, `:199-221` `_resolve_cleanup_path`), but it now has a genuinely new *kind* of writer: every previous writer of a `file_cleanup_intents` row (goal deletion, generated-artifact cleanup, etc.) is an in-process API/job call, which by definition only happens while the API server is running. `scripts/withdraw_source.py` is the first writer that can run **while the server process is stopped** -- it opens its own `Engine`/session factory directly against the database file (module docstring, lines 33-38), independent of `api/app.py`'s lifespan. The pending intent it inserts is only physically actioned once a live server process either (a) runs its hourly `periodically_apply_retention` cycle (`api/app.py:316-319`, `RETENTION_INTERVAL_SECONDS = 3600` at line 153 -- the loop `sleep`s for the full interval *before* its first run, so a freshly (re)started server does not sweep immediately), or (b) dispatches any job at all for that owner, since `DurableJobDispatcher` calls `self._execute_external_cleanup(row.owner_id)` (`jobs_events/service.py:604-606`) after every job's terminal transition, success or failure -- and this is a single-local-owner product, so any job for the one owner triggers it. Net effect: the eventual-consistency window this gate already accepted for goal-body purges (round 2 finding 6/7, unchanged) now also covers an interval that can start while no server process exists at all, with no additional bound beyond "until the server next runs and either an hour passes or any job dispatches." No approved IDK-010 value governs a maximum delay here (§8's "Physical purge" row requires only that failure "remains visible," which it is -- `retention.cycle.completed`/`.failed` structured-log events fire with `diagnostic_classification="external-cleanup-failed"` when any pending intent fails, regardless of which module wrote it). Recorded as residual risk below, not a blocking finding, because it doesn't violate any approved value and the visibility/retry behavior is identical regardless of the intent's origin. | mechanism unchanged; a new class of writer exists; flagged as residual risk, not a violation |
| 9 | Now-reachable purge: does it change anything about §10's required review evidence? | No. §10's dataset list (two goals with transferred evidence, tombstoned evidence, imports, generated artifacts, transcripts, jobs, runner output) names nothing from the `sources`/`source_snapshots`/provenance surface, and its review-package checklist (export sample, delete preflight/completion record, captured logs, size/retention/rotation evidence, Settings copy) is unaffected by this commit -- none of those artifacts changed. §10 remains entirely unmet for reasons unrelated to this round's commits (see Notes) | no change |
| 10 | Schema re-verification: `sources.withdrawal_reason`/`superseded_by_source_id`, `file_cleanup_intents.kind` enum, and the `source_snapshots`/`source_snapshot_bodies` trigger split | Against `gate6_scratch.db` migrated fresh to `be4d11f03666`: `sources` carries `withdrawal_reason TEXT` with `CHECK (... IN ('license-revoked','license-changed-incompatible','publisher-retracted','factually-superseded','registry-declined') OR ... IS NULL)` and `CHECK ((availability_status = 'withdrawn') = (withdrawal_reason IS NOT NULL))`; `file_cleanup_intents.kind` CHECK includes `'source-snapshot'`; `source_snapshots` carries `trg_source_snapshots_no_update`/`_no_delete`/`_no_insert_replace` (fully immutable/undeletable) while `source_snapshot_bodies` carries only `trg_source_snapshot_bodies_no_update` (blocks UPDATE, not DELETE) -- exactly the asymmetry `purge_license_revoked_snapshot_bodies` depends on | confirmed against a freshly migrated scratch DB, not against `server/yuno.db`/`server/.e2e.db` |
| 11 | Other commits this round (`0a6e7c9`, `bd05da6`, `cbd4c16`, `796fa30`'s frontend half) -- do any touch export/delete/logging/retention? | `0a6e7c9`: `docs/assessment/IDK-009-critical-dimension-exposure.md`, `IMPLEMENTATION_TICKETS.md`, and a comment-only fix in `evidence_evaluation/models.py:395-408` (no behavior change) -- gate 5 (rubrics/assessment) territory, not this gate's. `bd05da6`: `TopicContent.test.tsx`/`CorePages.tsx`, rendering IDK-003 §7 citation attribution on a resources tab -- a read surface, not export/delete/logging/retention (confirmed by grepping the diff for those terms: only hits were the JS `export function` keyword). `cbd4c16`: onboarding target-level fail-closed selection -- unrelated UX/validation surface, no lifecycle hits in the diff. `796fa30`'s frontend half: `CorePages.tsx` prop-shape refactor (`TopicTools`), test-only compatibility-default removal per AGENTS.md -- no lifecycle surface touched | none of the four touch this gate's scope |
| 12 | Prior-round drift note (B20): `018ecd8`'s Java-only migration bulk deletion -- still outstanding? | `git merge-base --is-ancestor 018ecd8 HEAD` confirms ancestry; `git show --stat 018ecd8` confirms the commit is unmodified. Re-grepped `IDK-010-data-lifecycle-policy.md` for "migration"/"relational"/"runner-language"/"java_runner" -- no hits. No document has named this category since round 2. | still outstanding, unchanged |
| 13 | `server/yuno.db` / `server/.e2e.db` alembic state | Not re-queried this round (round 2 already established both are behind head and unusable as §10 review evidence as-is; nothing in this round's commits changes that state, and per the task's hard rule any such claim must come from a fresh scratch DB, which finding 10 above provides for schema verification) | not re-verified this round; round 2's finding stands unless someone migrates or reseeds either file |

## Blocking findings

None.

No commit since round 2 changed an approved IDK-010 §5-9 value, weakened the export/delete/logging mechanisms this gate governs, or introduced a size/retention/export/delete/logging behavior inconsistent with policy 1.0. The one substantive change in scope -- `withdraw_source` gaining a production entry point -- adds a new caller to an already-approved-pattern purge mechanism (the same `file_cleanup_intents`/`execute_pending_cleanup` machinery already assessed as matching IDK-010 §8's physical-purge posture), fails closed on the required role grant at both the service and CLI layer (independently re-tested), and does not touch any category IDK-010 itself governs (IDK-010 has no jurisdiction over `sources`/`source_snapshots`; that remains IDK-003's territory, unchanged).

## Notes and residual risk

- **Scope of this re-run.** Round 3, re-run against `git log 2621d29..HEAD` (6 commits: `5596654`, `0a6e7c9`, `bd05da6`, `cbd4c16`, `796fa30`). Round 2's gate-6 file was read once for scope only; nothing in this document copies a citation forward from it.
- **New this round -- residual risk, not a blocking finding: the purge's cleanup intent can now be written while no server process is running at all.** See Findings #8. `scripts/withdraw_source.py` opens its own database connection independent of `api/app.py`'s lifespan, so an editorial operator can run it against a stopped instance's database file. The DB-side state change (source marked `withdrawn`, body pointer row deleted) is immediate and transactional; the actual file unlink is deferred to whenever a server process next either completes an hourly retention sweep or dispatches any job for the owner. This is the same eventual-consistency contract IDK-010 already accepts for goal-body purges (round 2 findings 6-7, unchanged this round), extended for the first time to a writer that isn't itself part of a running server process. No approved numeric value is violated (IDK-010 §8's "Physical purge" row specifies a mechanism and failure-visibility requirement, not a maximum latency), and failure visibility is identical regardless of the intent's origin (`retention.cycle.completed`/`.failed` structured-log events, `api/app.py:283-291`). Flagged for the product/privacy owner's awareness, not as a defect.
- **Drift note (carried forward from round 2, still outstanding, unchanged).** `018ecd8` ("narrow the runner to Java only under approved IDK-005/IDK-008") performs an irreversible, migration-driven bulk deletion of `language='relational'` runner confirmations/records and their exclusively owned inputs/bodies/output chunks, outside any IDK-010 §6/§14.2 expiry schedule. IDK-010 policy 1.0 names no category for this. Re-verified this round: `018ecd8` is still an ancestor of HEAD, unmodified, and no document has named this category since round 2. Recommend the product/privacy owner note this as an accepted category during attestation.
- **Stale status text inside IDK-003 itself (observation, not a gate-6 finding; still true this round).** `docs/decisions/IDK-003-source-licensing-and-snapshot-policy.md:86` still reads "Not implemented — no purge-by-license-event path exists; only reference-count pruning does" for the License-revocation purge row -- now doubly stale, since the purge is not only implemented (true as of round 2) but as of this round also has a real production caller. `IDK-003-source-licensing-and-snapshot-policy.md` is an approved decision document; per this task's hard rules it was not edited, and no gate-6 finding depends on its text being current, since IDK-010 (this gate's decision-of-record) never names the `sources`/`source_snapshots` category in the first place.
- **Section 10's hands-on requirement remains entirely unmet, and closing it is not this round's or this gate's work.** IDK-010 §10 requires the product/privacy owner to personally review one representative local dataset and a review package containing a field-checked export sample, a delete preflight/completion record, captured structured logs, and configured-limit evidence. No commit since the 2026-08-13 baseline (across all three rounds) adds any such artifact: `docs/privacy/IDK-010-policy-1.0-review-evidence.md` does not exist in this tree (confirmed: no file under `docs/privacy/`), and none of this round's six commits touch an export package, a delete-preflight record, or a rotated log file. This remains explicitly **not** represented as done by this document's "inspection-passed-pending-attestation" disposition.
- **`server/yuno.db` and `server/.e2e.db` were not re-queried this round.** Round 2 established both are behind alembic head (`yuno.db` at `4747447ccaa3`, two revisions behind; `.e2e.db` at `a9d4e6f1b208`, eight revisions behind) and therefore cannot exercise the new `withdrawal_reason`/`superseded_by_source_id` columns or the license-revocation purge path at all. Nothing in this round's commits changes either file (both are read-only review artifacts per this task's hard rules; neither was written to). All schema claims in this document (finding 10) were verified against a freshly migrated scratch database instead, per the task's instruction.
- **No approval is granted by this document.** Disposition is bounded to `inspection-passed-pending-attestation` per the task's hard rules; the product/privacy owner has not signed, and this document records that fact rather than closing it.
