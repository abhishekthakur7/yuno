# IDK-010 policy 1.0 engineering map

Policy authority: `docs/decisions/IDK-010-data-lifecycle-policy.md` at approval commit `1aa6be0`. This map transcribes section 14.6 into implementation and verification ownership; it does not change policy values.

| Section 14.6 gap | Enforcement and composition | Schema / cleanup | UI and contract | Primary evidence |
| --- | --- | --- | --- | --- |
| Approved byte and count limits | Typed, assignment-validated `Settings`; import, evidence, generation, interview, runner, overlay, and durable-job services enforce exact boundaries before authoritative publication | Live body rows, rather than lifetime headers, determine retained-count capacity | Settings policy endpoint exposes only the fixed policy 1.0 values and exact error codes remain learner-visible | `test_config.py`; module exact/equal-plus-one, owner-isolation, rollback, and concurrency tests |
| Retention schedule | Startup lifecycle pass follows job reconciliation and the one-hour runner janitor; an hourly coordinator applies database expiry, then retries external cleanup outside SQLite transactions | Forward `e10d1a0c0100` separates removable bodies; owner-scoped retention handles diagnostics, interviews, jobs/attempts/results, SSE, runner output/workspaces, and exports | Settings displays every expiry period; cleanup failures retain safe classifications | `test_policy_retention_repository.py`, `test_data_lifecycle.py`, runner and logging integration suites |
| Append-only reconciliation | Stable header/hash/version/provenance rows stay immutable; governed DELETE applies only to removable body/exhaust rows | Forward migration backfills hashes and body tables, removes obsolete inline columns, and preserves update/append-only triggers | Export represents missing governed bodies explicitly without fabricating content | Alembic single-head, fresh upgrade, metadata parity, append-only and lifecycle tests |
| Complete goal deletion | One database transaction validates the immutable impact, removes all goal-owned database bodies, creates durable file-cleanup intents, tombstones D5 evidence, downgrades dependent LearningState, tombstones the goal, and appends audit facts | App-managed runner/source/quarantine files are removed after commit through hash- and root-validated retryable intents | Confirmation states irreversible/no undelete/no Yuno backup/external-backup caveat/not forensic sanitization; failure remains classified | `test_evidence_transfer_delete.py`, `test_data_lifecycle.py`, body-category deletion and cleanup-intent tests, Settings Vitest/Playwright |
| Portable export 1.0 | Read phase builds one canonical UTF-8 document; short write phase publishes it with SHA-256 over canonical `data`; activation defaults fail-closed until the separate privacy review passes | Package body expires after 24 hours; operation metadata/hash expires after 30 days | Owner-scoped download uses the approved UTC filename; explicit inventory/exclusions and unavailable vocabulary; unsupported majors rejected; Settings truthfully shows the activation gate | `test_portable_export.py`, `test_data_lifecycle.py`, Settings download/gate Vitest/Playwright, OpenAPI drift |
| Owner-only local logs | Strict allowlist/redaction feeds one local rotating file handler and no remote handler/transport | OS application-data directory 0700; files 0600; five files, 10 MiB each, 50 MiB aggregate, 14-day expiry, no archive | Settings states there is no remote support, upload, telemetry forwarding, or support credential | `test_structured_logging.py` including rotation, age, permissions, provider/runner failures, and handler inspection |
| Truthful Settings | Server-supplied fixed lifecycle policy values prevent client drift | No UI-only retention or backup state exists | Displays every approved cap/expiry plus irreversible deletion, no recovery window, no Yuno-managed backup, external-backup caveat, no remote support, and application-level deletion qualification | `test_settings_api.py`, `OperationalPages.test.tsx`, `selected-app.spec.ts` |

Portable-export production activation remains fail-closed until this entire map, the required validation matrix, and the separate manual product/privacy inspection pass. The review-evidence artifact remains unchecked until a reviewer actually performs that inspection; automated engineering success does not open the gate.
